# StringArtIt — Development Orchestrator

## Purpose

This is the single navigation document for building StringArtIt against `docs/specs/`. It sequences work into milestones, states which specs and design-system rules govern each one, states dependencies between milestones, and carries a status table to track progress across sessions. Any session picking up this project should start here, find the first milestone not marked Done, read its listed specs, implement, test, update status, and move on — rather than re-reading all 19 specs cold every time.

Specs live in [`docs/specs/`](../specs/), numbered 00–19. Design reference lives in [`docs/design/editor-mockup.dc.html`](../design/editor-mockup.dc.html). This document does not restate spec content — it only sequences and cross-references it.

## How to Use This Document

1. Find the first row in the [Status Table](#status-table) not marked `Done`.
2. Check its **Depends on** column — those milestones must be `Done` first (or explicitly descoped).
3. Read its **Specs** column in full before writing code.
4. Implement inside the layer boundaries from [01-architecture.md](../specs/01-architecture.md) — domain/geometry code stays framework-free; UI code stays in `src/ui/`.
5. For any UI milestone, also read [18-design-system.md](../specs/18-design-system.md) — tokens/components are not optional polish, they're the spec.
6. Turn every `Gherkin` scenario in the milestone's specs into an actual automated test (Vitest for domain/application logic, a component/interaction test for UI behavior). A milestone is not Done until its specs' test cases are green.
7. Update the Status Table (status + one-line note) before ending the session on that milestone.
8. If implementation reveals a spec is wrong, ambiguous, or incomplete: fix the spec file first, in the same change, then implement against the corrected spec. Specs are living documents, not frozen requirements — but they must never silently drift from what's built.

## Architectural Note: Commands Come Early, Not Last

The product spec's own [Recommended Development Sequence](../specs/00-overview-and-scope.md#recommended-development-sequence) lists the undo/redo Command architecture as "Phase G," near the end. **Do not build it last.** Every mutation in [10-undo-redo.md](../specs/10-undo-redo.md) and the cascading-delete rule in [11-erasers.md](../specs/11-erasers.md) require that document mutations already go through Command objects (per [01-architecture.md](../specs/01-architecture.md)) — retrofitting that onto a codebase full of direct state mutations is a rewrite, not an add-on. This orchestrator therefore pulls the **Command skeleton** into M0 (empty `execute/undo/redo` plumbing + a `HistoryStack`) and requires every milestone from M2 onward to route mutations through it from the start. The full undo/redo *UI and shortcut wiring* (M7) still comes later — only the underlying mechanism moves earlier.

## Milestones

### M0 — Project Scaffolding & Command Skeleton
**Specs:** [01-architecture.md](../specs/01-architecture.md)
**Depends on:** —
**Deliverables:**
- Vite + React 19 project, Node 22 LTS engines pinned (`package.json` `engines`).
- Folder structure per architecture spec: `src/domain/`, `src/application/`, `src/infrastructure/`, `src/ui/`.
- Test runner configured (Vitest recommended — fast, ESM-native, works headless for domain code) and a component-testing setup for `src/ui/`.
- `Command` interface + `HistoryStack` (execute/undo/redo, linear history, redo cleared on new command) in `src/application/commands/`, unit-tested against the generic contract (not yet wired to any real mutation).
- Lint boundary check (even a simple script/ESLint rule) that fails CI if `src/domain/` or `src/application/` imports from `src/ui/` or `react`.
**Exit criteria:** boundary-check script passes on an intentionally-violating test file (proves it actually catches violations); `HistoryStack` unit tests cover execute/undo/redo/redo-invalidation.

### M1 — Geometry Engine
**Specs:** [07-pin-geometry-engine.md](../specs/07-pin-geometry-engine.md), geometry portions of [06-symmetry.md](../specs/06-symmetry.md) (transform math only, not UI), [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (physical↔screen coordinate transform, snapping priority pipeline as pure functions)
**Depends on:** M0
**Deliverables:** `src/domain/shapes/`, `src/domain/paths/`, `src/domain/transforms/`, `src/domain/snapping/`, `src/domain/symmetry/` — path length, point-at-distance, open/closed pin distribution, mirror/radial transforms, grid/pin snapping, snap-radius logic. Pure functions/classes, zero DOM/React.
**Exit criteria:** every Test Cases scenario in 07-pin-geometry-engine.md passes as a unit test, including the exact worked examples (8cm/1cm→9 pins; 7.5cm/1cm→8 pins; 31cm perimeter/2cm→16 pins @1.9375cm). Mirror/radial transform tests from 06-symmetry.md's geometry-only scenarios pass.

### M2 — Board & Viewport
**Specs:** [02-document-model.md](../specs/02-document-model.md) (Board shape), [03-board-configuration.md](../specs/03-board-configuration.md), [04-board-appearance.md](../specs/04-board-appearance.md), [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (full — modes, grid, zoom/pan, status bar), [18-design-system.md](../specs/18-design-system.md)
**Depends on:** M1
**Deliverables:** Board setup flow (shape + dimensions + appearance), main editor shell (top bar, mode switcher, canvas viewport, status bar) rendering an empty board. Physical-unit rendering via M1's coordinate transform. No pins/threads yet.
**Exit criteria:** Test Cases in 03, 04, 05 pass; editor shell visually matches `docs/design/editor-mockup.dc.html` token-for-token (colours/type/spacing per 18-design-system.md).

### M3 — Pin Editor
**Specs:** [08-pin-tools-and-properties.md](../specs/08-pin-tools-and-properties.md), [09-selection-and-editing.md](../specs/09-selection-and-editing.md) (Pin Path portions), [11-erasers.md](../specs/11-erasers.md) (Pin Eraser only — thread cascade deferred to M5)
**Depends on:** M2
**Deliverables:** Pin mode drawing tools (Line/Arc/Ellipse/Circle/Rectangle/Square/Polygon-Star dropdown), Alt constraint modifier, live pin/spacing preview, guide lines, pin property panel, Pin Path selection + geometry editing, Pin Eraser. All mutations go through M0's Command architecture.
**Exit criteria:** Test Cases in 08, 09 (pin portions), and the Pin Eraser scenarios in 11 pass. Every pin-creating/editing action is undoable (manual check against 10-undo-redo.md's list, even though M7 wires the shortcuts).

### M4 — Symmetry
**Specs:** [06-symmetry.md](../specs/06-symmetry.md) (full — UI wiring on top of M1 math)
**Depends on:** M3
**Deliverables:** Symmetry mode controls, live mirrored-copy generation while drawing, movable radial centre, source-edit-regenerates-copies behavior.
**Exit criteria:** Full Test Cases in 06-symmetry.md pass, including "editing source regenerates copies" and "deleting source removes copies in one undo step."

### M5 — Thread Editor
**Specs:** [12-thread-editor.md](../specs/12-thread-editor.md), [11-erasers.md](../specs/11-erasers.md) (Thread Eraser + cascading pin-delete-removes-threads rule, now that both Pin and Thread objects exist)
**Depends on:** M3
**Deliverables:** Thread mode, nearest-pin detection + highlight states, thread preview, click/double-click/right-click/Esc termination, 1/2/3-colour spiral rendering, Thread Eraser, cascading delete.
**Exit criteria:** Full Test Cases in 12 pass; cascading-delete scenarios in 11 pass as single undo steps. Nearest-pin lookup implementation notes the spatial-index performance requirement from 07-pin-geometry-engine.md §Performance even if a naive O(n) scan ships first — file it as a tracked follow-up, not silently ignored.

### M6 — Layers
**Specs:** [13-layers.md](../specs/13-layers.md)
**Depends on:** M3, M5
**Deliverables:** Pin Layers / Thread Layers panels, create/rename/delete/duplicate/reorder/visibility/lock, per [18-design-system.md](../specs/18-design-system.md) Layer Row component.
**Exit criteria:** Full Test Cases in 13 pass, including hidden-retains-contents and locked-blocks-edits guarantees enforced against M3/M5 editing paths.

### M7 — Undo/Redo Wiring
**Specs:** [10-undo-redo.md](../specs/10-undo-redo.md)
**Depends on:** M2, M3, M4, M5, M6
**Deliverables:** Keyboard shortcuts (Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z), undo/redo toolbar buttons wired to M0's `HistoryStack`, confirmation that every operation listed in 10-undo-redo.md's Undoable Operations table is actually routed through a Command (audit, not new mechanism — the mechanism shipped in M0).
**Exit criteria:** Full Test Cases in 10 pass, run as an end-to-end pass over every milestone delivered so far (this is the integration checkpoint for M1–M6).

### M8 — Persistence
**Specs:** [16-persistence.md](../specs/16-persistence.md), [02-document-model.md](../specs/02-document-model.md) (serialization round-trip)
**Depends on:** M7
**Deliverables:** New/Save/Open/Duplicate, versioned project file schema, migration hook (even if only version 1 exists initially, the seam must exist).
**Exit criteria:** Full Test Cases in 16 pass; a full Project (board + multiple pin/thread layers + symmetry + undo history irrelevant to save) round-trips losslessly through save/load.

### M9 — Printing & Export (MVP slice)
**Specs:** [14-printing.md](../specs/14-printing.md) (1:1 / fit / custom scale, paper config, print element checklist — tiling and calibration are Phase 2, see below), [15-export.md](../specs/15-export.md) (native project export only for MVP), [17-statistics.md](../specs/17-statistics.md)
**Depends on:** M8
**Deliverables:** Print preview with independently selectable elements, scale modes, paper size/orientation; pin/thread statistics panels.
**Exit criteria:** MVP-scoped Test Cases in 14 (excluding tiling/calibration), 15 (native export only), and 17 pass.

### M10 — Phase 2 (post-MVP)
**Specs:** Remainder of [14-printing.md](../specs/14-printing.md) (tiling, calibration), remainder of [15-export.md](../specs/15-export.md) (SVG, PDF, PNG, JPEG), autosave in [16-persistence.md](../specs/16-persistence.md), configurable spiral twist density in [12-thread-editor.md](../specs/12-thread-editor.md), advanced board textures in [04-board-appearance.md](../specs/04-board-appearance.md).
**Depends on:** M9
**Deliverables/Exit criteria:** tracked as separate sub-tickets when this milestone starts — do not scope in detail until MVP (M0–M9) is Done, per [00-overview-and-scope.md](../specs/00-overview-and-scope.md) MVP/Phase 2 split.

### M11 — Play Mode
**Specs:** [19-play-mode.md](../specs/19-play-mode.md)
**Depends on:** M9 (reuses the SVG-rasterization/canvas-export precedent established by M9/M10's PNG/JPEG export)
**Deliverables:** A fifth Editor mode replaying Thread Path draw order segment-by-segment, with full transport controls (play/pause, stop, first/previous/next/last, editable frame number, read-only total, per-frame interval) and a video-export button (`captureStream`/`MediaRecorder`, `.webm`).
**Exit criteria:** All Test Cases in 19 pass as automated tests (video-export browser mechanics excepted, per the same jsdom-rasterization limitation already accepted for PDF/raster export in M10).

### M12 — In-App Help
**Specs:** [20-help.md](../specs/20-help.md)
**Depends on:** M2–M11 (documents every tool/mode/panel built through Play mode, plus Layers from M6)
**Deliverables:** A "Help" toolbar button (between Stats and Print) opening a full-bleed overlay with seven tabs (Edit, Pin, Thread, Pan, Play, Layers, Keyboard & Mouse) covering every tool/button documented in specs 05–13, 19, plus a consolidated keyboard-shortcut/mouse-gesture reference. Data-driven content module (`helpContent.ts`) rather than bespoke per-tab components. Default tab matches the current Editor mode on open.
**Exit criteria:** All Test Cases in 20-help.md pass as automated tests; content only documents features actually implemented (spec-vs-code gaps already tracked elsewhere, e.g. wheel-zoom in 05-canvas-and-viewport.md, are *not* described as available).

### M13 — Scale Tool & Pin Distance
**Specs:** [21-scale-and-pin-distance.md](../specs/21-scale-and-pin-distance.md)
**Depends on:** M6, M7
**Deliverables:** Fifth Edit-mode tool (Scale — press-drag-release, resizes the selected Pin Path about its own centroid), Pin distance numeric input in the Selection panel, both recomputing pins through the existing 07-pin-geometry-engine.md distribution rules, plus Nearest-Pin Reattachment so existing Thread Path segments follow the recomputed pins instead of going dangling — one undo step per Scale drag or Pin distance edit, covering both the pin layer and thread layer change together.
**Exit criteria:** Full Test Cases in 21 pass; locked-layer blocking and Esc-cancel verified; live-verified via Playwright (scale a shape with a connected thread, confirm the thread stays attached to the nearest new pin; same for Pin distance; confirm single-step Undo).

### M14 — Thread Draft Pattern-Follow (Right Arrow)
**Specs:** [22-thread-follow-pattern.md](../specs/22-thread-follow-pattern.md)
**Depends on:** M5
**Deliverables:** An `ArrowRight` shortcut in Thread Draw mode that, once the in-progress draft has 4+ vertices, auto-appends the next vertex by extrapolating the numeric pin-position pattern of the vertices added so far (interleaved-by-position-parity arithmetic sequences, wrapping around whichever Pin Path the pattern currently occupies). Pure logic in `src/application/document/threadPattern.ts`, wired via a new non-undoable `EditorStore.advanceThreadDraftByPattern()` method (same transient-draft treatment as `extendThreadDraft`/`retractThreadDraft`), keyboard-wired in `useThreadDrawing.ts` alongside the existing `Escape`/`ArrowLeft` handling. In-app Help content and README.md updated to document the new shortcut.
**Exit criteria:** Full Test Cases in 22-thread-follow-pattern.md pass as automated tests (unit tests for the extrapolation logic, a store-level test for the 16-pin wraparound example, a Canvas interaction test firing a real `ArrowRight` keydown); live-verified via Playwright per this document's UI-change convention.

### M15 — Keyboard Move/Rotate/Scale
**Specs:** [23-keyboard-transform.md](../specs/23-keyboard-transform.md)
**Depends on:** M7, M13
**Deliverables:** Arrow-key nudge of the selected Pin Path, routed through whichever of Move/Rotation/Scale is the current Edit tool — 1 unit per press, 10 with Shift, reusing the existing `commitPinPathTransform`/`commitPinPathScale` store methods (locked-layer blocking, undo, and thread reattachment all inherited unchanged).
**Exit criteria:** Full Test Cases in 23-keyboard-transform.md pass as automated tests; live-verified via Playwright.

### M16 — Internationalization
**Specs:** [24-internationalization.md](../specs/24-internationalization.md), [18-design-system.md](../specs/18-design-system.md) (new dropdown component pattern)
**Depends on:** M2–M15 (translates every user-facing string introduced across the whole UI, including M12's Help content)
**Deliverables:** `react-i18next`/`i18next` wired via a module-singleton init (no backend, no Suspense, bundled JSON resources) imported once at app bootstrap and once in the shared Vitest setup so all pre-existing tests keep passing unchanged; English + Portuguese (pt-BR) translations for every string across `src/ui/` (10 namespaces) plus the full Help panel content; a shared, custom-built `LanguageSwitcher` component (Globe trigger + accessible listbox menu, own click-outside/keyboard handling) mounted identically in Board Setup and the Editor Shell top bar, both reading the same global `i18n.language`; first-visit auto-detection from `navigator.language` with an explicit-choice override persisted to `localStorage`; the two user-visible error alert paths (Open Project, Export/PDF) translated via UI-layer discriminant mapping over named error classes, without importing i18next into `src/domain/` or `src/application/`.
**Exit criteria:** Full Test Cases in 24-internationalization.md pass; en/pt-BR key-parity test green across every namespace; all pre-existing tests pass unmodified; live-verified via Playwright (switch language in both mount points, confirm text updates immediately, confirm persistence across reload, confirm auto-detect, confirm a translated error alert, confirm Help panel prose is fully translated, no new console errors).

### M17 — Radial Context Menu
**Specs:** [25-radial-context-menu.md](../specs/25-radial-context-menu.md), [18-design-system.md](../specs/18-design-system.md) (new Radial context menu component + `--shadow-float` token)
**Depends on:** M3, M4, M5, M6, M11, M13 (needs Edit-mode `selectTool` state and Merge from M3/M13, Thread draft mechanics from M5/M14, Play transport from M11)
**Deliverables:** Right-click on the canvas opens a cursor-anchored radial menu (via `@spaceymonk/react-radial-menu`) with mode-aware, icon-only slices with tooltips: Select/Move/Rotate/Scale/Merge (+conditional Commit Merge) in Edit mode; Line/Arc/Ellipse/Circle/Rect/Square/Freehand/Eraser/Path Eraser in Pin mode; Draw/Eraser/Segment (or Cut/Back/Next while a thread draft is active) in Thread mode; Fit/Zoom In/Zoom Out in Pan mode; First/Previous/Play-Pause/Next/Last in Play mode. Replaces the two previous instant right-click actions (Thread draft finish, Merge commit) with menu picks. Every slice calls the same `EditorStore` method its toolbar equivalent already calls — no new business logic.
**Exit criteria:** Full Test Cases in 25-radial-context-menu.md pass as automated tests (slice-set-per-mode unit tests + a component test); live-verified via Playwright per `docs/conventions/workflow.md`.

### M18 — Edit-Mode Multi-Select & Granularity
**Specs:** [26-edit-mode-multi-select.md](../specs/26-edit-mode-multi-select.md)
**Depends on:** M6, M13, M15, M17
**Deliverables:** A Pin Path/Pins granularity switch in the Edit Tool area; click/Alt-Cmd-click/rubber-band (with Alt-Cmd union variant) multi-select for both granularities; Move/Rotation/Scale generalized to operate over N selected paths or N selected pins with the combined/mean-centroid rule; Merge unified into an instant action fired against the current selection (path-mode combine-into-one-path-with-full-renumber algorithm, pins-mode destination-path rule with centroid/id tiebreaks), replacing its old independent accumulate-click gesture; radial context menu's Merge slice retargeted.
**Exit criteria:** Full Test Cases in 26 pass; locked-layer all-or-nothing abort verified for every tool; pre-existing single-selection M3/M13/M15 tests still pass; live-verified via Playwright.

### M19 — Thread Select Tool
**Specs:** [27-thread-select-tool.md](../specs/27-thread-select-tool.md)
**Depends on:** M5, M12
**Deliverables:** A 4th Thread-mode tool, Select, for single Thread Path selection; a new Thread properties panel editing colours/width on the selected Thread Path (distinct from the existing drawing-defaults panel); locked-layer blocking.
**Exit criteria:** Full Test Cases in 27 pass; live-verified via Playwright.

### M20 — Grid Settings Dialog
**Specs:** [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (Grid section + new Grid Settings UI subsection), [18-design-system.md](../specs/18-design-system.md) (new Anchored settings popover component vocabulary entry), [docs/conventions/ui-patterns.md](../conventions/ui-patterns.md) (new Anchored settings popover pattern)
**Depends on:** M2
**Deliverables:** A "Grid settings" button in the Canvas Toolbar, next to Snap, opening a small popover anchored to the button holding Gap X, Gap Y, Grid colour, and Grid opacity (moved out of the toolbar's main row, which previously showed them inline). Grid visibility and Snap remain their own toggle chips, unchanged.
**Exit criteria:** Full Test Cases in 05's new "Grid settings popover" feature pass; live-verified via Playwright.

### M21 — Help: About Tab
**Specs:** [28-help-about-tab.md](../specs/28-help-about-tab.md)
**Depends on:** M12 (In-App Help)
**Deliverables:** An 8th "About" tab appended to the Help overlay's `HELP_TABS` (`src/ui/panels/help/helpContent.ts`), via a new optional `HelpTab.Content` custom-render field (the other 7 tabs keep using `sections`, now optional). New `AboutTabContent.tsx` rendering read-only app-info property rows (name/description/live `package.json` version/author) plus a contact form (Name, Subject restricted to Question/Support/Issue/Other, Message) whose Send control builds and opens a `mailto:lfsvendrame@gmail.com` link, disabled until all three fields are filled.
**Exit criteria:** Full Test Cases in 28 pass; `keyParity.test.ts` green across all 5 locales; live-verified via Playwright.

### M22 — Text Pin Path

**Specs:** [29-text-pin-path.md](../specs/29-text-pin-path.md), multi-contour distribution addendum in [07-pin-geometry-engine.md](../specs/07-pin-geometry-engine.md)
**Depends on:** M3 (Pin Editor), M13 (Scale tool + the geometry-transform machinery Text's generic Move/Rotate/Scale cases plug into)
**Deliverables:** A Text Pin tool (click-once-then-type, next to Freehand) that places an empty Text Pin Path and hands off to Edit mode with it selected; a Selection-panel field group (Font/Weight/Italic/Size/Letter spacing/Text) that regenerates the pin path live on every change; a new multi-contour `PinPathGeometry` variant (`contours: Point[][]`) so a letter's hole/disconnected pieces are representable inside one Pin Path; 20 bundled Google Fonts (`public/fonts/`, SIL OFL 1.1) spanning serif/sans-serif/cursive/monospace via `opentype.js`.
**Exit criteria:** Full Test Cases in 29-text-pin-path.md and the new multi-contour scenarios in 07-pin-geometry-engine.md pass as automated tests; live-verified via Playwright (place text, type a hole-letter word, confirm pins on both the outer and inner contour, Move/Scale/Merge it, Undo, reload a saved project and confirm it renders without re-fetching a font).

### M23 — SEO: Technical Tags & Landing Content

**Specs:** [30-seo.md](../specs/30-seo.md), [18-design-system.md](../specs/18-design-system.md) (landing content must reuse existing tokens/components only), [24-internationalization.md](../specs/24-internationalization.md) (new `landing` namespace)
**Depends on:** M2, M16
**Deliverables:** `index.html` head rewrite (meta description, canonical, Open Graph, Twitter card, theme-color, `SoftwareApplication` JSON-LD, favicon/manifest links); `public/robots.txt`, `public/sitemap.xml`, `public/manifest.webmanifest`, `public/favicon.svg`, `public/apple-touch-icon.png`, `public/og-image.png`; `document.documentElement.lang` synced to the active i18next locale in `src/i18n/index.ts`; four new inline landing sections (`src/ui/panels/landing/`: `LandingHero`, `LandingFeatures`, `LandingHowItWorks`, `LandingFAQ`) wired into `App.tsx`'s `!entered` branch around the existing `BoardSetup` (its own heading demoted `<h1>`→`<h2>`); new `landing` i18n namespace with real copy authored for all 5 locales.
**Exit criteria:** Full Test Cases in 30-seo.md pass (component tests for each landing section, `BoardSetup`'s heading-level test, `keyParity.test.ts` green for the new namespace — 686 tests total); live-verified via Playwright: hero `<h1>` + BoardSetup's demoted `<h2>` + Features/How-it-works/FAQ all render correctly, full BoardSetup flow (shape/dimension/appearance pick → Continue to Editor → EditorShell) confirmed unchanged, language switch updates both the translated `BoardSetup` strings and `document.documentElement.lang` (confirmed `en`→`es`→`en`), no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning and an unrelated favicon.ico 404); built `dist/index.html` inspected directly and confirmed to carry every new meta/OG/Twitter/JSON-LD tag verbatim, since crawlers don't execute JS.

### M24 — WebMCP Agent Tools
**Specs:** [31-webmcp-agent-tools.md](../specs/31-webmcp-agent-tools.md)
**Depends on:** M8, M9, M13 (needs Persistence-era `EditorStore` methods for board/pins/threads/layers stable, plus Scale-era statistics helpers)
**Deliverables:** `src/infrastructure/webmcp/tools.ts` registering board-setup, pin-layer/pin-path, thread-layer/thread-path, undo/redo, and read-only-stats tools via `navigator.modelContext.registerTool` (feature-detected, no-op when unsupported), wired from `App.tsx`; new `projectThreadTotals` in `src/application/document/statistics.ts`.
**Exit criteria:** Full Test Cases in 31-webmcp-agent-tools.md pass as automated tests (registration + every tool's execute handler, against a mocked `navigator.modelContext` — the real Chrome dispatch path isn't testable/live-verifiable anywhere yet, per the spec's Browser support section); live-verified via Playwright that the app boots with zero new console errors and all existing functionality is unaffected when `navigator.modelContext` is absent (today's reality in every shipping browser).

### M25 — Generator Mode
**Specs:** [32-generator-mode.md](../specs/32-generator-mode.md), [18-design-system.md](../specs/18-design-system.md) (ModeSwitcher option-count correction)
**Depends on:** M6 (Layers — Confirm creates layers), M7 (Undo/Redo — Confirm is one Command)
**Deliverables:** A sixth Editor mode/tab, Generate/Re-generate/Confirm workflow (`EditorStore.generatePattern`/`confirmGeneratedPattern`, transient `generatorDraft`), a pattern engine (`src/domain/generator/`, `src/application/document/generator/`: registry + 5 patterns — Mandala/Star/Freestyle/Star of David/Spirals), `GeneratorToolbar`/`GeneratorPanel`/`GeneratorPreviewOverlay`.
**Exit criteria:** Full Test Cases in 32-generator-mode.md pass; live-verified via Playwright.

## Status Table

| # | Milestone | Depends on | Status | Notes |
|---|---|---|---|---|
| M0 | Project Scaffolding & Command Skeleton | — | Done | Vite+React19+TS+Vitest scaffold, Clean Architecture folders, `Command`/`HistoryStack` (unit-tested), ESLint boundary rules (`import/no-restricted-paths` for ui/**, `no-restricted-imports` for react/react-dom — proven via `boundaries.test.ts`). Lint/test/build/dev all green. |
| M1 | Geometry Engine | M0 | Done | Path/Segment model (Line/CircularArc/EllipticalArc), all MVP shapes (line/arc/ellipse/circle/rect/square/regular-polygon/star/polygram), open+closed pin distribution, viewport transform, snap pipeline (pin>grid priority, screen-space radius), mirror+radial symmetry math. 57 tests, all worked examples from spec verified. |
| M2 | Board & Viewport | M1 | Done | Board setup screen (shape/dims/appearance, undoable), editor shell (top bar, mode switcher, canvas w/ grid+zoom+fit, status bar). SVG rendering adapter (path->d, board fill). 85 tests total. Browser visual check blocked by extension site permissions (localhost not granted) — verified via component tests + build instead. |
| M3 | Pin Editor | M2 | Done | Pin Path model + single default layer (M6 will add the panel), all 9 drawing tools wired to real mouse interaction (line 2-click, arc 3-click w/ live curvature, bounding-box drag for ellipse/circle/rect/square w/ Alt constraint, centre-drag for polygon/star/polygram family), Pin Eraser, Select-mode click-to-select + numeric geometry editing (recalculates pins), live pin-count/spacing status bar. All mutations undoable. 113 tests. Selection geometry editing uses numeric inputs, not canvas drag-handles (scope note). |
| M4 | Symmetry | M3 | Done | Symmetry config on PinPath (none/horizontal/vertical/both/radial), mirrored/radial copies derived on every render (never stored, so source edits always stay in sync), dual-context (defaults vs selected-path, selected-path change is undoable), movable radial centre + interval via numeric fields, canvas overlay (axes/spokes+centre). 120 tests. Radial centre is numeric-input only, not canvas drag (same scope note as M3 selection editing). |
| M5 | Thread Editor | M3 | Done | ThreadPath/ThreadLayer model, full drawing workflow (click extends, double-click finishes+segment, right-click finishes without segment, Esc cancel-or-finish per confirmed-segment count), pin highlight states (normal/candidate/active-origin), 1/2/3-colour spiral rendering (same geometry, dash-offset strands), cascading pin-delete into threads as one undo step (interior removal correctly splits into surviving fragments, endpoint removal keeps one). 137 tests. Thread Eraser removes a whole Thread Path per click (segment-level splitting deferred — scope note); nearest-pin lookup is still an O(n) scan (spatial index deferred per 07-pin-geometry-engine.md §Performance, tracked as a follow-up not silently dropped). **Post-ship fix (2026-09-16, found by the user):** Thread insertion's nearest-pin detection (candidate highlight, click-extend, double-click-finish) could target pins on a hidden Pin Layer, and picked whichever pin was geometrically closest regardless of which Pin Layer was active. Fixed via new `nearestThreadInsertionPin` (`src/ui/canvas/hitTesting.ts`), wired into all three call sites in `useThreadDrawing.ts`: filters to visible Pin Layers only, then checks the active Pin Layer as a priority stage (wins outright if it has any pin in range, even a farther one) before falling back to the nearest pin among the other visible layers — mirrors the pin-over-grid snapping priority shape from 05-canvas-and-viewport.md. Scoped to Thread insertion only; `nearestPinOrMirrorOwner` (Edit-mode Select) and `nearestPinOwner` (Pin/Path Erasers) were left unchanged, not requested. Specs 12 and 13 updated (new "Pin Layer Scope for Thread Insertion" subsection + 3 Gherkin scenarios in spec 12). 5 new unit tests in `hitTesting.test.ts` (657 tests total); live-verified via Playwright + the debug hook: 3 pins at increasing distance from the cursor on 3 layers (active/other-visible/hidden) confirmed the active layer's farther pin wins the hover-candidate and a real click; moving the active layer's pin out of range confirmed fallback to the other visible layer, with the hidden layer's closer pin still excluded — no new console-error classes. |
| M6 | Layers | M3, M5 | Done | Pin/Thread Layers panels (tabs, create/rename/duplicate/delete/reorder/visibility/lock), generic layerOps shared by both kinds, active-layer targeting for new pins/threads, hidden-retains-contents + locked-blocks-edits-but-not-visibility guarantees, layer duplication regenerates pin IDs so existing threads keep pointing at the originals, deleting a layer cascades into thread segments in one undo step. 151 tests. "Select all objects on a layer" (multi-select) not implemented — scope note, would require extending Selection beyond single-object. |
| M7 | Undo/Redo Wiring | M2–M6 | Done | Ctrl/Cmd+Z / Ctrl/Cmd+Shift+Z global shortcut (skips text-entry targets so native input undo isn't hijacked), toolbar buttons reflect live history state. Audit of Undoable Operations list against M2–M6: all covered by Commands except "Add segment" during an in-progress thread draft, which is treated as part of the single "Add thread" commit-on-finish command rather than a per-click undo step (judgment call — undoing mid-draw individual clicks isn't a normal editing action; documented here rather than silently assumed). Full suite: 155 tests, this is the integration checkpoint for M1–M6. |
| M8 | Persistence | M7 | Done | Versioned project file schema (v1) with a migration seam (no-op today, ready for v2), New/Save/Open wired via browser download + file picker (File System Access API / autosave adapter deferred to Phase 2), loading resets undo history, full round-trip verified through real `JSON.stringify`/`parse` including pin IDs and thread pin references staying intact. 163 tests. |
| M9 | Printing & Export (MVP) | M8 | Done | Print preview (independently selectable elements, 1:1/fit/custom scale, paper size+orientation, print-vs-editor visibility independence verified), pin+thread statistics panels (project totals, per-path cards). Native export already satisfied by M8's Save. 179 tests. Print preview is on-screen only (no @media print stylesheet / physical-accuracy calibration — that's explicitly Phase 2). |
| M10 | Phase 2 | M9 | Done | All six Phase 2 items: (1) board appearance UI + custom-texture upload + 8 wood/4 paint presets (this also filled a real M2 gap — no appearance UI existed before); (2) thread twist pitch, configurable per-thread and as a drawing default; (3) autosave (debounced, localStorage, restore/discard banner); (4) print calibration (reference-line flow, correction factor applied to 1:1 scale); (5) print tiling (overlap, trim/alignment marks, page numbers/coordinates, multi-page preview); (6) SVG/PDF/PNG/JPEG export (SVG and PDF are true vector, PDF lazy-loaded via dynamic import since svg2pdf.js is ~470KB and browser-only). 223 tests total. Found and fixed a real pre-existing bug while building tiling: bounding-box code sampled only each path segment's start point, collapsing circles/arcs/ellipses to a single point — silently wrong since M2 (affected Fit-to-viewport too, just never exercised by a test until tiling's math caught it). Also discovered jsdom 25's Blob/File lack `.text()`/`.arrayBuffer()` and `URL.createObjectURL` doesn't exist at all — added a Node-Blob/File swap + a create/revokeObjectURL stub to test setup, which retroactively let M8's FileMenu Save/Open get real tests it never had. PDF export itself (`pdfExport.ts`) cannot be unit-tested here — svg2pdf.js's README states it does not work under jsdom (crashes at module-load); confirmed firsthand, not assumed. Raster export's actual canvas rasterization is similarly untestable in jsdom (no real 2D context backend) — only its pure DPI/mime-type math is covered. |
| M11 | Play Mode | M9 | Done | Fifth Editor mode (`"play"`) replaying Thread Path draw order one segment at a time; frame model reconstructed from current array order (no timestamp field exists — documented, accepted approximation, see 19-play-mode.md). Transport is local `EditorShell` state (`usePlaybackTransport`), not routed through `EditorStore`/undo — matches the existing `overlay` (Print/Stats) precedent, since Play mode never mutates the document. Reuses `BoardLayer`/`GridLayer`/`PinLayersView`/`ThreadLayersView` unchanged in a new dedicated `PlaybackCanvas` (deliberately not the interactive `Canvas`, which is built entirely around per-mode pointer dispatch Play mode has no use for). Video export (`captureStream`+`MediaRecorder`, `.webm` only — no cross-browser MP4 encoder exists) rasterizes each frame by serializing the LIVE `<svg>` DOM node, so the export matches the on-screen multi-colour/twist rendering exactly, rather than reusing the static exporter's simpler single-colour thread rendering. 349 tests total. Video-recording mechanics (`MediaRecorder`/`captureStream`/real canvas rasterization) cannot be unit-tested in jsdom — same class of limitation already accepted for PDF/raster export in M10; only the pure MIME-type-selection and bounding-box/pixel-sizing math are unit-tested, the real recording path verified live via Playwright instead. |
| M12 | In-App Help | M2–M11 | Done | Help button (between Stats and Print) opens a full-bleed overlay matching the Stats/Print convention exactly (no backdrop, no Escape-to-close), with 7 tabs (Edit, Pin, Thread, Pan, Play, Layers, Keyboard & Mouse) mirroring the Mode Switcher order plus 2 cross-cutting topics. Content is one data-driven `helpContent.ts` module rendered through a single generic section/item view, not 7 bespoke components. Default tab matches the current Editor mode on open, re-defaulting on every reopen since the panel only mounts while `overlay==="help"`. Keyboard/Mouse tab documents only what's actually implemented in code (confirmed via full pointer/keydown-handler audit) — omits spec-only, unimplemented gestures (wheel-zoom, shift-click multi-select, arrow-key nudging, Delete/Backspace) rather than describing them as available. 359 tests total (+9 over M11). Live-verified via Playwright: default-tab-per-mode, tab switching, Close, and re-default after a mode switch all confirmed. |
| M13 | Scale Tool & Pin Distance | M6, M7 | Done | Fifth Edit tool (Scale, grouped with Move/Rotation/Merge) — press-drag-release, horizontal-pixel-delta-driven factor, always pivots on the shape's own centroid (centre stays invariant, only size fields scale; Arc's curvature scales too since it's a physical sagitta length). Pin distance numeric field added to the Selection panel. Both feed into the existing per-shape `distributePins` pipeline unchanged, then reattach every Thread Path pin reference to its nearest new pin (`buildNearestPinRemap` over `allPinsWithMirrors`, generalizing Merge's remap-and-collapse via new `remapPinsInThreadPathByMap`) as one bundled `{pinLayers, threadLayers}` undo step (`commitPinPathWithReattach`, shared by `commitPinPathScale` and `setPinProperty`'s spacing branch). 384 tests total (+25 over M12). Live-verified via Playwright: radius scale up/down with centroid fixed, pin-count recompute, thread visibly reattached to the new nearest pin, single-step Undo, Esc-cancel leaves state untouched, locked-layer blocks the commit (re-verified on a clean reload after an initial debug-instrumented run was confounded by Vite/Chromium HTTP caching serving a stale module — not a real defect, noted here so a future session doesn't rediscover the same red herring). Scope note: this reattachment fix is intentionally limited to Scale and Pin distance, per explicit request — other numeric geometry-field edits (typed-in Position/Radius/etc.) still mint fresh pin ids with no thread reattachment, a pre-existing gap left as-is. |

| M14 | Thread Draft Pattern-Follow (Right Arrow) | M5 | Done | `ArrowRight` in Thread Draw mode: once the draft has 4+ vertices, extrapolates the next one from the two interleaved-by-position-parity arithmetic sequences the vertices form (`threadPattern.ts`'s `computeNextPatternPinId`), wrapping around whichever Pin Path the pattern's most recent member in that group belongs to. Wired as a non-undoable `EditorStore.advanceThreadDraftByPattern()` (same transient-draft treatment as `extendThreadDraft`/`retractThreadDraft`), keyed in `useThreadDrawing.ts` alongside `Escape`/`ArrowLeft`. 403 tests total (+19 over M13). Live-verified via Playwright: seeded pins 3/6/9/12 on a real 16-pin circle Pin Path, sent real `ArrowRight` keydowns through the mounted Canvas, confirmed the exact 15/2/5/8 wraparound sequence from the spec's worked example, confirmed `ArrowLeft` still composes correctly on top of pattern-added vertices, and confirmed the new Help panel copy renders. Scope note: only plain (non-mirrored) pins participate — a symmetry-mirrored pin id has no stable Pin-N position, so it makes `ArrowRight` a no-op rather than guessing, documented in 22-thread-follow-pattern.md's Scope limits. |

| M15 | Keyboard Move/Rotate/Scale | M7, M13 | Done | Arrow-key nudge (`useKeyboardTransform.ts`) driven by whichever of Move/Rotation/Scale is the active Edit tool — 1px/1°/1% per press, 10x with Shift, ignoring text-entry targets. Reuses `commitPinPathTransform`/`commitPinPathScale` unchanged (locked-layer blocking, undo, thread reattachment inherited for free); `geometryCenter` exported from `pinPath.ts` as the keyboard Rotate/Scale pivot (unlike the mouse Rotation tool's external press-point pivot); `isTextEntryTarget` extracted to `src/ui/keyboard.ts`, shared with the Undo/Redo shortcut. Big-step modifier was originally Alt/Cmd, switched to Shift after live use showed Alt/Cmd colliding with OS/browser shortcuts (e.g. macOS Option+Arrow word-navigation). Hold-to-repeat: native `event.repeat` auto-fire is ignored in favour of a homegrown `setTimeout`/`setInterval` loop at 500ms delay / 100ms interval — the WPF/UWP `RepeatButton` de facto standard (researched, not guessed), chosen because native OS key-repeat delay/rate is user-configurable and inconsistent across machines. 19 new tests (423 total, incl. fake-timer coverage of the delay/interval/keyup/mid-hold-lock/key-switch cases). Help content and README updated. Live-verified via Playwright + the debug hook: Move/Shift+Move nudge distances matched `screenDistanceToDocument` at the real fitted-viewport zoom (8.57, not the pre-fit default of 4) exactly; Rotate confirmed anticlockwise/clockwise signs and centroid invariance on a Rectangle; Scale confirmed 1%/10% factors, centroid invariance, pin-count recompute, and thread reattachment to the nearest new pin; single-step Undo confirmed for both Move and Scale; locked-layer blocking confirmed; confirmed a focused numeric input's own native arrow-stepping fires instead of the keyboard transform (width went 8→8.1 via the input's own step, not 8.08). Hold-to-repeat also live-verified with real timing (`page.keyboard.down`/`up` + real waits, not fake timers): holding ArrowRight 650ms produced exactly 2 commits (immediate + one repeat tick at ~600ms, `10 + 2×0.1167 = 10.233`), release stopped it immediately, and 2 separate Undo steps restored the original position. |
| M16 | Internationalization | M2–M15 | Done | `react-i18next`/`i18next` wired via a Provider-less module-singleton (bundled JSON resources, `initAsync: false`, no Suspense) imported once in `main.tsx` and once in `test/setup.ts` — verified all 441 pre-existing tests kept passing unmodified throughout every extraction batch, since `useTranslation()` falls back to the singleton when no `<I18nextProvider>` wraps the tree. English + full Portuguese (pt-BR) translations across 10 namespaces (`common`, `boardSetup`, `editorShell`, `menus`, `toolbars`, `panels`, `printPreview`, `canvas`, `help`, `errors`) covering every `src/ui/` component plus `src/App.tsx`'s autosave-restore banner (outside `src/ui/`, caught only by a live-verification pass, not the static string inventory) and the full ~240-line Help panel (`helpContent.ts` restructured to hold translation keys, not literal text). Custom-built `LanguageSwitcher` (Globe trigger + accessible listbox, own click-outside/Escape/arrow-key handling — a new dropdown pattern, `18-design-system.md` had none) mounted identically in Board Setup and the Editor Shell top bar, both reading the shared global `i18n.language`. First-visit auto-detect from `navigator.language` (`pt*` → pt-BR), explicit choice persisted to `localStorage` (`stringartit:language:v1`) and overriding detection thereafter. `projectFile.ts` error throws promoted to named classes (`InvalidProjectFileError`, `NoMigrationPathError`, `IncompatibleProjectVersionError` gaining a public `foundVersion` field) so `src/ui/toolbars/openFileErrors.ts` can translate the Open-file alert via `instanceof` discrimination instead of matching English `.message` text; domain-layer invariant-guard errors and raw export-failure `err.message` text stay untranslated by design (framework boundary / unscripted diagnostic detail, respectively). 453 tests total (+30 over M15): `detectLanguage.test.ts`, `languagePreference.test.ts`, `LanguageSwitcher.test.tsx`, `keyParity.test.ts` (walks every en/pt-BR namespace pair, asserts identical key sets — all 10 pass), `LanguageSwitcher.integration.test.tsx` (real `BoardSetup`/`EditorShell` mount, switches language, asserts real translated text renders in both). Live-verified via Playwright: language switch on both mount points updates text immediately; Help panel's 7 tabs fully render in Portuguese; localStorage persistence confirmed across reload in both directions; a live Open-file error alert round-tripped through Portuguese and back to English. Live verification caught one real bug the unit tests couldn't (they only assert `alertSpy` was called, never the message content): `openFileErrors.ts`'s `t()` calls omitted `{ns: "errors"}`, so under `FileMenu.tsx`'s `useTranslation(["menus","errors"])` (default ns "menus", the first array element) they silently resolved to the raw key string instead of translated text — fixed by making every call pass `{ns: "errors"}` explicitly rather than relying on the caller's hook default namespace. **Post-ship addition (2026-09-15):** three more languages added — Spanish (`es`), French (`fr`), German (`de`) — extending every mechanism built for pt-BR rather than introducing anything new: `SupportedLanguage`/`SUPPORTED_LANGUAGES`/`LANGUAGE_META` in `languages.ts` grew to 5 entries; 30 new namespace JSON files (`src/i18n/locales/{es,fr,de}/*.json`) mirroring the `en` key structure leaf-for-leaf, wired into `resources.ts` and `index.ts`'s `supportedLngs`; `detectLanguage.ts`'s prefix-matching chain extended (`es*`→es, `fr*`→fr, `de*`→de); `LanguageSwitcher.tsx` needed no code change, since it already iterated `SUPPORTED_LANGUAGES` generically. `keyParity.test.ts` generalized from a hardcoded en/pt-BR pair to loop over every non-en language in `SUPPORTED_LANGUAGES`; `detectLanguage.test.ts` and `languagePreference.test.ts` updated since both had used `fr` as their "unsupported value" example (swapped to `it`, which stays genuinely unsupported). 575 tests total (+44 over M19, the extra key-parity/detection cases). Live-verified via Playwright + the debug hook: Language Switcher lists all 5 options at both mount points; switching to Spanish, French, and German each updated Board Setup and Editor Shell top-bar text immediately; `localStorage` persistence confirmed across reload for French; Help panel rendered fully translated prose in German; a live Open-file error alert round-tripped through Spanish and back to English; no new console errors. |

| M17 | Radial Context Menu | M3, M4, M5, M6, M11, M13 | Done | Right-click on the canvas opens a cursor-anchored radial menu (`@spaceymonk/react-radial-menu`, zero-dep, React-19-safe — chosen over `react-circular-menu` (styled-components dep), `@jonbell/react-radial-menu` (excludes React 19), and a custom-built menu (user's explicit choice)), themed to design-system tokens via the library's internal CSS custom properties + a new `--shadow-float` token (its first floating-component use). Slices are mode/sub-state-aware (`radialMenuSlices.ts`, pure + unit-tested): Select/Move/Rotate/Scale/Merge (+conditional Commit Merge at 2+ accumulated pins) in Edit mode; the 9 basic Pin tools (Polygon/Star family deliberately excluded — toolbar-only, scope note); Draw/Eraser/Segment in Thread mode, entirely swapped for Cut/Back/Next while a draft is in progress; Fit/Zoom In/Zoom Out (anchored at the click point, not viewport centre) in Pan mode; the full playback transport in Play mode. Replaces the two previous instant right-click actions (Thread draft finish, Merge commit) with menu picks — a deliberate behaviour change, confirmed with the user first. Mounted once in `EditorShell.tsx` (not `Canvas.tsx`) since it must cover both the interactive Canvas and the separate `PlaybackCanvas` (Play mode). Escape/outside-click dismissal is a deliberate departure from the Print/Stats/Help `overlay` convention's "no Escape-to-close" rule, documented as such. 473 tests total (+20 over M16), including 2 pre-existing Canvas tests adapted since the behaviour they covered moved to EditorShell. Live-verified via Playwright + the debug hook: real right-click + real slice clicks confirmed the exact slice set and correct store mutation for all 5 modes, the Commit Merge conditional slice (absent below 2, appears at 2+, merges on click), the Thread draft swap (Next no-ops below 4 vertices, Back retracts, Cut commits as one undo step), Pan zoom anchored at the click point (asymmetric panOrigin shift confirmed, not the toolbar's centre-anchor), Play mode's Next Frame slice advancing playback, and Escape/outside-click both dismissing without side effects — no new console errors (one pre-existing, already-documented ModeSwitcher border/borderColor warning from M11 fired on ordinary mode switches, unrelated to this feature). Help panel's `rightClick` entry and README updated to describe the new radial-menu behaviour instead of the old instant right-click actions. |

| M18 | Edit-Mode Multi-Select & Granularity | M6, M13, M15, M17 | Done | Pin Path/Pins granularity switch (segmented pill, same visual family as `ModeSwitcher`) placed above the Edit Tools grid in `SelectToolbar.tsx`; flipping it clears the selection. `Selection` union replaced (`pinPath` → `pinPaths`/`pins`/`threadPath` variants, `EditorState.ts`) — single-selection consumers (`getSelectedPinPath`, `PinPropertiesPanel`, `SymmetryPanel`, keyboard transform) unaffected via a thin wrapper over the new `getSelectedPinPaths()`. New `useSelectTool.ts` implements click (replace) / Alt-Cmd-click (toggle) / rubber-band (replace) / Alt-Cmd-rubber-band (union) / Esc-cancels-drag, both granularities, via new `pinPathsTouchingRect`/`pinsTouchingRect` (`hitTesting.ts`). Move/Rotation/Scale generalized to N paths or N pins (`useMoveTool`/`useRotateTool`/`useScaleTool`, `useKeyboardTransform`, new `pinGroups.ts` for pins-mode per-path grouping) — path-mode Scale's pivot is the exact single-path `geometryCenter` formula unchanged for 1 path, the mean of every selected path's pins for 2+ (`multiSelect.ts`'s `selectionCentroid`); pins-mode pivot is always the mean of the selected pins (`pinsCentroid`); pins-mode Move/Rotation/Scale is a direct raw x/y pin transform with geometry/spacing left untouched (`commitPinsTransform`), same "custom/stale" precedent as the Pin Eraser. `scaleGeometry` generalized to `scaleGeometryAboutPivot` (an external-pivot bug in the naive generalization — circle/ellipse/polygon/star/polygram centers and rectangle/square position not moving under an external pivot — caught and fixed before it shipped, single-path behavior proven byte-identical via `pivot===geometryCenter`). Merge unified into an instant action (`commitSelectionMerge`/`canCommitSelectionMerge`, `EditorStore.ts`) fired against the current selection instead of its own accumulate-click gesture — `useMergeTool.ts`/`MergeSelectionOverlay.tsx` deleted, replaced by `SelectedPinsOverlay.tsx` (pins-mode highlight) and `SelectionMarqueeOverlay.tsx` (live rubber-band rect); toolbar Merge is now a full-width action button, radial menu's `commitMerge` slice removed and `merge` itself gated by `canCommitSelectionMerge`. Path-mode Merge (`combinePinPaths`, `multiSelect.ts`) combines every selected path's pins into the first-selected path's identity, collapses pins within a floating-point-safety tolerance (1e-6, not a visual snap distance), then remints **every** surviving pin a fresh id and reattaches every original pin (colliding or not) to its nearest survivor via the same nearest-pin mechanism Scale already uses — confirmed user decision, not the minimal "only colliding pins renumber" option. Pins-mode Merge keeps the existing per-pin averaging rule but resolves its destination path via `resolveMergeDestinationPath` (most selected pins wins, then closest own-pins centroid, then lowest numeric pin-path id — confirmed via a `pinpath-10` vs `pinpath-9` test proving it's a numeric, not lexicographic, comparison). All new plural commit methods (`commitPinPathsTransform`, `commitPinPathsScale`, `commitPinsTransform`, `commitSelectionMerge`) are all-or-nothing on any locked layer and commit as one bundled `SetValueCommand`. 525 tests total (+52 over M17), incl. new `multiSelect.test.ts`, `hitTesting.test.ts`, `EditorStore.multiSelect.test.ts`, `EditorStore.merge.test.ts`, `SelectToolbar.test.tsx`, and every pre-existing single-selection test (M3/M13/M15/M17) updated to the new `Selection` shape and passing unmodified in substance. Live-verified via Playwright + the debug hook: real rubber-band drag selected both of two circles; real Alt-click toggled a path in/out of the selection; a real Move drag on 2 selected paths committed and reverted as one Undo; a real Scale drag on 2 selected paths recomputed both paths' pins about the exact predicted combined centroid, reattached a connecting thread to fresh nearest pins, and reverted both paths + the thread in one Undo; locked-layer block confirmed (zero mutation); toolbar Merge combined 2 circles (31+31→62 pins) and reverted in one Undo; granularity switch clearing the selection confirmed; a real click + real Alt-click in Pins granularity built a 2-pin selection (`SelectedPinsOverlay` rendered both), and the Merge button correctly combined them into 1 pin at the destination path resolved by the most-selected-pins rule; a real right-click + real click on the radial menu's Merge slice combined a third path into the destination. No new console-error classes (the pre-existing `ModeSwitcher` `border`/`borderColor` shorthand-conflict warning from M11/M17 also fires on the new granularity switch tabs, since they copy the exact same conditional-style pattern — documented, not fixed, consistent with the existing precedent). Scope note: multi-selection has no Delete action (SelectionPanel shows a summary only for 2+ paths or any pins) — not requested, a known gap per `26-edit-mode-multi-select.md`. **Post-ship fix (found by the user):** path-mode Merge originally left the destination's `geometry` as whichever single shape type the first-selected path was, while `pins[]` held the pooled pins from every merged shape; since Scale/Pin-distance always regenerate `pins[]` strictly from `geometry`, merging e.g. a circle with a line and then scaling silently deleted every pin from whichever shape didn't match the retained geometry type — reproduced live against the user's saved project file (a circle+line merge collapsed 97 pins to 61, losing the entire second shape). Fixed by giving the merge's destination path a Freehand geometry threading through every combined pin instead of keeping one shape's original geometry (`commitPathModeMerge`, `EditorStore.ts`); spec 26 and its Merge test both updated; new regression test (`EditorStore.merge.test.ts`) reproduces the circle+line+Scale scenario; re-verified live against the exact reported repro (loaded the user's `.tmp/string-art-project.json`, merged the circle with the line, scaled — all 97 pins now survive and scale correctly, confirmed by Undo restoring the exact pre-scale and pre-merge states). **Post-ship fix (2026-09-16, found by the user, same request as M5's row):** Edit-mode Select (click and rubber-band, both Pin Path and Pins granularity) could target/select pins and paths on a hidden Pin Layer, and had no active-layer preference at all — whichever pin/path was geometrically nearest (click) or simply touched (rubber-band) won, regardless of which Pin Layer was active. Since Move/Rotation/Scale/Merge only ever act on `state.selection`, fixing Select's targeting was sufficient to scope all four transitively — no changes needed in `useMoveTool`/`useRotateTool`/`useScaleTool`/`commitSelectionMerge` themselves. Fixed via 4 new `hitTesting.ts` functions built on a shared `visibleLayersPrioritized` helper (also used to refactor M5's `nearestThreadInsertionPin` onto the same primitive): `nearestSelectPathHit`/`nearestSelectPinHit` (click, either granularity) and `selectablePinPathsTouchingRect`/`selectablePinsTouchingRect` (rubber-band, either granularity) — each filters to visible Pin Layers, then tries the active layer alone first (a match there wins outright — nearer pin or bigger touched set on another visible layer notwithstanding) before falling back to the other visible layers. Wired into all 4 call sites in `useSelectTool.ts`, replacing the unscoped `nearestPinOrMirrorOwner`/`nearestPinOwner`/`pinPathsTouchingRect`/`pinsTouchingRect` calls there (those 4 primitives are kept as-is and still exported/tested unscoped, since `nearestPinOwner` is also shared by the unrelated Pin/Path Erasers in Pin mode — explicitly out of scope for this request). Spec 26 gained a new "Pin Layer Scope for Selection" section + 5 Gherkin scenarios. 11 new unit tests in `hitTesting.test.ts` (668 tests total). Live-verified via Playwright + the debug hook: 3 pins on 3 layers (active/other-visible/hidden) at increasing distance from a click point confirmed the active layer's farther pin/path wins a real click in Pin Path granularity; a real rubber-band drag touching both the active and another visible layer's path selected only the active layer's; moving the active layer's pin out of the rect confirmed fallback to the other visible layer while the hidden layer's path (also inside the rect) stayed excluded throughout — no new console-error classes. **Post-ship addition (2026-09-16, requested by the user):** a read-only Stats box (Pins/Actual gap/Path perimeter) added to `SelectionPanel.tsx`, shown whenever one or more Pin Paths are selected in Edit mode (absent for a Pins-granularity or empty selection). Single-path selection shows all 3 figures; a 2+ path selection sums Pins and Path perimeter across the selection but omits Actual gap (ambiguous across differently-spaced paths — confirmed with the user rather than guessed, e.g. via an average). `PinPathStatistics` (`src/application/document/statistics.ts`) gained a new `perimeterCm` field — `geometryToContourPaths(path.geometry)` summed through `pathLength` per contour, so multi-contour Text Pin Paths sum correctly — reused by both the single- and multi-path box (no duplicate computation). The pre-existing `multiPathsSelected` summary sentence dropped its "(N pins total)" parenthetical since Pins now lives in the box instead. i18n keys added to all 5 locales (`selectionPanel.stats.*`), `keyParity.test.ts` green. 4 new tests (674 total): `statistics.test.ts` (perimeter on a closed circle built to an exact known perimeter, and on a hand-built 2-contour Text fixture), `SelectionPanel.test.tsx` (single-selection values, multi-selection sum + Actual-gap omission, absent when nothing selected). Live-verified via Playwright + the debug hook: a real 7cm-radius circle selection showed Pins 44 / Actual gap 1.00 cm / Path perimeter 43.98 cm (2π·7, exact); adding and selecting a second 3cm-radius circle showed the multi box (Pins 63, Path perimeter 62.83 cm = 2π·7 + 2π·3, exact) with no Actual gap row; deselecting hid the box entirely — no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor warning and an unrelated favicon 404). |
| M19 | Thread Select Tool | M5, M12 | Done | 4th Thread tool, `select` (`ThreadToolbar.tsx`, `EditorState.ts`'s `ThreadTool` union), reusing the existing `nearestThreadPath` hit-test already used by the Thread/Segment Erasers — no new hit-testing needed. New `{type:"threadPath",layerId,pathId}` `Selection` variant; `EditorStore.getSelectedThreadPath()`/`setThreadProperty(layerId,pathId,{colours?,width?})` (locked-Thread-Layer guarded, one `SetValueCommand<ThreadLayer[]>` per edit) — deliberately separate from `setThreadDefaults` (which only ever edits what the *next* drawn thread uses, confirmed untouched by a live check). New `ThreadPropertiesPanel.tsx` (mirrors `SelectionPanel`'s empty-state pattern, reuses `ThreadToolbar`'s exact colour-swatch/width markup) mounted in `EditorShell.tsx` under `ThreadToolbar` when a Thread Path is selected — Thread mode previously had no side panel at all. `ThreadPathVisual`/`ThreadLayersView` gained a `selected` prop rendering an additive `--accent` halo stroke underneath the existing multi-colour spiral rendering (same approach `PinPathVisual` already uses for its selected guide line), not a replacement of it. Selecting a Thread Path persists across switching to another Thread tool (Draw/Eraser/Segment), matching existing cross-mode selection-persistence precedent. 525 tests total (shared with M18's count — both milestones landed in the same session): `EditorStore.threadSelect.test.ts`, new assertions in `Canvas.thread-drawing.test.tsx` and `ThreadPropertiesPanel.test.tsx`. Live-verified via Playwright + the debug hook: a real click on a drawn thread's segment selected it and rendered the `ThreadPropertiesPanel` with its live colours/width; editing the panel's Width field updated the selected Thread Path's `width` (confirmed the drawing-defaults `threadDefaults.width` stayed untouched) and reverted on Undo; locking the Thread Layer correctly blocked the same edit. **Post-ship fix (found by the user):** the original design put colour/width/twist-pitch fields in BOTH `ThreadToolbar` (drawing defaults) and `ThreadPropertiesPanel` (selected thread) — shown together whenever a thread was selected, a confusing duplicate-fields UI, and the selected-thread copy never exposed the colour-count (1/2/3) buttons at all, so an existing thread's colour count could never be changed. Fixed by making `ThreadPropertiesPanel` dual-context exactly like `PinPropertiesPanel` (always rendered, title switches between "(selected)"/"(defaults)", one set of fields for both), stripping `ThreadToolbar` down to just its tool buttons + legend, and generalizing `EditorStore.setThreadProperty` from a 3-arg `(layerId,pathId,patch)` selected-thread-only method into a 1-arg `(patch)` dual-context method (branches on `state.selection`, falls through to the existing `setThreadDefaults` when nothing's selected) — same shape as `setPinProperty`. Spec 27 updated to describe the unified panel; 3 new tests (531 total) including a regression test for the colour-count bug; live-verified via Playwright (defaults context, selected context, 1→3 colour-count change on a selected thread, twist-pitch field appearing) — no duplicate fields, no new console errors. **Post-ship addition (2026-09-16, requested by the user):** a read-only Stats box (Segments/Thread length/Pins visited, per docs/specs/17-statistics.md) added to `ThreadPropertiesPanel.tsx` right after the twist-pitch field, shown only in selected-thread context (absent in defaults context) — reuses `threadPathStatistics(thread, pinLayers)` unchanged (same fn `StatisticsPanel.tsx`'s per-thread `StatCard` calls), no new store methods or props, row styling mirrors `StatCard`'s label/value rows. i18n keys added to all 5 locales (`threadPropertiesPanel.stats.*`), `keyParity.test.ts` green. 2 new tests in `ThreadPropertiesPanel.test.tsx` (values match a computed fixture; box absent with nothing selected). Live-verified via Playwright + the debug hook: real selection of a drawn thread showed the box with correct live values; deselecting hid it; erasing an endpoint pin (which forks the thread's `pathId`, a pre-existing, out-of-scope selection-staleness behavior) correctly fell back to the defaults context; reselecting the resulting shorter thread showed updated Segments/length/pins-visited without remounting — no new console errors (only the pre-existing documented `ModeSwitcher` border/borderColor warning). |

| M20 | Grid Settings Dialog | M2 | Done | New `GridSettingsPopover.tsx` (anchored popover, `docs/conventions/ui-patterns.md` §Anchored settings popover — 3rd consumer of that shape alongside `ExportMenu`/`LanguageSwitcher`) holds Gap X/Gap Y/Grid colour/Grid opacity, opened by a "Grid settings" button next to Snap in `CanvasToolbar.tsx`; the 4 fields were removed from the toolbar's main row (Grid/Snap toggle chips stayed put). Reuses `EditorStore.setGrid()` unchanged (still transient/non-undoable, no change to that). Adopted `--shadow-float` for the popover's elevation instead of repeating `ExportMenu`/`LanguageSwitcher`'s hardcoded box-shadow — those two are left as-is (out of scope) but `18-design-system.md` now documents both the token's second consumer and a new "Anchored settings popover" component-vocabulary entry, correcting a stale claim that the Radial Context Menu was the app's only floating component. Spec 05 gained the previously-undocumented Grid colour/opacity settings plus a new "Grid Settings UI"/"Grid settings popover" section and Gherkin scenarios. 581 tests total (net +6 new): `GridSettingsPopover.test.tsx` (open/close, pre-filled values, field edits, Escape/outside-click dismissal, re-click-to-close) plus `Canvas.test.tsx`'s two inline-field tests replaced by one direct-`setGrid` dot-rendering check (popover field editing no longer needs `Canvas` mounted at all). i18n `canvasToolbar.gridSettings` key added across all 5 locales, `keyParity.test.ts` green. Live-verified via Playwright + the debug hook: real click opened the popover pre-filled with live grid state; real field edits (Gap X, Grid colour) updated the store and the rendered grid dots immediately; Escape and an outside click on the canvas both closed it; toolbar's main row confirmed to no longer show the 4 inline fields; no new console-error classes (only the pre-existing documented `ModeSwitcher`/toggle-chip `border`/`borderColor` shorthand warning and an unrelated favicon 404). |

| M21 | Help: About Tab | M12 | Done | 8th/last Help tab (`src/ui/panels/help/AboutTabContent.tsx`), wired via a new optional `HelpTab.Content` field on the existing data-driven `helpContent.ts` (the other 7 tabs keep using `sections`, now optional; `HelpPanel.tsx` renders `tab.Content` when present, else falls back to the generic `HelpSectionView` list). Read-only app-info property rows (Name/Description/live `package.json` version/Author); contact form (Name, Subject restricted to Question/Support/Issue/Other, Message) whose Send is an `<a href="mailto:lfsvendrame@gmail.com?subject=StringArtIt - <Token>&body=...">` — a genuinely new "send" pattern for this backend-less app, documented in `docs/conventions/ui-patterns.md` §Mailto contact link (kept a real `href="#"` + `aria-disabled` when inert, rather than omitting `href`, so the element keeps its accessible `link` role either way). The mailto subject token stays canonical English regardless of UI locale — only the dropdown's visible option labels translate — a deliberate scope note in spec 28. New `Textarea` entry added to `18-design-system.md`'s Component Vocabulary (first use in the app). i18n: `about.*` + `tabs.about` added to all 5 `help.json` locales with real translations, `keyParity.test.ts` green. 586 tests total (+5 over M20: `AboutTabContent.test.tsx`'s 4 new tests plus 1 new `HelpPanel.test.tsx` case, with the existing seven-tabs test widened to eight): app-info fields, exactly-4-subject-options, Send inert with empty fields, and the built mailto href/subject/body all covered. Live-verified via Playwright + the debug hook: real click through Help→About rendered app info with the live `package.json` version; a real form fill produced the exact `mailto:lfsvendrame@gmail.com?subject=StringArtIt - Support&body=Name: Ana\n\nHelp please` href; Send stayed inert (`aria-disabled`, `href="#"`) with empty fields; Close still dismissed the overlay; no new console-error classes (only the pre-existing, already-documented favicon 404). |

| M22 | Text Pin Path | M3, M13 | Done | New Text Pin tool (`PinToolbar.tsx`, next to Freehand) — click-once-then-type instead of click-drag, wired in `Canvas.tsx` as a single-click branch (same shape as `eraser`/`path-eraser`) that awaits the default font then calls the existing generic `addPinPath`, which already hands off to Edit mode with the new path selected — no new sync code needed for that flow. First genuinely multi-contour `PinPathGeometry` variant (`contours: Point[][]`), added because a letter's hole ("o", "e") or disconnected piece ("i"'s dot) needs more than one closed loop inside one Pin Path — new domain primitive `closedPolylineShape` (mirrors `freehandShape` but closed) plus `commandsToContours` (pure Bezier-to-polyline flattening, framework-free, hand-built-command unit tests, no font file needed) and `geometryToContourPaths` (generalizes `geometryToPath` to N contours; every other shape is just `[geometryToPath(g)]`, byte-identical). `distributePins` gained a `"text"` branch running the closed-path algorithm independently per contour (documented as a new subsection in `07-pin-geometry-engine.md`) and concatenating pins, same `pins[]` shape as every other Pin Path. Move/Rotate/Scale need zero Text-specific tool code — `contours`+`origin` get the exact same treatment Freehand's `points[]` already gets in `translateGeometry`/`rotateGeometry`/`scaleGeometryAboutPivot`/`geometryCenter`; Merge needs no changes at all (operates on `pins[]`, agnostic of geometry type). Scope note (matches the pre-existing Pin-Eraser-vs-geometry-edit precedent): editing any of the six text fields after a manual Rotate/Scale regenerates contours from scratch at the field's literal values, discarding the transform — confirmed live, not just asserted. Font loading (`opentype.js`, `src/infrastructure/fonts/`) is the one place async touches this feature: `ensureFontLoaded` fetches+parses+memoizes by URL, so only a Font/Weight/Italic *switch* to a not-yet-loaded combination pays a network cost — plain typing never awaits a real network round-trip, keeping `EditorStore`/`pinPath.ts` fully synchronous. 20 bundled Google Fonts (SIL OFL 1.1, `public/fonts/` — new folder, sourced live via `raw.githubusercontent.com/google/fonts`, real static per-weight files hand-verified to exist rather than guessed, since most modern Google Fonts families are now variable-only) spanning sans-serif (Lato, Poppins, PT Sans, Barlow, Hind, Titillium Web), serif (PT Serif, Crimson Text, Arvo, Cardo, Tinos), cursive (Pacifico, Sacramento, Great Vibes, Kaushan Script, Alex Brush — Regular only, no bold/italic files exist for these), and monospace (Courier Prime, Space Mono, IBM Plex Mono, Anonymous Pro); no `@types/opentype.js` dependency (targets the stale 1.x API against this app's 2.0.0) — a small local ambient `.d.ts` declares just the 4 functions/fields actually used. 633 tests total (+47 over M21): `polyline.test.ts`, `glyphOutline.test.ts` (domain), `fontCatalog.test.ts`/`fontLoader.test.ts` (mocked fetch, module-reset-per-test cache isolation)/`textContours.test.ts` (integration against the real bundled pt-sans file — this is what caught and fixed a real sign bug: opentype.js's `Glyph.getPath` already negates the font's Y-up coordinates, so an initially-added extra flip in `textContours.ts` was double-flipping and rendering upside-down; removed, confirmed correct by both a unit test and the live screenshot below) (infrastructure), new `describe` blocks in `pinPath.test.ts` for multi-contour distribution and the four generic transforms against a hand-built "hole letter" fixture (application), `PinToolbar.test.tsx` (new file), `Canvas.text-drawing.test.tsx`, `PinPathVisual.test.tsx` (multi-contour `d` attribute), and 5 new cases in `SelectionPanel.test.tsx` (UI). i18n: `pinToolbar.basicTools.text`, `selectionPanel.fields.{font,weight,italic,size,letterSpacing,text}`, `selectionPanel.shapeTypes.text`, `selectionPanel.weightOptions.{regular,bold}` across all 5 locales, `keyParity.test.ts` green. Live-verified via Playwright + the debug hook: a real click placed an empty Text Pin Path and switched to Edit mode with it selected (zero mock); real typing of "on" into the Text field produced 3 contours (o's ring+hole, n's single loop) and 26 pins with no new console-error classes; a screenshot at a computed fit-to-selection viewport visually confirmed correct, right-side-up multi-contour rendering (the "o"'s hole and both pin rings clearly visible); a real Move drag shifted `origin` and every contour point together; a programmatic Scale (per this codebase's live-verification convention for drag-math-independent checks) roughly doubled the pin count and rescaled about the shape's own centroid; Undo/Redo confirmed per-keystroke (matches the existing, pre-M22 SelectionPanel/PinPropertiesPanel precedent — no debouncing exists anywhere in this codebase to copy, so none was invented here); a `toProjectFile`/`loadProject` round-trip preserved contours/pins exactly with no re-fetch. Scope notes: Weight+Italic never combine into a single file (Italic always resolves to the plain italic file regardless of the Weight dropdown, since no Bold+Italic files were sourced); script/cursive fonts offer Regular only (no Weight/Italic options); `size`/`letterSpacing` field edits are per-keystroke undo steps like every other SelectionPanel field, not batched. **Post-ship refinement:** moved Text to the first field in the group (was last) and added autofocus — a `useEffect` in `SelectionPanel.tsx` keyed on `state.selection` (not on every geometry edit, since typing goes through `updatePinPathGeometry` and never touches `selection`) focuses the Text input whenever a Text Pin Path becomes selected, covering both the tool's placement click and re-selecting an existing Text Pin Path later; 636 tests total (+3: field-order, focus-on-select, and no-refocus-on-keystroke), live-verified via Playwright (real click → Text field shown first and focused → typed directly with no intervening click, confirmed via the accessibility tree's `[active]` marker and `document.activeElement`). |
| M23 | SEO — Technical Tags & Landing Content | M2, M16 | Done | `index.html` head rewrite (description/canonical/OG/Twitter/theme-color/favicon/manifest/JSON-LD `SoftwareApplication`); `public/robots.txt`, `sitemap.xml`, `manifest.webmanifest`, `favicon.svg`, `apple-touch-icon.png`, `og-image.png`; `document.documentElement.lang` now synced to the active i18next locale. 4 new inline landing sections (Hero/Features/How-it-works/FAQ, `src/ui/panels/landing/`) wired around `BoardSetup` in `App.tsx`'s `!entered` branch — `BoardSetup`'s own heading demoted `<h1>`→`<h2>` so the page has exactly one `<h1>`. New `landing` i18n namespace, real copy authored for all 5 locales (`en`/`pt-BR`/`es`/`fr`/`de`) — the initial ship had shipped English placeholders for the 4 non-English locales, flagged as a known gap and fixed the same day once the user called it out. 686 tests total. Live-verified via Playwright: hero/BoardSetup/features/how-it-works/FAQ all render correctly; full BoardSetup flow (shape/dimension/appearance → Continue to Editor → EditorShell) unchanged; language switch (`en`→`es`→`en`) updated both `BoardSetup`'s translated strings and `document.documentElement.lang`; no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning and an unrelated favicon.ico 404); built `dist/index.html` inspected directly and confirmed to carry every new tag verbatim. |

| M24 | WebMCP Agent Tools | M8, M9, M13 | Done | 18 tools registered via `navigator.modelContext.registerTool` (`src/infrastructure/webmcp/tools.ts`), feature-detected, wired from `App.tsx` alongside (but kept separate-and-documented from) the unstable debug hook: `get_document_stats`; board `set_board_shape`/`set_board_dimensions`/`set_board_appearance` (solid/wood/painted only — gradients/custom-texture stay UI-only); pin-layer `add_pin_layer`/`set_active_pin_layer`/`toggle_pin_layer_visible`/`toggle_pin_layer_locked`; `add_pin_path` (10 of 11 `PinPathGeometry` variants — `text` excluded, needs UI-only font infra) / `delete_pin_path`; the same 4 for thread layers; `add_thread_path` (composes `cancelThreadDraft`/`extendThreadDraft`×N/`finishThreadDraft` — no new store method) / `delete_thread_path`; `undo`/`redo`. New `projectThreadTotals` in `statistics.ts` (project-level thread aggregate, mirrors `projectTotalPins`). 696 tests total (+10: `tools.test.ts` covers every Gherkin scenario against a mocked `navigator.modelContext`, `statistics.test.ts` +2). Live-verified via Playwright: real Chrome (no `navigator.modelContext`, true of every shipping browser as of this spec) confirmed the no-op path — `'modelContext' in navigator` is `false`, zero new console errors, board setup/debug-hook interaction unaffected. Accepted gap, documented in the spec itself: the real Chrome-146-Canary dispatch path (an actual agent invoking a tool through the browser's own implementation) isn't live-verifiable anywhere yet — same class of limitation this codebase already accepts for PDF/MediaRecorder mechanics (M10/M11) — so registration and every execute handler's correctness rests entirely on the unit tests instead. |

| M25 | Generator Mode | M6, M7 | Done | 6th Editor mode ("Generate", `ModeSwitcher`/`GeneratorToolbar`/`GeneratorPanel`/`GeneratorPreviewOverlay`). Researched a competitor app's 19 built-in patterns first (`~/projects/pocs/research_string_art`, not in this repo) — all 19 are pure parametric math (pins from circle/line/polygon primitives, 17/19 single-or-composite regular shapes, 2/19 sampled along a closed-form curve), so freehand geometry is used only for the curve-sampled case, never as a "couldn't figure out the shape" fallback. Shipped a curated 5-pattern set exercising every engine capability — Mandala (single circle, modular multiplication `i·base mod n`), Star (circle+star composite), Freestyle (up to 3 independent circles, round-robin), Star of David (two equilateral triangles 60° apart — the standard hexagram construction), Spirals (polar-arm curve sampled directly into a `"freehand"` geometry, bypassing `distributePins`' arc-length resampling since the formula-computed points ARE the desired pin positions) — the other 14 researched patterns are the same registry shape and are an explicit follow-up, not built here. New pure engine: `src/domain/generator/` (`roundRobin.ts`, `mandala.ts`, `spiralArms.ts`) + `src/application/document/generator/generatorPatterns.ts` (registry + `buildGeneratorPattern` dispatch), plus a new domain primitive `spacingForPinCount` (`src/domain/paths/distribution.ts`) bridging this app's spacing-driven pin distribution to the pattern engine's count-driven params, floating-point-safety-tested to land on the exact requested count (not just close). Generate/Re-generate build a transient, non-undoable `generatorDraft` (same treatment as an in-progress Thread Path draft); Confirm is the only undoable step — always creates 2 brand-new permanent layers (never merges into an existing one, so no locked-layer check applies) in one bundled `SetValueCommand<{pinLayers,threadLayers}>`, single Undo removes both. Leaving Generator mode (or loading a project) discards an uncommitted draft. 738 tests total (+15 over M24: `distribution.test.ts`, `roundRobin.test.ts`, `mandala.test.ts`, `spiralArms.test.ts`, `generatorPatterns.test.ts`, `EditorStore.generator.test.ts`, `GeneratorPanel.test.tsx`, plus updated `ModeSwitcher.test.tsx`/`radialMenuSlices.test.ts` for the new mode). **Caught by the real typecheck, not the live-verification pass:** `npx tsc --noEmit -p .` was silently checking zero files this whole session (root `tsconfig.json` uses project references with `files: []`, which needs `tsc -b`/build mode to actually traverse them) — switching to `tsc -b --force` surfaced two real exhaustiveness gaps the new `"generate"` EditorMode had opened: `radialMenuSlices.ts`'s mode switch (no `default`, would return `undefined` and crash `RadialContextMenu`'s `.map` on a Generator-mode right-click) and `helpContent.ts`'s `MODE_TO_HELP_TAB: Record<EditorMode, HelpTabId>` (missing key). Both fixed (empty slice set for Generate; Help defaults to the Edit tab, since Generator mode has no dedicated Help content yet) and regression-tested. i18n: `generatorToolbar`/`generatorPanel`/`modes.generate` keys added to all 5 locales, `keyParity.test.ts` green. `18-design-system.md`'s stale "four editor modes, not reused past ~4 options" ModeSwitcher note corrected to six (it had already fallen behind Play mode without being updated). Live-verified via Playwright + the debug hook: real clicks generated and visually confirmed all 5 patterns (screenshotted — a genuine cardioid/spirograph Mandala, two visibly overlapping triangles for Star of David, 3 real pinwheel arms for Spirals), Re-generate fully replaced the prior preview each time, Confirm created exactly 2 new real layers (visible in the Layers panel, named e.g. "Generated — Spirals") and switched them active, single Undo removed both, switching to Edit mode mid-draft discarded it with zero orphaned layers and re-entering Generate showed no stale draft — no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning, now firing once per mode switch as expected). |

Status values: `Not Started`, `In Progress`, `Blocked` (note the blocker), `Done`.

## Definition of Done (applies to every milestone)

- [ ] All Gherkin scenarios in the milestone's listed specs exist as automated tests and pass.
- [ ] No domain/application code imports React or `src/ui/` (M0's boundary check stays green).
- [ ] All new mutations route through a `Command` (undoable), not direct state assignment.
- [ ] UI matches [18-design-system.md](../specs/18-design-system.md) tokens/components (no ad-hoc colours, fonts, radii, or one-off component patterns).
- [ ] Status Table row updated to `Done` with a one-line note (what shipped, any deferred edge case).
- [ ] Any spec correction made during implementation is committed alongside the code change, not left as a mental note.
