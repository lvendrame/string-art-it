# StringArtIt — Development Orchestrator

## Purpose

This is the single navigation document for building StringArtIt against `docs/specs/`. It sequences work into milestones, states which specs and design-system rules govern each one, states dependencies between milestones, and carries a status table to track progress across sessions. Any session picking up this project should start here, find the first milestone not marked Done, read its listed specs, implement, test, update status, and move on — rather than re-reading all 19 specs cold every time.

Specs live in [`docs/specs/`](../specs/), numbered 00–19. Design reference lives in [`docs/design/editor-mockup.dc.html`](../design/editor-mockup.dc.html). This document does not restate spec content — it only sequences and cross-references it.

## How to Use This Document

1. Find the first row in the [Status Table](#status-table) not marked `Done`.
2. Check its **Depends on** column — those milestones must be `Done` first (or explicitly descoped).
3. Read its **Specs** column in full before writing code.
4. Implement inside the layer boundaries from [01-architecture.md](../specs/01-architecture.md) — domain/geometry code stays framework-free; UI code stays in `src/ui/`.
5. For any UI milestone, also read [18-design-system.md](../specs/18-design-system.md) — tokens/components are not optional polish, they're the spec.
6. Turn every `Gherkin` scenario in the milestone's specs into an actual automated test (Vitest for domain/application logic, a component/interaction test for UI behavior). A milestone is not Done until its specs' test cases are green.
7. Update the Status Table (status + one-line note) before ending the session on that milestone.
8. If implementation reveals a spec is wrong, ambiguous, or incomplete: fix the spec file first, in the same change, then implement against the corrected spec. Specs are living documents, not frozen requirements — but they must never silently drift from what's built.

## Architectural Note: Commands Come Early, Not Last

The product spec's own [Recommended Development Sequence](../specs/00-overview-and-scope.md#recommended-development-sequence) lists the undo/redo Command architecture as "Phase G," near the end. **Do not build it last.** Every mutation in [10-undo-redo.md](../specs/10-undo-redo.md) and the cascading-delete rule in [11-erasers.md](../specs/11-erasers.md) require that document mutations already go through Command objects (per [01-architecture.md](../specs/01-architecture.md)) — retrofitting that onto a codebase full of direct state mutations is a rewrite, not an add-on. This orchestrator therefore pulls the **Command skeleton** into M0 (empty `execute/undo/redo` plumbing + a `HistoryStack`) and requires every milestone from M2 onward to route mutations through it from the start. The full undo/redo *UI and shortcut wiring* (M7) still comes later — only the underlying mechanism moves earlier.

## Milestones

### M0 — Project Scaffolding & Command Skeleton
**Specs:** [01-architecture.md](../specs/01-architecture.md)
**Depends on:** —
**Deliverables:**
- Vite + React 19 project, Node 22 LTS engines pinned (`package.json` `engines`).
- Folder structure per architecture spec: `src/domain/`, `src/application/`, `src/infrastructure/`, `src/ui/`.
- Test runner configured (Vitest recommended — fast, ESM-native, works headless for domain code) and a component-testing setup for `src/ui/`.
- `Command` interface + `HistoryStack` (execute/undo/redo, linear history, redo cleared on new command) in `src/application/commands/`, unit-tested against the generic contract (not yet wired to any real mutation).
- Lint boundary check (even a simple script/ESLint rule) that fails CI if `src/domain/` or `src/application/` imports from `src/ui/` or `react`.
**Exit criteria:** boundary-check script passes on an intentionally-violating test file (proves it actually catches violations); `HistoryStack` unit tests cover execute/undo/redo/redo-invalidation.

### M1 — Geometry Engine
**Specs:** [07-pin-geometry-engine.md](../specs/07-pin-geometry-engine.md), geometry portions of [06-symmetry.md](../specs/06-symmetry.md) (transform math only, not UI), [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (physical↔screen coordinate transform, snapping priority pipeline as pure functions)
**Depends on:** M0
**Deliverables:** `src/domain/shapes/`, `src/domain/paths/`, `src/domain/transforms/`, `src/domain/snapping/`, `src/domain/symmetry/` — path length, point-at-distance, open/closed pin distribution, mirror/radial transforms, grid/pin snapping, snap-radius logic. Pure functions/classes, zero DOM/React.
**Exit criteria:** every Test Cases scenario in 07-pin-geometry-engine.md passes as a unit test, including the exact worked examples (8cm/1cm→9 pins; 7.5cm/1cm→8 pins; 31cm perimeter/2cm→16 pins @1.9375cm). Mirror/radial transform tests from 06-symmetry.md's geometry-only scenarios pass.

### M2 — Board & Viewport
**Specs:** [02-document-model.md](../specs/02-document-model.md) (Board shape), [03-board-configuration.md](../specs/03-board-configuration.md), [04-board-appearance.md](../specs/04-board-appearance.md), [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (full — modes, grid, zoom/pan, status bar), [18-design-system.md](../specs/18-design-system.md)
**Depends on:** M1
**Deliverables:** Board setup flow (shape + dimensions + appearance), main editor shell (top bar, mode switcher, canvas viewport, status bar) rendering an empty board. Physical-unit rendering via M1's coordinate transform. No pins/threads yet.
**Exit criteria:** Test Cases in 03, 04, 05 pass; editor shell visually matches `docs/design/editor-mockup.dc.html` token-for-token (colours/type/spacing per 18-design-system.md).

### M3 — Pin Editor
**Specs:** [08-pin-tools-and-properties.md](../specs/08-pin-tools-and-properties.md), [09-selection-and-editing.md](../specs/09-selection-and-editing.md) (Pin Path portions), [11-erasers.md](../specs/11-erasers.md) (Pin Eraser only — thread cascade deferred to M5)
**Depends on:** M2
**Deliverables:** Pin mode drawing tools (Line/Arc/Ellipse/Circle/Rectangle/Square/Polygon-Star dropdown), Alt constraint modifier, live pin/spacing preview, guide lines, pin property panel, Pin Path selection + geometry editing, Pin Eraser. All mutations go through M0's Command architecture.
**Exit criteria:** Test Cases in 08, 09 (pin portions), and the Pin Eraser scenarios in 11 pass. Every pin-creating/editing action is undoable (manual check against 10-undo-redo.md's list, even though M7 wires the shortcuts).

### M4 — Symmetry
**Specs:** [06-symmetry.md](../specs/06-symmetry.md) (full — UI wiring on top of M1 math)
**Depends on:** M3
**Deliverables:** Symmetry mode controls, live mirrored-copy generation while drawing, movable radial centre, source-edit-regenerates-copies behavior.
**Exit criteria:** Full Test Cases in 06-symmetry.md pass, including "editing source regenerates copies" and "deleting source removes copies in one undo step."

### M5 — Thread Editor
**Specs:** [12-thread-editor.md](../specs/12-thread-editor.md), [11-erasers.md](../specs/11-erasers.md) (Thread Eraser + cascading pin-delete-removes-threads rule, now that both Pin and Thread objects exist)
**Depends on:** M3
**Deliverables:** Thread mode, nearest-pin detection + highlight states, thread preview, click/double-click/right-click/Esc termination, 1/2/3-colour spiral rendering, Thread Eraser, cascading delete.
**Exit criteria:** Full Test Cases in 12 pass; cascading-delete scenarios in 11 pass as single undo steps. Nearest-pin lookup implementation notes the spatial-index performance requirement from 07-pin-geometry-engine.md §Performance even if a naive O(n) scan ships first — file it as a tracked follow-up, not silently ignored.

### M6 — Layers
**Specs:** [13-layers.md](../specs/13-layers.md)
**Depends on:** M3, M5
**Deliverables:** Pin Layers / Thread Layers panels, create/rename/delete/duplicate/reorder/visibility/lock, per [18-design-system.md](../specs/18-design-system.md) Layer Row component.
**Exit criteria:** Full Test Cases in 13 pass, including hidden-retains-contents and locked-blocks-edits guarantees enforced against M3/M5 editing paths.

### M7 — Undo/Redo Wiring
**Specs:** [10-undo-redo.md](../specs/10-undo-redo.md)
**Depends on:** M2, M3, M4, M5, M6
**Deliverables:** Keyboard shortcuts (Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z), undo/redo toolbar buttons wired to M0's `HistoryStack`, confirmation that every operation listed in 10-undo-redo.md's Undoable Operations table is actually routed through a Command (audit, not new mechanism — the mechanism shipped in M0).
**Exit criteria:** Full Test Cases in 10 pass, run as an end-to-end pass over every milestone delivered so far (this is the integration checkpoint for M1–M6).

### M8 — Persistence
**Specs:** [16-persistence.md](../specs/16-persistence.md), [02-document-model.md](../specs/02-document-model.md) (serialization round-trip)
**Depends on:** M7
**Deliverables:** New/Save/Open/Duplicate, versioned project file schema, migration hook (even if only version 1 exists initially, the seam must exist).
**Exit criteria:** Full Test Cases in 16 pass; a full Project (board + multiple pin/thread layers + symmetry + undo history irrelevant to save) round-trips losslessly through save/load.

### M9 — Printing & Export (MVP slice)
**Specs:** [14-printing.md](../specs/14-printing.md) (1:1 / fit / custom scale, paper config, print element checklist — tiling and calibration are Phase 2, see below), [15-export.md](../specs/15-export.md) (native project export only for MVP), [17-statistics.md](../specs/17-statistics.md)
**Depends on:** M8
**Deliverables:** Print preview with independently selectable elements, scale modes, paper size/orientation; pin/thread statistics panels.
**Exit criteria:** MVP-scoped Test Cases in 14 (excluding tiling/calibration), 15 (native export only), and 17 pass.

### M10 — Phase 2 (post-MVP)
**Specs:** Remainder of [14-printing.md](../specs/14-printing.md) (tiling, calibration), remainder of [15-export.md](../specs/15-export.md) (SVG, PDF, PNG, JPEG), autosave in [16-persistence.md](../specs/16-persistence.md), configurable spiral twist density in [12-thread-editor.md](../specs/12-thread-editor.md), advanced board textures in [04-board-appearance.md](../specs/04-board-appearance.md).
**Depends on:** M9
**Deliverables/Exit criteria:** tracked as separate sub-tickets when this milestone starts — do not scope in detail until MVP (M0–M9) is Done, per [00-overview-and-scope.md](../specs/00-overview-and-scope.md) MVP/Phase 2 split.

### M11 — Play Mode
**Specs:** [19-play-mode.md](../specs/19-play-mode.md)
**Depends on:** M9 (reuses the SVG-rasterization/canvas-export precedent established by M9/M10's PNG/JPEG export)
**Deliverables:** A fifth Editor mode replaying Thread Path draw order segment-by-segment, with full transport controls (play/pause, stop, first/previous/next/last, editable frame number, read-only total, per-frame interval) and a video-export button (`captureStream`/`MediaRecorder`, `.webm`).
**Exit criteria:** All Test Cases in 19 pass as automated tests (video-export browser mechanics excepted, per the same jsdom-rasterization limitation already accepted for PDF/raster export in M10).

### M12 — In-App Help
**Specs:** [20-help.md](../specs/20-help.md)
**Depends on:** M2–M11 (documents every tool/mode/panel built through Play mode, plus Layers from M6)
**Deliverables:** A "Help" toolbar button (between Stats and Print) opening a full-bleed overlay with seven tabs (Edit, Pin, Thread, Pan, Play, Layers, Keyboard & Mouse) covering every tool/button documented in specs 05–13, 19, plus a consolidated keyboard-shortcut/mouse-gesture reference. Data-driven content module (`helpContent.ts`) rather than bespoke per-tab components. Default tab matches the current Editor mode on open.
**Exit criteria:** All Test Cases in 20-help.md pass as automated tests; content only documents features actually implemented (spec-vs-code gaps already tracked elsewhere, e.g. wheel-zoom in 05-canvas-and-viewport.md, are *not* described as available).

### M13 — Scale Tool & Pin Distance
**Specs:** [21-scale-and-pin-distance.md](../specs/21-scale-and-pin-distance.md)
**Depends on:** M6, M7
**Deliverables:** Fifth Edit-mode tool (Scale — press-drag-release, resizes the selected Pin Path about its own centroid), Pin distance numeric input in the Selection panel, both recomputing pins through the existing 07-pin-geometry-engine.md distribution rules, plus Nearest-Pin Reattachment so existing Thread Path segments follow the recomputed pins instead of going dangling — one undo step per Scale drag or Pin distance edit, covering both the pin layer and thread layer change together.
**Exit criteria:** Full Test Cases in 21 pass; locked-layer blocking and Esc-cancel verified; live-verified via Playwright (scale a shape with a connected thread, confirm the thread stays attached to the nearest new pin; same for Pin distance; confirm single-step Undo).

### M14 — Thread Draft Pattern-Follow (Right Arrow)
**Specs:** [22-thread-follow-pattern.md](../specs/22-thread-follow-pattern.md)
**Depends on:** M5
**Deliverables:** An `ArrowRight` shortcut in Thread Draw mode that, once the in-progress draft has 4+ vertices, auto-appends the next vertex by extrapolating the numeric pin-position pattern of the vertices added so far (interleaved-by-position-parity arithmetic sequences, wrapping around whichever Pin Path the pattern currently occupies). Pure logic in `src/application/document/threadPattern.ts`, wired via a new non-undoable `EditorStore.advanceThreadDraftByPattern()` method (same transient-draft treatment as `extendThreadDraft`/`retractThreadDraft`), keyboard-wired in `useThreadDrawing.ts` alongside the existing `Escape`/`ArrowLeft` handling. In-app Help content and README.md updated to document the new shortcut.
**Exit criteria:** Full Test Cases in 22-thread-follow-pattern.md pass as automated tests (unit tests for the extrapolation logic, a store-level test for the 16-pin wraparound example, a Canvas interaction test firing a real `ArrowRight` keydown); live-verified via Playwright per this document's UI-change convention.

### M15 — Keyboard Move/Rotate/Scale
**Specs:** [23-keyboard-transform.md](../specs/23-keyboard-transform.md)
**Depends on:** M7, M13
**Deliverables:** Arrow-key nudge of the selected Pin Path, routed through whichever of Move/Rotation/Scale is the current Edit tool — 1 unit per press, 10 with Shift, reusing the existing `commitPinPathTransform`/`commitPinPathScale` store methods (locked-layer blocking, undo, and thread reattachment all inherited unchanged).
**Exit criteria:** Full Test Cases in 23-keyboard-transform.md pass as automated tests; live-verified via Playwright.

### M16 — Internationalization
**Specs:** [24-internationalization.md](../specs/24-internationalization.md), [18-design-system.md](../specs/18-design-system.md) (new dropdown component pattern)
**Depends on:** M2–M15 (translates every user-facing string introduced across the whole UI, including M12's Help content)
**Deliverables:** `react-i18next`/`i18next` wired via a module-singleton init (no backend, no Suspense, bundled JSON resources) imported once at app bootstrap and once in the shared Vitest setup so all pre-existing tests keep passing unchanged; English + Portuguese (pt-BR) translations for every string across `src/ui/` (10 namespaces) plus the full Help panel content; a shared, custom-built `LanguageSwitcher` component (Globe trigger + accessible listbox menu, own click-outside/keyboard handling) mounted identically in Board Setup and the Editor Shell top bar, both reading the same global `i18n.language`; first-visit auto-detection from `navigator.language` with an explicit-choice override persisted to `localStorage`; the two user-visible error alert paths (Open Project, Export/PDF) translated via UI-layer discriminant mapping over named error classes, without importing i18next into `src/domain/` or `src/application/`.
**Exit criteria:** Full Test Cases in 24-internationalization.md pass; en/pt-BR key-parity test green across every namespace; all pre-existing tests pass unmodified; live-verified via Playwright (switch language in both mount points, confirm text updates immediately, confirm persistence across reload, confirm auto-detect, confirm a translated error alert, confirm Help panel prose is fully translated, no new console errors).

### M17 — Radial Context Menu
**Specs:** [25-radial-context-menu.md](../specs/25-radial-context-menu.md), [18-design-system.md](../specs/18-design-system.md) (new Radial context menu component + `--shadow-float` token)
**Depends on:** M3, M4, M5, M6, M11, M13 (needs Edit-mode `selectTool` state and Merge from M3/M13, Thread draft mechanics from M5/M14, Play transport from M11)
**Deliverables:** Right-click on the canvas opens a cursor-anchored radial menu (via `@spaceymonk/react-radial-menu`) with mode-aware, icon-only slices with tooltips: Select/Move/Rotate/Scale/Merge (+conditional Commit Merge) in Edit mode; Line/Arc/Ellipse/Circle/Rect/Square/Freehand/Eraser/Path Eraser in Pin mode; Draw/Eraser/Segment (or Cut/Back/Next while a thread draft is active) in Thread mode; Fit/Zoom In/Zoom Out in Pan mode; First/Previous/Play-Pause/Next/Last in Play mode. Replaces the two previous instant right-click actions (Thread draft finish, Merge commit) with menu picks. Every slice calls the same `EditorStore` method its toolbar equivalent already calls — no new business logic.
**Exit criteria:** Full Test Cases in 25-radial-context-menu.md pass as automated tests (slice-set-per-mode unit tests + a component test); live-verified via Playwright per `docs/conventions/workflow.md`.

### M18 — Board Templates
**Specs:** [26-board-templates.md](../specs/26-board-templates.md), [18-design-system.md](../specs/18-design-system.md)
**Depends on:** M8, M16
**Deliverables:** A Templates section on the New Board screen offering 9 pre-built designs (generated ahead of time from external reference art into real `PinPath`/`ThreadPath` document data via `scripts/generateBoardTemplates.mjs`), each loadable via the existing `loadProject`/`fitViewportForBoard` path, replacing the setup wizard entirely on selection.
**Exit criteria:** Full Test Cases in 26-board-templates.md pass; every template file round-trips through `migrateProjectFile`/`projectFileToDocument`; live-verified via Playwright per `docs/conventions/workflow.md`.

## Status Table

| # | Milestone | Depends on | Status | Notes |
|---|---|---|---|---|
| M0 | Project Scaffolding & Command Skeleton | — | Done | Vite+React19+TS+Vitest scaffold, Clean Architecture folders, `Command`/`HistoryStack` (unit-tested), ESLint boundary rules (`import/no-restricted-paths` for ui/**, `no-restricted-imports` for react/react-dom — proven via `boundaries.test.ts`). Lint/test/build/dev all green. |
| M1 | Geometry Engine | M0 | Done | Path/Segment model (Line/CircularArc/EllipticalArc), all MVP shapes (line/arc/ellipse/circle/rect/square/regular-polygon/star/polygram), open+closed pin distribution, viewport transform, snap pipeline (pin>grid priority, screen-space radius), mirror+radial symmetry math. 57 tests, all worked examples from spec verified. |
| M2 | Board & Viewport | M1 | Done | Board setup screen (shape/dims/appearance, undoable), editor shell (top bar, mode switcher, canvas w/ grid+zoom+fit, status bar). SVG rendering adapter (path->d, board fill). 85 tests total. Browser visual check blocked by extension site permissions (localhost not granted) — verified via component tests + build instead. |
| M3 | Pin Editor | M2 | Done | Pin Path model + single default layer (M6 will add the panel), all 9 drawing tools wired to real mouse interaction (line 2-click, arc 3-click w/ live curvature, bounding-box drag for ellipse/circle/rect/square w/ Alt constraint, centre-drag for polygon/star/polygram family), Pin Eraser, Select-mode click-to-select + numeric geometry editing (recalculates pins), live pin-count/spacing status bar. All mutations undoable. 113 tests. Selection geometry editing uses numeric inputs, not canvas drag-handles (scope note). |
| M4 | Symmetry | M3 | Done | Symmetry config on PinPath (none/horizontal/vertical/both/radial), mirrored/radial copies derived on every render (never stored, so source edits always stay in sync), dual-context (defaults vs selected-path, selected-path change is undoable), movable radial centre + interval via numeric fields, canvas overlay (axes/spokes+centre). 120 tests. Radial centre is numeric-input only, not canvas drag (same scope note as M3 selection editing). |
| M5 | Thread Editor | M3 | Done | ThreadPath/ThreadLayer model, full drawing workflow (click extends, double-click finishes+segment, right-click finishes without segment, Esc cancel-or-finish per confirmed-segment count), pin highlight states (normal/candidate/active-origin), 1/2/3-colour spiral rendering (same geometry, dash-offset strands), cascading pin-delete into threads as one undo step (interior removal correctly splits into surviving fragments, endpoint removal keeps one). 137 tests. Thread Eraser removes a whole Thread Path per click (segment-level splitting deferred — scope note); nearest-pin lookup is still an O(n) scan (spatial index deferred per 07-pin-geometry-engine.md §Performance, tracked as a follow-up not silently dropped). |
| M6 | Layers | M3, M5 | Done | Pin/Thread Layers panels (tabs, create/rename/duplicate/delete/reorder/visibility/lock), generic layerOps shared by both kinds, active-layer targeting for new pins/threads, hidden-retains-contents + locked-blocks-edits-but-not-visibility guarantees, layer duplication regenerates pin IDs so existing threads keep pointing at the originals, deleting a layer cascades into thread segments in one undo step. 151 tests. "Select all objects on a layer" (multi-select) not implemented — scope note, would require extending Selection beyond single-object. |
| M7 | Undo/Redo Wiring | M2–M6 | Done | Ctrl/Cmd+Z / Ctrl/Cmd+Shift+Z global shortcut (skips text-entry targets so native input undo isn't hijacked), toolbar buttons reflect live history state. Audit of Undoable Operations list against M2–M6: all covered by Commands except "Add segment" during an in-progress thread draft, which is treated as part of the single "Add thread" commit-on-finish command rather than a per-click undo step (judgment call — undoing mid-draw individual clicks isn't a normal editing action; documented here rather than silently assumed). Full suite: 155 tests, this is the integration checkpoint for M1–M6. |
| M8 | Persistence | M7 | Done | Versioned project file schema (v1) with a migration seam (no-op today, ready for v2), New/Save/Open wired via browser download + file picker (File System Access API / autosave adapter deferred to Phase 2), loading resets undo history, full round-trip verified through real `JSON.stringify`/`parse` including pin IDs and thread pin references staying intact. 163 tests. |
| M9 | Printing & Export (MVP) | M8 | Done | Print preview (independently selectable elements, 1:1/fit/custom scale, paper size+orientation, print-vs-editor visibility independence verified), pin+thread statistics panels (project totals, per-path cards). Native export already satisfied by M8's Save. 179 tests. Print preview is on-screen only (no @media print stylesheet / physical-accuracy calibration — that's explicitly Phase 2). |
| M10 | Phase 2 | M9 | Done | All six Phase 2 items: (1) board appearance UI + custom-texture upload + 8 wood/4 paint presets (this also filled a real M2 gap — no appearance UI existed before); (2) thread twist pitch, configurable per-thread and as a drawing default; (3) autosave (debounced, localStorage, restore/discard banner); (4) print calibration (reference-line flow, correction factor applied to 1:1 scale); (5) print tiling (overlap, trim/alignment marks, page numbers/coordinates, multi-page preview); (6) SVG/PDF/PNG/JPEG export (SVG and PDF are true vector, PDF lazy-loaded via dynamic import since svg2pdf.js is ~470KB and browser-only). 223 tests total. Found and fixed a real pre-existing bug while building tiling: bounding-box code sampled only each path segment's start point, collapsing circles/arcs/ellipses to a single point — silently wrong since M2 (affected Fit-to-viewport too, just never exercised by a test until tiling's math caught it). Also discovered jsdom 25's Blob/File lack `.text()`/`.arrayBuffer()` and `URL.createObjectURL` doesn't exist at all — added a Node-Blob/File swap + a create/revokeObjectURL stub to test setup, which retroactively let M8's FileMenu Save/Open get real tests it never had. PDF export itself (`pdfExport.ts`) cannot be unit-tested here — svg2pdf.js's README states it does not work under jsdom (crashes at module-load); confirmed firsthand, not assumed. Raster export's actual canvas rasterization is similarly untestable in jsdom (no real 2D context backend) — only its pure DPI/mime-type math is covered. |
| M11 | Play Mode | M9 | Done | Fifth Editor mode (`"play"`) replaying Thread Path draw order one segment at a time; frame model reconstructed from current array order (no timestamp field exists — documented, accepted approximation, see 19-play-mode.md). Transport is local `EditorShell` state (`usePlaybackTransport`), not routed through `EditorStore`/undo — matches the existing `overlay` (Print/Stats) precedent, since Play mode never mutates the document. Reuses `BoardLayer`/`GridLayer`/`PinLayersView`/`ThreadLayersView` unchanged in a new dedicated `PlaybackCanvas` (deliberately not the interactive `Canvas`, which is built entirely around per-mode pointer dispatch Play mode has no use for). Video export (`captureStream`+`MediaRecorder`, `.webm` only — no cross-browser MP4 encoder exists) rasterizes each frame by serializing the LIVE `<svg>` DOM node, so the export matches the on-screen multi-colour/twist rendering exactly, rather than reusing the static exporter's simpler single-colour thread rendering. 349 tests total. Video-recording mechanics (`MediaRecorder`/`captureStream`/real canvas rasterization) cannot be unit-tested in jsdom — same class of limitation already accepted for PDF/raster export in M10; only the pure MIME-type-selection and bounding-box/pixel-sizing math are unit-tested, the real recording path verified live via Playwright instead. |
| M12 | In-App Help | M2–M11 | Done | Help button (between Stats and Print) opens a full-bleed overlay matching the Stats/Print convention exactly (no backdrop, no Escape-to-close), with 7 tabs (Edit, Pin, Thread, Pan, Play, Layers, Keyboard & Mouse) mirroring the Mode Switcher order plus 2 cross-cutting topics. Content is one data-driven `helpContent.ts` module rendered through a single generic section/item view, not 7 bespoke components. Default tab matches the current Editor mode on open, re-defaulting on every reopen since the panel only mounts while `overlay==="help"`. Keyboard/Mouse tab documents only what's actually implemented in code (confirmed via full pointer/keydown-handler audit) — omits spec-only, unimplemented gestures (wheel-zoom, shift-click multi-select, arrow-key nudging, Delete/Backspace) rather than describing them as available. 359 tests total (+9 over M11). Live-verified via Playwright: default-tab-per-mode, tab switching, Close, and re-default after a mode switch all confirmed. |
| M13 | Scale Tool & Pin Distance | M6, M7 | Done | Fifth Edit tool (Scale, grouped with Move/Rotation/Merge) — press-drag-release, horizontal-pixel-delta-driven factor, always pivots on the shape's own centroid (centre stays invariant, only size fields scale; Arc's curvature scales too since it's a physical sagitta length). Pin distance numeric field added to the Selection panel. Both feed into the existing per-shape `distributePins` pipeline unchanged, then reattach every Thread Path pin reference to its nearest new pin (`buildNearestPinRemap` over `allPinsWithMirrors`, generalizing Merge's remap-and-collapse via new `remapPinsInThreadPathByMap`) as one bundled `{pinLayers, threadLayers}` undo step (`commitPinPathWithReattach`, shared by `commitPinPathScale` and `setPinProperty`'s spacing branch). 384 tests total (+25 over M12). Live-verified via Playwright: radius scale up/down with centroid fixed, pin-count recompute, thread visibly reattached to the new nearest pin, single-step Undo, Esc-cancel leaves state untouched, locked-layer blocks the commit (re-verified on a clean reload after an initial debug-instrumented run was confounded by Vite/Chromium HTTP caching serving a stale module — not a real defect, noted here so a future session doesn't rediscover the same red herring). Scope note: this reattachment fix is intentionally limited to Scale and Pin distance, per explicit request — other numeric geometry-field edits (typed-in Position/Radius/etc.) still mint fresh pin ids with no thread reattachment, a pre-existing gap left as-is. |

| M14 | Thread Draft Pattern-Follow (Right Arrow) | M5 | Done | `ArrowRight` in Thread Draw mode: once the draft has 4+ vertices, extrapolates the next one from the two interleaved-by-position-parity arithmetic sequences the vertices form (`threadPattern.ts`'s `computeNextPatternPinId`), wrapping around whichever Pin Path the pattern's most recent member in that group belongs to. Wired as a non-undoable `EditorStore.advanceThreadDraftByPattern()` (same transient-draft treatment as `extendThreadDraft`/`retractThreadDraft`), keyed in `useThreadDrawing.ts` alongside `Escape`/`ArrowLeft`. 403 tests total (+19 over M13). Live-verified via Playwright: seeded pins 3/6/9/12 on a real 16-pin circle Pin Path, sent real `ArrowRight` keydowns through the mounted Canvas, confirmed the exact 15/2/5/8 wraparound sequence from the spec's worked example, confirmed `ArrowLeft` still composes correctly on top of pattern-added vertices, and confirmed the new Help panel copy renders. Scope note: only plain (non-mirrored) pins participate — a symmetry-mirrored pin id has no stable Pin-N position, so it makes `ArrowRight` a no-op rather than guessing, documented in 22-thread-follow-pattern.md's Scope limits. |

| M15 | Keyboard Move/Rotate/Scale | M7, M13 | Done | Arrow-key nudge (`useKeyboardTransform.ts`) driven by whichever of Move/Rotation/Scale is the active Edit tool — 1px/1°/1% per press, 10x with Shift, ignoring text-entry targets. Reuses `commitPinPathTransform`/`commitPinPathScale` unchanged (locked-layer blocking, undo, thread reattachment inherited for free); `geometryCenter` exported from `pinPath.ts` as the keyboard Rotate/Scale pivot (unlike the mouse Rotation tool's external press-point pivot); `isTextEntryTarget` extracted to `src/ui/keyboard.ts`, shared with the Undo/Redo shortcut. Big-step modifier was originally Alt/Cmd, switched to Shift after live use showed Alt/Cmd colliding with OS/browser shortcuts (e.g. macOS Option+Arrow word-navigation). Hold-to-repeat: native `event.repeat` auto-fire is ignored in favour of a homegrown `setTimeout`/`setInterval` loop at 500ms delay / 100ms interval — the WPF/UWP `RepeatButton` de facto standard (researched, not guessed), chosen because native OS key-repeat delay/rate is user-configurable and inconsistent across machines. 19 new tests (423 total, incl. fake-timer coverage of the delay/interval/keyup/mid-hold-lock/key-switch cases). Help content and README updated. Live-verified via Playwright + the debug hook: Move/Shift+Move nudge distances matched `screenDistanceToDocument` at the real fitted-viewport zoom (8.57, not the pre-fit default of 4) exactly; Rotate confirmed anticlockwise/clockwise signs and centroid invariance on a Rectangle; Scale confirmed 1%/10% factors, centroid invariance, pin-count recompute, and thread reattachment to the nearest new pin; single-step Undo confirmed for both Move and Scale; locked-layer blocking confirmed; confirmed a focused numeric input's own native arrow-stepping fires instead of the keyboard transform (width went 8→8.1 via the input's own step, not 8.08). Hold-to-repeat also live-verified with real timing (`page.keyboard.down`/`up` + real waits, not fake timers): holding ArrowRight 650ms produced exactly 2 commits (immediate + one repeat tick at ~600ms, `10 + 2×0.1167 = 10.233`), release stopped it immediately, and 2 separate Undo steps restored the original position. |
| M16 | Internationalization | M2–M15 | Done | `react-i18next`/`i18next` wired via a Provider-less module-singleton (bundled JSON resources, `initAsync: false`, no Suspense) imported once in `main.tsx` and once in `test/setup.ts` — verified all 441 pre-existing tests kept passing unmodified throughout every extraction batch, since `useTranslation()` falls back to the singleton when no `<I18nextProvider>` wraps the tree. English + full Portuguese (pt-BR) translations across 10 namespaces (`common`, `boardSetup`, `editorShell`, `menus`, `toolbars`, `panels`, `printPreview`, `canvas`, `help`, `errors`) covering every `src/ui/` component plus `src/App.tsx`'s autosave-restore banner (outside `src/ui/`, caught only by a live-verification pass, not the static string inventory) and the full ~240-line Help panel (`helpContent.ts` restructured to hold translation keys, not literal text). Custom-built `LanguageSwitcher` (Globe trigger + accessible listbox, own click-outside/Escape/arrow-key handling — a new dropdown pattern, `18-design-system.md` had none) mounted identically in Board Setup and the Editor Shell top bar, both reading the shared global `i18n.language`. First-visit auto-detect from `navigator.language` (`pt*` → pt-BR), explicit choice persisted to `localStorage` (`stringartit:language:v1`) and overriding detection thereafter. `projectFile.ts` error throws promoted to named classes (`InvalidProjectFileError`, `NoMigrationPathError`, `IncompatibleProjectVersionError` gaining a public `foundVersion` field) so `src/ui/toolbars/openFileErrors.ts` can translate the Open-file alert via `instanceof` discrimination instead of matching English `.message` text; domain-layer invariant-guard errors and raw export-failure `err.message` text stay untranslated by design (framework boundary / unscripted diagnostic detail, respectively). 453 tests total (+30 over M15): `detectLanguage.test.ts`, `languagePreference.test.ts`, `LanguageSwitcher.test.tsx`, `keyParity.test.ts` (walks every en/pt-BR namespace pair, asserts identical key sets — all 10 pass), `LanguageSwitcher.integration.test.tsx` (real `BoardSetup`/`EditorShell` mount, switches language, asserts real translated text renders in both). Live-verified via Playwright: language switch on both mount points updates text immediately; Help panel's 7 tabs fully render in Portuguese; localStorage persistence confirmed across reload in both directions; a live Open-file error alert round-tripped through Portuguese and back to English. Live verification caught one real bug the unit tests couldn't (they only assert `alertSpy` was called, never the message content): `openFileErrors.ts`'s `t()` calls omitted `{ns: "errors"}`, so under `FileMenu.tsx`'s `useTranslation(["menus","errors"])` (default ns "menus", the first array element) they silently resolved to the raw key string instead of translated text — fixed by making every call pass `{ns: "errors"}` explicitly rather than relying on the caller's hook default namespace. |

| M17 | Radial Context Menu | M3, M4, M5, M6, M11, M13 | Done | Right-click on the canvas opens a cursor-anchored radial menu (`@spaceymonk/react-radial-menu`, zero-dep, React-19-safe — chosen over `react-circular-menu` (styled-components dep), `@jonbell/react-radial-menu` (excludes React 19), and a custom-built menu (user's explicit choice)), themed to design-system tokens via the library's internal CSS custom properties + a new `--shadow-float` token (its first floating-component use). Slices are mode/sub-state-aware (`radialMenuSlices.ts`, pure + unit-tested): Select/Move/Rotate/Scale/Merge (+conditional Commit Merge at 2+ accumulated pins) in Edit mode; the 9 basic Pin tools (Polygon/Star family deliberately excluded — toolbar-only, scope note); Draw/Eraser/Segment in Thread mode, entirely swapped for Cut/Back/Next while a draft is in progress; Fit/Zoom In/Zoom Out (anchored at the click point, not viewport centre) in Pan mode; the full playback transport in Play mode. Replaces the two previous instant right-click actions (Thread draft finish, Merge commit) with menu picks — a deliberate behaviour change, confirmed with the user first. Mounted once in `EditorShell.tsx` (not `Canvas.tsx`) since it must cover both the interactive Canvas and the separate `PlaybackCanvas` (Play mode). Escape/outside-click dismissal is a deliberate departure from the Print/Stats/Help `overlay` convention's "no Escape-to-close" rule, documented as such. 473 tests total (+20 over M16), including 2 pre-existing Canvas tests adapted since the behaviour they covered moved to EditorShell. Live-verified via Playwright + the debug hook: real right-click + real slice clicks confirmed the exact slice set and correct store mutation for all 5 modes, the Commit Merge conditional slice (absent below 2, appears at 2+, merges on click), the Thread draft swap (Next no-ops below 4 vertices, Back retracts, Cut commits as one undo step), Pan zoom anchored at the click point (asymmetric panOrigin shift confirmed, not the toolbar's centre-anchor), Play mode's Next Frame slice advancing playback, and Escape/outside-click both dismissing without side effects — no new console errors (one pre-existing, already-documented ModeSwitcher border/borderColor warning from M11 fired on ordinary mode switches, unrelated to this feature). Help panel's `rightClick` entry and README updated to describe the new radial-menu behaviour instead of the old instant right-click actions. |

| M18 | Board Templates | M8, M16 | Done | Templates section on the New Board screen (`BoardSetup.tsx`), 9 template cards (name-only, no thumbnail image — a plain manifest `boardTemplates.ts` of `{id, nameKey, url}`, mirroring `helpContent.ts`'s data-driven pattern). Each template is a real, standalone `ProjectFile` (the app's own save format) generated ahead of time by `scripts/generateBoardTemplates.mjs` from external reference art (`.tmp/patterns/*.svg`, scratch/not committed) and written as a static JSON asset to `public/board-templates/<id>.json` — fetched on click, never bundled, and containing no SVG or reference to the source art. The generation script does real shape detection rather than forcing everything into circles: it fits each source's one drawn arm (or whole shape) against line/circle/ellipse/rectangle/square/regular-polygon/star, gated by an edge-hugging check so a shape merely having incidental rotational symmetry isn't misclassified as a clean polygon (caught and fixed a real case: Lotus's organic blob passed a loose rotational-symmetry test but its points didn't actually sit on the fitted polygon's edges, so it correctly falls back to `freehand`), falling back to explicit `freehand` only when nothing else fits — and separately detects when a source is really one arm repeated via the app's own radial/mirror Symmetry feature, storing only that one arm plus `<pinId>~mirror-N` thread references (`application/document/symmetryConfig.ts`) exactly as a person building it by hand would. Results verified case by case: Spiral → one big Circle; Comet → one Arc mirrored horizontally; Polygon → a Pentagon; Star of David → correctly rejected as a simple star (it's really two overlapping triangles) and left as a faithful `freehand`; Flower/Lotus/Sun/Spirals/Assymetry → `freehand` (confirmed to have no matching primitive, not assumed). Selecting a card calls the same `loadProject`/`fitViewportForBoard` path `FileMenu`'s Open action already uses (with the same translated error-alert handling on a failed fetch), replacing the wizard entirely (fresh undo history, confirmed `undoStack.length === 0` immediately after via the debug hook). 12 new tests (485 total): round-trip validity of every template file read from `public/board-templates/`, template-card rendering, fetch-mocked click-behaviour, and a load-failure alert test in `BoardSetup.test.tsx`. Live-verified via Playwright for every template: all 9 cards render name-only with no console errors; Spiral/Comet/Polygon/Star-of-David spot-checked pixel-visually and confirmed correct; main JS bundle dropped back to ~540KB (from an earlier bundled-thumbnail prototype's ~1.27MB) since template data is no longer part of the bundle. Known scope notes: (1) only Spiral, Comet, and Polygon are exact-by-construction; the rest are honest `freehand` approximations, not pixel-exact; (2) each template's chord sequence is evenly downsampled (not head-truncated) to at most 1200 source lines for interactive performance — the densest sources (Lotus/Sun, originally ~2600-3000 lines) lose some density but keep full spatial coverage. Fixed a real fidelity bug caught by live re-verification: the `freehand` fallback's points were ordered by angle-from-centroid (fine for shape-*fitting*, which only needs a point set), but a multi-lobe organic shape scatters badly under angle order — two points on adjacent lobes can share nearly the same angle while being far apart in space, so the resulting Pin Path zig-zagged between lobes instead of tracing the drawn shape (visually: pins scattered with no coherent connecting path). Replaced with a greedy nearest-neighbour chain (`orderByNearestNeighbourChain`, starting from the point farthest from centroid) for every freehand fallback; re-verified live for Flower, Lotus, Assymetry, and Star of David — all now show a coherent traced outline instead of a tangled crisscross. |

Status values: `Not Started`, `In Progress`, `Blocked` (note the blocker), `Done`.

## Definition of Done (applies to every milestone)

- [ ] All Gherkin scenarios in the milestone's listed specs exist as automated tests and pass.
- [ ] No domain/application code imports React or `src/ui/` (M0's boundary check stays green).
- [ ] All new mutations route through a `Command` (undoable), not direct state assignment.
- [ ] UI matches [18-design-system.md](../specs/18-design-system.md) tokens/components (no ad-hoc colours, fonts, radii, or one-off component patterns).
- [ ] Status Table row updated to `Done` with a one-line note (what shipped, any deferred edge case).
- [ ] Any spec correction made during implementation is committed alongside the code change, not left as a mental note.
