# StringArtIt — Development Orchestrator

## Purpose

This is the single navigation document for building StringArtIt against `docs/specs/`. It sequences work into milestones, states which specs and design-system rules govern each one, states dependencies between milestones, and carries a status table to track progress across sessions. Any session picking up this project should start here, find the first milestone not marked Done, read its listed specs, implement, test, update status, and move on — rather than re-reading all 19 specs cold every time.

Specs live in [`docs/specs/`](../specs/), numbered 00–19. Design reference lives in [`docs/design/editor-mockup.dc.html`](../design/editor-mockup.dc.html). This document does not restate spec content — it only sequences and cross-references it.

## How to Use This Document

1. Find the first row in the [Status Table](#status-table) not marked `Done`.
2. Check its **Depends on** column — those milestones must be `Done` first (or explicitly descoped).
3. Read its **Specs** column in full before writing code.
4. Implement inside the layer boundaries from [01-architecture.md](../specs/01-architecture.md) — domain/geometry code stays framework-free; UI code stays in `src/ui/`.
5. For any UI milestone, also read [18-design-system.md](../specs/18-design-system.md) — tokens/components are not optional polish, they're the spec.
6. Turn every `Gherkin` scenario in the milestone's specs into an actual automated test (Vitest for domain/application logic, a component/interaction test for UI behavior). A milestone is not Done until its specs' test cases are green.
7. Update the Status Table (status + one-line note) before ending the session on that milestone.
8. If implementation reveals a spec is wrong, ambiguous, or incomplete: fix the spec file first, in the same change, then implement against the corrected spec. Specs are living documents, not frozen requirements — but they must never silently drift from what's built.

## Architectural Note: Commands Come Early, Not Last

The product spec's own [Recommended Development Sequence](../specs/00-overview-and-scope.md#recommended-development-sequence) lists the undo/redo Command architecture as "Phase G," near the end. **Do not build it last.** Every mutation in [10-undo-redo.md](../specs/10-undo-redo.md) and the cascading-delete rule in [11-erasers.md](../specs/11-erasers.md) require that document mutations already go through Command objects (per [01-architecture.md](../specs/01-architecture.md)) — retrofitting that onto a codebase full of direct state mutations is a rewrite, not an add-on. This orchestrator therefore pulls the **Command skeleton** into M0 (empty `execute/undo/redo` plumbing + a `HistoryStack`) and requires every milestone from M2 onward to route mutations through it from the start. The full undo/redo *UI and shortcut wiring* (M7) still comes later — only the underlying mechanism moves earlier.

## Milestones

### M0 — Project Scaffolding & Command Skeleton
**Specs:** [01-architecture.md](../specs/01-architecture.md)
**Depends on:** —
**Deliverables:**
- Vite + React 19 project, Node 22 LTS engines pinned (`package.json` `engines`).
- Folder structure per architecture spec: `src/domain/`, `src/application/`, `src/infrastructure/`, `src/ui/`.
- Test runner configured (Vitest recommended — fast, ESM-native, works headless for domain code) and a component-testing setup for `src/ui/`.
- `Command` interface + `HistoryStack` (execute/undo/redo, linear history, redo cleared on new command) in `src/application/commands/`, unit-tested against the generic contract (not yet wired to any real mutation).
- Lint boundary check (even a simple script/ESLint rule) that fails CI if `src/domain/` or `src/application/` imports from `src/ui/` or `react`.
**Exit criteria:** boundary-check script passes on an intentionally-violating test file (proves it actually catches violations); `HistoryStack` unit tests cover execute/undo/redo/redo-invalidation.

### M1 — Geometry Engine
**Specs:** [07-pin-geometry-engine.md](../specs/07-pin-geometry-engine.md), geometry portions of [06-symmetry.md](../specs/06-symmetry.md) (transform math only, not UI), [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (physical↔screen coordinate transform, snapping priority pipeline as pure functions)
**Depends on:** M0
**Deliverables:** `src/domain/shapes/`, `src/domain/paths/`, `src/domain/transforms/`, `src/domain/snapping/`, `src/domain/symmetry/` — path length, point-at-distance, open/closed pin distribution, mirror/radial transforms, grid/pin snapping, snap-radius logic. Pure functions/classes, zero DOM/React.
**Exit criteria:** every Test Cases scenario in 07-pin-geometry-engine.md passes as a unit test, including the exact worked examples (8cm/1cm→9 pins; 7.5cm/1cm→8 pins; 31cm perimeter/2cm→16 pins @1.9375cm). Mirror/radial transform tests from 06-symmetry.md's geometry-only scenarios pass.

### M2 — Board & Viewport
**Specs:** [02-document-model.md](../specs/02-document-model.md) (Board shape), [03-board-configuration.md](../specs/03-board-configuration.md), [04-board-appearance.md](../specs/04-board-appearance.md), [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (full — modes, grid, zoom/pan, status bar), [18-design-system.md](../specs/18-design-system.md)
**Depends on:** M1
**Deliverables:** Board setup flow (shape + dimensions + appearance), main editor shell (top bar, mode switcher, canvas viewport, status bar) rendering an empty board. Physical-unit rendering via M1's coordinate transform. No pins/threads yet.
**Exit criteria:** Test Cases in 03, 04, 05 pass; editor shell visually matches `docs/design/editor-mockup.dc.html` token-for-token (colours/type/spacing per 18-design-system.md).

### M3 — Pin Editor
**Specs:** [08-pin-tools-and-properties.md](../specs/08-pin-tools-and-properties.md), [09-selection-and-editing.md](../specs/09-selection-and-editing.md) (Pin Path portions), [11-erasers.md](../specs/11-erasers.md) (Pin Eraser only — thread cascade deferred to M5)
**Depends on:** M2
**Deliverables:** Pin mode drawing tools (Line/Arc/Ellipse/Circle/Rectangle/Square/Polygon-Star dropdown), Alt constraint modifier, live pin/spacing preview, guide lines, pin property panel, Pin Path selection + geometry editing, Pin Eraser. All mutations go through M0's Command architecture.
**Exit criteria:** Test Cases in 08, 09 (pin portions), and the Pin Eraser scenarios in 11 pass. Every pin-creating/editing action is undoable (manual check against 10-undo-redo.md's list, even though M7 wires the shortcuts).

### M4 — Symmetry
**Specs:** [06-symmetry.md](../specs/06-symmetry.md) (full — UI wiring on top of M1 math)
**Depends on:** M3
**Deliverables:** Symmetry mode controls, live mirrored-copy generation while drawing, movable radial centre, source-edit-regenerates-copies behavior.
**Exit criteria:** Full Test Cases in 06-symmetry.md pass, including "editing source regenerates copies" and "deleting source removes copies in one undo step."

### M5 — Thread Editor
**Specs:** [12-thread-editor.md](../specs/12-thread-editor.md), [11-erasers.md](../specs/11-erasers.md) (Thread Eraser + cascading pin-delete-removes-threads rule, now that both Pin and Thread objects exist)
**Depends on:** M3
**Deliverables:** Thread mode, nearest-pin detection + highlight states, thread preview, click/double-click/right-click/Esc termination, 1/2/3-colour spiral rendering, Thread Eraser, cascading delete.
**Exit criteria:** Full Test Cases in 12 pass; cascading-delete scenarios in 11 pass as single undo steps. Nearest-pin lookup implementation notes the spatial-index performance requirement from 07-pin-geometry-engine.md §Performance even if a naive O(n) scan ships first — file it as a tracked follow-up, not silently ignored.

### M6 — Layers
**Specs:** [13-layers.md](../specs/13-layers.md)
**Depends on:** M3, M5
**Deliverables:** Pin Layers / Thread Layers panels, create/rename/delete/duplicate/reorder/visibility/lock, per [18-design-system.md](../specs/18-design-system.md) Layer Row component.
**Exit criteria:** Full Test Cases in 13 pass, including hidden-retains-contents and locked-blocks-edits guarantees enforced against M3/M5 editing paths.

### M7 — Undo/Redo Wiring
**Specs:** [10-undo-redo.md](../specs/10-undo-redo.md)
**Depends on:** M2, M3, M4, M5, M6
**Deliverables:** Keyboard shortcuts (Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z), undo/redo toolbar buttons wired to M0's `HistoryStack`, confirmation that every operation listed in 10-undo-redo.md's Undoable Operations table is actually routed through a Command (audit, not new mechanism — the mechanism shipped in M0).
**Exit criteria:** Full Test Cases in 10 pass, run as an end-to-end pass over every milestone delivered so far (this is the integration checkpoint for M1–M6).

### M8 — Persistence
**Specs:** [16-persistence.md](../specs/16-persistence.md), [02-document-model.md](../specs/02-document-model.md) (serialization round-trip)
**Depends on:** M7
**Deliverables:** New/Save/Open/Duplicate, versioned project file schema, migration hook (even if only version 1 exists initially, the seam must exist).
**Exit criteria:** Full Test Cases in 16 pass; a full Project (board + multiple pin/thread layers + symmetry + undo history irrelevant to save) round-trips losslessly through save/load.

### M9 — Printing & Export (MVP slice)
**Specs:** [14-printing.md](../specs/14-printing.md) (1:1 / fit / custom scale, paper config, print element checklist — tiling and calibration are Phase 2, see below), [15-export.md](../specs/15-export.md) (native project export only for MVP), [17-statistics.md](../specs/17-statistics.md)
**Depends on:** M8
**Deliverables:** Print preview with independently selectable elements, scale modes, paper size/orientation; pin/thread statistics panels.
**Exit criteria:** MVP-scoped Test Cases in 14 (excluding tiling/calibration), 15 (native export only), and 17 pass.

### M10 — Phase 2 (post-MVP)
**Specs:** Remainder of [14-printing.md](../specs/14-printing.md) (tiling, calibration), remainder of [15-export.md](../specs/15-export.md) (SVG, PDF, PNG, JPEG), autosave in [16-persistence.md](../specs/16-persistence.md), configurable spiral twist density in [12-thread-editor.md](../specs/12-thread-editor.md), advanced board textures in [04-board-appearance.md](../specs/04-board-appearance.md).
**Depends on:** M9
**Deliverables/Exit criteria:** tracked as separate sub-tickets when this milestone starts — do not scope in detail until MVP (M0–M9) is Done, per [00-overview-and-scope.md](../specs/00-overview-and-scope.md) MVP/Phase 2 split.

### M11 — Play Mode
**Specs:** [19-play-mode.md](../specs/19-play-mode.md)
**Depends on:** M9 (reuses the SVG-rasterization/canvas-export precedent established by M9/M10's PNG/JPEG export)
**Deliverables:** A fifth Editor mode replaying Thread Path draw order segment-by-segment, with full transport controls (play/pause, stop, first/previous/next/last, editable frame number, read-only total, per-frame interval) and a video-export button (`captureStream`/`MediaRecorder`, `.webm`).
**Exit criteria:** All Test Cases in 19 pass as automated tests (video-export browser mechanics excepted, per the same jsdom-rasterization limitation already accepted for PDF/raster export in M10).

### M12 — In-App Help
**Specs:** [20-help.md](../specs/20-help.md)
**Depends on:** M2–M11 (documents every tool/mode/panel built through Play mode, plus Layers from M6)
**Deliverables:** A "Help" toolbar button (between Stats and Print) opening a full-bleed overlay with seven tabs (Edit, Pin, Thread, Pan, Play, Layers, Keyboard & Mouse) covering every tool/button documented in specs 05–13, 19, plus a consolidated keyboard-shortcut/mouse-gesture reference. Data-driven content module (`helpContent.ts`) rather than bespoke per-tab components. Default tab matches the current Editor mode on open.
**Exit criteria:** All Test Cases in 20-help.md pass as automated tests; content only documents features actually implemented (spec-vs-code gaps already tracked elsewhere, e.g. wheel-zoom in 05-canvas-and-viewport.md, are *not* described as available).

### M13 — Scale Tool & Pin Distance
**Specs:** [21-scale-and-pin-distance.md](../specs/21-scale-and-pin-distance.md)
**Depends on:** M6, M7
**Deliverables:** Fifth Edit-mode tool (Scale — press-drag-release, resizes the selected Pin Path about its own centroid), Pin distance numeric input in the Selection panel, both recomputing pins through the existing 07-pin-geometry-engine.md distribution rules, plus Nearest-Pin Reattachment so existing Thread Path segments follow the recomputed pins instead of going dangling — one undo step per Scale drag or Pin distance edit, covering both the pin layer and thread layer change together.
**Exit criteria:** Full Test Cases in 21 pass; locked-layer blocking and Esc-cancel verified; live-verified via Playwright (scale a shape with a connected thread, confirm the thread stays attached to the nearest new pin; same for Pin distance; confirm single-step Undo).

### M14 — Thread Draft Pattern-Follow (Right Arrow)
**Specs:** [22-thread-follow-pattern.md](../specs/22-thread-follow-pattern.md)
**Depends on:** M5
**Deliverables:** An `ArrowRight` shortcut in Thread Draw mode that, once the in-progress draft has 4+ vertices, auto-appends the next vertex by extrapolating the numeric pin-position pattern of the vertices added so far (interleaved-by-position-parity arithmetic sequences, wrapping around whichever Pin Path the pattern currently occupies). Pure logic in `src/application/document/threadPattern.ts`, wired via a new non-undoable `EditorStore.advanceThreadDraftByPattern()` method (same transient-draft treatment as `extendThreadDraft`/`retractThreadDraft`), keyboard-wired in `useThreadDrawing.ts` alongside the existing `Escape`/`ArrowLeft` handling. In-app Help content and README.md updated to document the new shortcut.
**Exit criteria:** Full Test Cases in 22-thread-follow-pattern.md pass as automated tests (unit tests for the extrapolation logic, a store-level test for the 16-pin wraparound example, a Canvas interaction test firing a real `ArrowRight` keydown); live-verified via Playwright per this document's UI-change convention.

### M15 — Keyboard Move/Rotate/Scale
**Specs:** [23-keyboard-transform.md](../specs/23-keyboard-transform.md)
**Depends on:** M7, M13
**Deliverables:** Arrow-key nudge of the selected Pin Path, routed through whichever of Move/Rotation/Scale is the current Edit tool — 1 unit per press, 10 with Shift, reusing the existing `commitPinPathTransform`/`commitPinPathScale` store methods (locked-layer blocking, undo, and thread reattachment all inherited unchanged).
**Exit criteria:** Full Test Cases in 23-keyboard-transform.md pass as automated tests; live-verified via Playwright.

### M16 — Internationalization
**Specs:** [24-internationalization.md](../specs/24-internationalization.md), [18-design-system.md](../specs/18-design-system.md) (new dropdown component pattern)
**Depends on:** M2–M15 (translates every user-facing string introduced across the whole UI, including M12's Help content)
**Deliverables:** `react-i18next`/`i18next` wired via a module-singleton init (no backend, no Suspense, bundled JSON resources) imported once at app bootstrap and once in the shared Vitest setup so all pre-existing tests keep passing unchanged; English + Portuguese (pt-BR) translations for every string across `src/ui/` (10 namespaces) plus the full Help panel content; a shared, custom-built `LanguageSwitcher` component (Globe trigger + accessible listbox menu, own click-outside/keyboard handling) mounted identically in Board Setup and the Editor Shell top bar, both reading the same global `i18n.language`; first-visit auto-detection from `navigator.language` with an explicit-choice override persisted to `localStorage`; the two user-visible error alert paths (Open Project, Export/PDF) translated via UI-layer discriminant mapping over named error classes, without importing i18next into `src/domain/` or `src/application/`.
**Exit criteria:** Full Test Cases in 24-internationalization.md pass; en/pt-BR key-parity test green across every namespace; all pre-existing tests pass unmodified; live-verified via Playwright (switch language in both mount points, confirm text updates immediately, confirm persistence across reload, confirm auto-detect, confirm a translated error alert, confirm Help panel prose is fully translated, no new console errors).

### M17 — Radial Context Menu
**Specs:** [25-radial-context-menu.md](../specs/25-radial-context-menu.md), [18-design-system.md](../specs/18-design-system.md) (new Radial context menu component + `--shadow-float` token)
**Depends on:** M3, M4, M5, M6, M11, M13 (needs Edit-mode `selectTool` state and Merge from M3/M13, Thread draft mechanics from M5/M14, Play transport from M11)
**Deliverables:** Right-click on the canvas opens a cursor-anchored radial menu (via `@spaceymonk/react-radial-menu`) with mode-aware, icon-only slices with tooltips: Select/Move/Rotate/Scale/Merge (+conditional Commit Merge) in Edit mode; Line/Arc/Ellipse/Circle/Rect/Square/Freehand/Eraser/Path Eraser in Pin mode; Draw/Eraser/Segment (or Cut/Back/Next while a thread draft is active) in Thread mode; Fit/Zoom In/Zoom Out in Pan mode; First/Previous/Play-Pause/Next/Last in Play mode. Replaces the two previous instant right-click actions (Thread draft finish, Merge commit) with menu picks. Every slice calls the same `EditorStore` method its toolbar equivalent already calls — no new business logic.
**Exit criteria:** Full Test Cases in 25-radial-context-menu.md pass as automated tests (slice-set-per-mode unit tests + a component test); live-verified via Playwright per `docs/conventions/workflow.md`.

### M18 — Edit-Mode Multi-Select & Granularity
**Specs:** [26-edit-mode-multi-select.md](../specs/26-edit-mode-multi-select.md)
**Depends on:** M6, M13, M15, M17
**Deliverables:** A Pin Path/Pins granularity switch in the Edit Tool area; click/Alt-Cmd-click/rubber-band (with Alt-Cmd union variant) multi-select for both granularities; Move/Rotation/Scale generalized to operate over N selected paths or N selected pins with the combined/mean-centroid rule; Merge unified into an instant action fired against the current selection (path-mode combine-into-one-path-with-full-renumber algorithm, pins-mode destination-path rule with centroid/id tiebreaks), replacing its old independent accumulate-click gesture; radial context menu's Merge slice retargeted.
**Exit criteria:** Full Test Cases in 26 pass; locked-layer all-or-nothing abort verified for every tool; pre-existing single-selection M3/M13/M15 tests still pass; live-verified via Playwright.

### M19 — Thread Select Tool
**Specs:** [27-thread-select-tool.md](../specs/27-thread-select-tool.md)
**Depends on:** M5, M12
**Deliverables:** A 4th Thread-mode tool, Select, for single Thread Path selection; a new Thread properties panel editing colours/width on the selected Thread Path (distinct from the existing drawing-defaults panel); locked-layer blocking.
**Exit criteria:** Full Test Cases in 27 pass; live-verified via Playwright.

### M20 — Grid Settings Dialog
**Specs:** [05-canvas-and-viewport.md](../specs/05-canvas-and-viewport.md) (Grid section + new Grid Settings UI subsection), [18-design-system.md](../specs/18-design-system.md) (new Anchored settings popover component vocabulary entry), [docs/conventions/ui-patterns.md](../conventions/ui-patterns.md) (new Anchored settings popover pattern)
**Depends on:** M2
**Deliverables:** A "Grid settings" button in the Canvas Toolbar, next to Snap, opening a small popover anchored to the button holding Gap X, Gap Y, Grid colour, and Grid opacity (moved out of the toolbar's main row, which previously showed them inline). Grid visibility and Snap remain their own toggle chips, unchanged.
**Exit criteria:** Full Test Cases in 05's new "Grid settings popover" feature pass; live-verified via Playwright.

### M21 — Help: About Tab
**Specs:** [28-help-about-tab.md](../specs/28-help-about-tab.md)
**Depends on:** M12 (In-App Help)
**Deliverables:** An 8th "About" tab appended to the Help overlay's `HELP_TABS` (`src/ui/panels/help/helpContent.ts`), via a new optional `HelpTab.Content` custom-render field (the other 7 tabs keep using `sections`, now optional). New `AboutTabContent.tsx` rendering read-only app-info property rows (name/description/live `package.json` version/author) plus a contact form (Name, Subject restricted to Question/Support/Issue/Other, Message) whose Send control builds and opens a `mailto:lfsvendrame@gmail.com` link, disabled until all three fields are filled.
**Exit criteria:** Full Test Cases in 28 pass; `keyParity.test.ts` green across all 5 locales; live-verified via Playwright.

### M22 — Text Pin Path

**Specs:** [29-text-pin-path.md](../specs/29-text-pin-path.md), multi-contour distribution addendum in [07-pin-geometry-engine.md](../specs/07-pin-geometry-engine.md)
**Depends on:** M3 (Pin Editor), M13 (Scale tool + the geometry-transform machinery Text's generic Move/Rotate/Scale cases plug into)
**Deliverables:** A Text Pin tool (click-once-then-type, next to Freehand) that places an empty Text Pin Path and hands off to Edit mode with it selected; a Selection-panel field group (Font/Weight/Italic/Size/Letter spacing/Text) that regenerates the pin path live on every change; a new multi-contour `PinPathGeometry` variant (`contours: Point[][]`) so a letter's hole/disconnected pieces are representable inside one Pin Path; 20 bundled Google Fonts (`public/fonts/`, SIL OFL 1.1) spanning serif/sans-serif/cursive/monospace via `opentype.js`.
**Exit criteria:** Full Test Cases in 29-text-pin-path.md and the new multi-contour scenarios in 07-pin-geometry-engine.md pass as automated tests; live-verified via Playwright (place text, type a hole-letter word, confirm pins on both the outer and inner contour, Move/Scale/Merge it, Undo, reload a saved project and confirm it renders without re-fetching a font).

### M23 — SEO: Technical Tags & Landing Content

**Specs:** [30-seo.md](../specs/30-seo.md), [18-design-system.md](../specs/18-design-system.md) (landing content must reuse existing tokens/components only), [24-internationalization.md](../specs/24-internationalization.md) (new `landing` namespace)
**Depends on:** M2, M16
**Deliverables:** `index.html` head rewrite (meta description, canonical, Open Graph, Twitter card, theme-color, `SoftwareApplication` JSON-LD, favicon/manifest links); `public/robots.txt`, `public/sitemap.xml`, `public/manifest.webmanifest`, `public/favicon.svg`, `public/apple-touch-icon.png`, `public/og-image.png`; `document.documentElement.lang` synced to the active i18next locale in `src/i18n/index.ts`; four new inline landing sections (`src/ui/panels/landing/`: `LandingHero`, `LandingFeatures`, `LandingHowItWorks`, `LandingFAQ`) wired into `App.tsx`'s `!entered` branch around the existing `BoardSetup` (its own heading demoted `<h1>`→`<h2>`); new `landing` i18n namespace with real copy authored for all 5 locales.
**Exit criteria:** Full Test Cases in 30-seo.md pass (component tests for each landing section, `BoardSetup`'s heading-level test, `keyParity.test.ts` green for the new namespace — 686 tests total); live-verified via Playwright: hero `<h1>` + BoardSetup's demoted `<h2>` + Features/How-it-works/FAQ all render correctly, full BoardSetup flow (shape/dimension/appearance pick → Continue to Editor → EditorShell) confirmed unchanged, language switch updates both the translated `BoardSetup` strings and `document.documentElement.lang` (confirmed `en`→`es`→`en`), no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning and an unrelated favicon.ico 404); built `dist/index.html` inspected directly and confirmed to carry every new meta/OG/Twitter/JSON-LD tag verbatim, since crawlers don't execute JS.

### M24 — WebMCP Agent Tools
**Specs:** [31-webmcp-agent-tools.md](../specs/31-webmcp-agent-tools.md)
**Depends on:** M8, M9, M13 (needs Persistence-era `EditorStore` methods for board/pins/threads/layers stable, plus Scale-era statistics helpers)
**Deliverables:** `src/infrastructure/webmcp/tools.ts` registering board-setup, pin-layer/pin-path, thread-layer/thread-path, undo/redo, and read-only-stats tools via `navigator.modelContext.registerTool` (feature-detected, no-op when unsupported), wired from `App.tsx`; new `projectThreadTotals` in `src/application/document/statistics.ts`.
**Exit criteria:** Full Test Cases in 31-webmcp-agent-tools.md pass as automated tests (registration + every tool's execute handler, against a mocked `navigator.modelContext` — the real Chrome dispatch path isn't testable/live-verifiable anywhere yet, per the spec's Browser support section); live-verified via Playwright that the app boots with zero new console errors and all existing functionality is unaffected when `navigator.modelContext` is absent (today's reality in every shipping browser).

### M25 — Generator Mode
**Specs:** [32-generator-mode.md](../specs/32-generator-mode.md), [18-design-system.md](../specs/18-design-system.md) (ModeSwitcher option-count correction)
**Depends on:** M6 (Layers — Confirm creates layers), M7 (Undo/Redo — Confirm is one Command)
**Deliverables:** A sixth Editor mode/tab, Generate/Re-generate/Confirm workflow (`EditorStore.generatePattern`/`confirmGeneratedPattern`, transient `generatorDraft`), a pattern engine (`src/domain/generator/`, `src/application/document/generator/`: registry + 5 patterns — Mandala/Star/Freestyle/Star of David/Spirals), `GeneratorToolbar`/`GeneratorPanel`/`GeneratorPreviewOverlay`.
**Exit criteria:** Full Test Cases in 32-generator-mode.md pass; live-verified via Playwright.

### M27 — Pin Path Tool
**Specs:** [33-pin-path-tool.md](../specs/33-pin-path-tool.md), touches [07-pin-geometry-engine.md](../specs/07-pin-geometry-engine.md) (Vertex-Anchored Pin Distribution list), [08-pin-tools-and-properties.md](../specs/08-pin-tools-and-properties.md) (tool list), [20-help.md](../specs/20-help.md) (Pin/Keyboard & Mouse tab content), [25-radial-context-menu.md](../specs/25-radial-context-menu.md) (Pin-mode conditional slice set)
**Depends on:** M3 (Pin Editor), M17 (Radial Context Menu — needs the conditional-slice-set machinery Thread already established), M7 (Undo/Redo)
**Deliverables:** A new "Path" Pin tool (`PinTool` id `"polygon"`) — click-per-vertex free-form polygon draft, closing on Esc/right-click "Cut"/clicking back on the first vertex, `ArrowLeft`/"Back" to retract, radial-menu-only "Cancel" to discard outright; new `PinPathGeometry` `"polygon"` variant (vertex-anchored, closed via `closedPolylineShape`); transient `EditorState.polygonDraft`, mirroring `ThreadDraft`'s non-undoable-until-committed shape.
**Exit criteria:** Full Test Cases in 33-pin-path-tool.md pass; live-verified via Playwright per `docs/conventions/workflow.md`.

### M28 — Keyboard Shortcuts

**Specs:** [34-keyboard-shortcuts.md](../specs/34-keyboard-shortcuts.md), touches [18-design-system.md](../specs/18-design-system.md) (toolbar `[X]` shortcut-suffix convention), [20-help.md](../specs/20-help.md) (Keyboard & Mouse tab content), [24-internationalization.md](../specs/24-internationalization.md) (new `help.json` keys)
**Depends on:** M2 (Mode Switcher), M3 (Pin Editor), M5 (Thread Editor), M11 (Play Mode), M12 (In-App Help), M16 (Internationalization), M18 (Edit-Mode Multi-Select & Granularity), M22 (Text Pin Path — Text tool's own text-entry field), M27 (Pin Path Tool)
**Deliverables:** A single central dispatcher (`useKeyboardShortcuts.ts`, mounted once in `EditorShell.tsx`) covering ~40 bindings: New Board screen (`Enter`/`L`), General app-wide (Save/Open/New/Help/Zoom In/Zoom Out/Fit/Grid/Snap), bare-digit tab switching (1-6), and every tool in the Edit/Pin/Thread/Play tabs. `SymmetryPanel.tsx` and `ThreadPropertiesPanel.tsx` refactored to share their type-change/colour-count logic with the keyboard shortcut (`symmetryConfigForType`, `coloursForCount`) so mouse and keyboard can't drift apart. `FileMenu.tsx` and `LanguageSwitcher.tsx` gained imperative-handle refs (`openFilePicker`, `openOrCycle`) so the dispatcher and `BoardSetup`'s own local shortcut listener can trigger their actions without duplicating logic.
**Exit criteria:** Full Test Cases in 34-keyboard-shortcuts.md pass; live-verified via Playwright per `docs/conventions/workflow.md`.

### M29 — Zig-Zag and Parabolic Thread Tools

**Specs:** [35-zigzag-parabolic-tools.md](../specs/35-zigzag-parabolic-tools.md), touches [25-radial-context-menu.md](../specs/25-radial-context-menu.md) (Thread-mode conditional slice set), [34-keyboard-shortcuts.md](../specs/34-keyboard-shortcuts.md) (`Z`/`P` Thread-tab bindings), [20-help.md](../specs/20-help.md) (Thread/Keyboard & Mouse tab content)
**Depends on:** M5 (Thread Editor), M17 (Radial Context Menu), M22 (Thread Follow Pattern — `findPinPosition`), M28 (Keyboard Shortcuts)
**Deliverables:** Two new Thread-mode tools (`ThreadTool` ids `"zigzag"`/`"parabolic"`) that insert a *computed* multi-segment Thread Path between two clicked pins instead of accumulating pin-by-pin. Pure math module `src/application/document/twoPinSequence.ts` (`computeSamePathCandidates`/`computeCrossPathCandidates`/`buildSameRangeSequence`/`buildCrossPathSequence`) — one shared interleave-with-optional-reverse algorithm covering both tools and both same-path/cross-path cases. Transient `EditorState.twoPinDraft`, mirroring `ThreadDraft`'s non-undoable-until-committed shape, plus a 3-click disambiguation flow (only when a closed path or an open direction choice leaves 2+ valid candidates) reusing the Arc tool's "hover previews, a free click confirms" convention. New `useTwoPinSequenceDrawing.ts` hook + `TwoPinDraftLayer.tsx` preview, `Z`/`P` keyboard shortcuts, and Thread-mode radial-menu slices (`threadZigzag`/`threadParabolic`, plus a draft-mode Cut/Back/Cancel swap).
**Exit criteria:** Full Test Cases in 35-zigzag-parabolic-tools.md pass; live-verified via Playwright per `docs/conventions/workflow.md`.

### M30 — Component Styling Migration (inline style → CSS files)
**Specs:** [18-design-system.md](../specs/18-design-system.md) (no new numbered spec — internal refactor, no new user-facing Gherkin scenario)
**Depends on:** — (all prior milestones already Done; purely a presentation-layer refactor with no new behavior)
**Deliverables:** Move all 36 components' inline `style={{...}}` props to co-located CSS files, extending the one existing precedent (`AutosaveDialog.tsx`/`.css`, BEM-ish classes, `.btn.<component-class>` compounded for deterministic cascade-specificity over `.btn`) to the whole codebase. Two new shared components extracted along the way: `<AnchoredPopover>` (+ `usePopoverDismiss` hook) replacing the duplicated anchored-popover recipe in `ExportMenu`/`LanguageSwitcher`/`GridSettingsPopover`/`ChangeBackgroundPopover`/`ZoomControl`, and `<OverlayPanel>` replacing the duplicated full-bleed-overlay recipe in `StatisticsPanel`/`HelpPanel`/`PrintPreviewPanel`. New shared `theme.css` classes: `.is-disabled`, `.tab-underline`/`.tab-underline--active`. Seven batches (see `docs/conventions/ui-patterns.md` once updated, or session history): 1) landing pages + field primitives + `ModeSwitcher` pilot, 2) `AnchoredPopover` + its 5 consumers, 3) `OverlayPanel` + `StatisticsPanel`/`HelpPanel`/`AboutTabContent`/`LayersPanel` + tab-underline, 4) `PrintPreviewPanel` alone (largest file), 5) remaining toolbars + canvas chrome, 6) remaining panels, 7) `EditorShell` last. Fixes a known, previously-tracked React console warning in `ModeSwitcher.tsx` (shorthand/longhand `border`/`borderColor` set on one style object) as a structural side effect.
**Exit criteria:** Zero `style={{` usages remain in `src/`; `src/test/style/cascadeGuard.test.ts` and `src/test/style/componentStyleSnapshot.test.ts` both green with every snapshot diff reviewed (not blindly regenerated); `npm run build`, `npx eslint .`, `npx vitest run` all green; each batch live-verified via Playwright (`test-with-debug-hook` skill) for visual parity and zero new console errors; `docs/conventions/ui-patterns.md` updated to describe the CSS-file convention and the two new shared components; the `ModeSwitcher` warning confirmed gone.

## Status Table

| # | Milestone | Depends on | Status | Notes |
|---|---|---|---|---|
| M0 | Project Scaffolding & Command Skeleton | — | Done | Vite+React19+TS+Vitest scaffold, Clean Architecture folders, `Command`/`HistoryStack` (unit-tested), ESLint boundary rules (`import/no-restricted-paths` for ui/**, `no-restricted-imports` for react/react-dom — proven via `boundaries.test.ts`). Lint/test/build/dev all green. |
| M1 | Geometry Engine | M0 | Done | Path/Segment model (Line/CircularArc/EllipticalArc), all MVP shapes (line/arc/ellipse/circle/rect/square/regular-polygon/star/polygram), open+closed pin distribution, viewport transform, snap pipeline (pin>grid priority, screen-space radius), mirror+radial symmetry math. 57 tests, all worked examples from spec verified. |
| M2 | Board & Viewport | M1 | Done | Board setup screen (shape/dims/appearance, undoable), editor shell (top bar, mode switcher, canvas w/ grid+zoom+fit, status bar). SVG rendering adapter (path->d, board fill). 85 tests total. Browser visual check blocked by extension site permissions (localhost not granted) — verified via component tests + build instead. **Post-ship addition (2026-09-20, requested by the user):** every tab now always starts from a fixed baseline instead of keeping whatever was last dialled in. `createDefaultBoard()` (`board.ts`) now defaults to Solid `#3b3b3b` instead of painted-wood/walnut (shape/diameter already matched). `EditorStore.setMode()` (`EditorStore.ts`) extended, mirroring its existing Pin-mode `pinDefaults`/`symmetryDefaults` reset-on-entry precedent: entering Select (Edit tab) resets `selectTool`→`"select"`/`selectGranularity`→`"path"`; entering Pin also resets `pinTool`→`"circle"`; entering Thread resets `threadTool`→`"draw"` and `threadDefaults` to a new `DEFAULT_THREAD_DEFAULTS` constant (one colour `#5b8def`, width 1.5). Generate tab already lands on Mandala for free — `GeneratorPanel`'s pattern selection is local `useState` that remounts fresh every time `EditorShell` conditionally mounts it on entering `"generate"` mode; left as-is, just live-verified. 921 tests total (net +4 new: 3 in `EditorStore.test.ts` covering Pin/Select/Thread mode-entry resets, 1 new `createDefaultBoard` test in `board.test.ts`). Live-verified via Playwright + the debug hook: New Board screen shows Circle/60cm/Solid/`#3b3b3b`; dirtying each tab's tool/defaults then round-tripping through another tab and back confirmed every value snaps back to baseline; picking a non-Mandala generator pattern and switching tabs away/back confirmed it resets to Mandala through real UI clicks. **Follow-up (2026-09-20, same request):** "Continue to Editor" (`App.tsx`'s `enterEditor`) never reset `mode` itself — if a prior session left the store in another mode (e.g. via File → New after using Thread/Generate), Continue to Editor landed there instead of Pin. Fixed by setting `store.setMode("pin")` immediately before `enterEditor()` in the `BoardSetup`'s `onContinue` prop only — scoped there deliberately, not folded into `enterEditor()` itself, so the autosave banner's Restore button (which also calls `enterEditor()`) keeps resuming whatever mode the restored session was in. 922 tests total (+1: `App.newProject.test.tsx` covers dirtying `mode` via the debug hook, clicking Continue to Editor, and asserting the Pin tab's `aria-selected`). Live-verified via Playwright + the debug hook: set `mode` to `"thread"`, clicked Continue to Editor, confirmed `store.getState().mode` came back `"pin"`. **Post-ship addition (2026-09-20, requested by the user):** added a "Change background" toolbar button (wallpaper icon) next to Grid Settings in `CanvasToolbar.tsx`, so board appearance can be edited after leaving the New Board screen, not just at board-creation time. New `ChangeBackgroundPopover.tsx` (`src/ui/canvas/`) copies the anchored-popover shell from `GridSettingsPopover.tsx` verbatim (local open state, pointerdown-outside + Escape dismissal, `position:absolute; top:110%` panel) per `docs/conventions/ui-patterns.md` §Anchored settings popover, and embeds the existing `BoardAppearancePanel` (`src/ui/panels/BoardAppearancePanel.tsx`) as its body unchanged — that panel already calls `store.setBoardAppearance()` directly on every control change (undoable via `HistoryStack`, already triggers a live `BoardLayer` re-render), so no new store method or state was needed for "reflect immediately." Added `canvasToolbar.changeBackground` i18n key to all 5 locales. 7 new tests in `ChangeBackgroundPopover.test.tsx` (963 tests total, up from 956). Live-verified via Playwright + the debug hook: opened the popover, clicked Radial to switch appearance type, confirmed both `store.getState().board.appearance` and the rendered board fill updated instantly with no extra click; confirmed outside-click and Escape both close it. Noted, not caused by this change and out of scope to fix: a pre-existing React inline-style (`border`/`borderColor` shorthand-conflict) console warning surfaces from the autosave-restore banner (`App.tsx`) on some re-renders. **Post-ship addition (2026-09-20, requested by the user):** `CanvasToolbar.tsx`'s zoom display (a plain `<span>{percent}%</span>` between the Zoom Out/Zoom In buttons) became an editable combobox, `ZoomControl.tsx` (`src/ui/canvas/`) — four independent ways to change zoom now all coexist: the existing +/- buttons and bare +/- keyboard shortcuts (untouched), typing any percent directly into the input (Enter or blur commits, clamped to [5, 1600]), and a dropdown of preset percents (10/25/50/75/100/150/200/300/400/800/1600) opened via a chevron button. New `zoomToPercentStep`/`ZOOM_PRESET_PERCENTS` in `zoomSteps.ts`, same `VIEWPORT_CENTER` anchor as `zoomInStep`/`zoomOutStep` so all four paths stay consistent. Follows `docs/conventions/ui-patterns.md` §Anchored settings popover, copying `LanguageSwitcher.tsx`'s shell (pointerdown-outside + Escape dismissal, `role=listbox`/`role=option` + ArrowUp/Down roving focus) — the new part is the trigger being an editable `<input role="combobox">` rather than a plain button, since nothing else in the app combines free text entry with picking from a list. `isTextEntryTarget` in `useKeyboardShortcuts.ts` already guards the global +/- shortcuts from firing while the input has focus, so no changes needed there. i18n `canvasToolbar.zoomLevel`/`zoomOptions` added to all 5 locales. `Canvas.test.tsx`'s zoom-percentage test and `App.viewportFit.test.tsx` updated from `getByText` on the old span to reading the new combobox's value; new `ZoomControl.test.tsx` (6 cases) covers typed-commit/clamp, blur-commit, Escape-revert-without-committing, preset selection, and empty/invalid-input discard. 969 tests total (up from 963, +6: `ZoomControl.test.tsx`). `tsc --noEmit` clean. Live-verified via Playwright + the debug hook against the app's own dev server: typed "123" + Enter set zoom to 123%; opening the dropdown and picking 400% set it to 400%; typing "999" then Escape correctly reverted to 400% (an earlier version of this fix had Escape's `blur()` call racing React's batched state update and re-committing the pre-revert text — caught by this live pass, not the unit tests, fixed with a `revertingRef` guard on `onBlur`); Zoom Out/In buttons and bare +/- keys both still worked identically to before. |
| M3 | Pin Editor | M2 | Done | Pin Path model + single default layer (M6 will add the panel), all 9 drawing tools wired to real mouse interaction (line 2-click, arc 3-click w/ live curvature, bounding-box drag for ellipse/circle/rect/square w/ Alt constraint, centre-drag for polygon/star/polygram family), Pin Eraser, Select-mode click-to-select + numeric geometry editing (recalculates pins), live pin-count/spacing status bar. All mutations undoable. 113 tests. Selection geometry editing uses numeric inputs, not canvas drag-handles (scope note). **Post-ship addition (2026-09-22, requested by the user):** unified Circle and Line's drag gestures with the Polygon/Star family. Circle's `geometryFromDrag` case switched from bounding-box (`normalizeBox`, corner-to-corner) to centre-radius (`radiusFrom(p0,p1)`, same as polygon/star) — the centre now stays pinned at the press point instead of moving as the box is dragged. Line switched from two independent clicks to one press-move-release drag: it gained a real `geometryFromDrag` case (mapping p0/p1 directly to start/end, returning `null` on a zero-length drag so a plain click creates nothing) and joined `DRAG_TOOLS`; its bespoke two-click branch in `usePinDrawing.ts`'s `handleMouseDown`/`previewGeometry` was removed, folding into the already-generic drag path. `Canvas.tsx` needed no changes — its dispatch was already generic over `DRAG_TOOLS`. Ellipse/Rectangle/Square (incl. their Alt constraint) remain the only bounding-box-drag group; this is now called out explicitly in a new "Drag Gesture Conventions" spec subsection (08-pin-tools-and-properties.md) alongside a note that Alt+Ellipse intentionally stays bounding-box even though the standalone Circle tool no longer does. Updated the Line help-panel description in all 5 locales (was describing the old 2-click gesture). 1018 tests total (net +4: Circle centre-radius + Line direct-mapping + Line zero-length-guard unit tests in `pinToolGeometry.test.ts`, Line zero-length regression test in `Canvas.pin-drawing.test.tsx`; the two renamed drag/commit tests for Circle and Line were rewritten in place). Live-verified via Playwright + the debug hook: real `page.mouse` drag on Circle showed the live preview staying centre-pinned throughout and committed the expected `center`/`radius`; real drag on Line committed a Line Pin Path from press to release with no second click, and a plain click (mousedown+mouseup at the same point) created nothing. |
| M4 | Symmetry | M3 | Done | Symmetry config on PinPath (none/horizontal/vertical/both/radial), mirrored/radial copies derived on every render (never stored, so source edits always stay in sync), dual-context (defaults vs selected-path, selected-path change is undoable), movable radial centre + interval via numeric fields, canvas overlay (axes/spokes+centre). 120 tests. Radial centre is numeric-input only, not canvas drag (same scope note as M3 selection editing). |
| M5 | Thread Editor | M3 | Done | ThreadPath/ThreadLayer model, full drawing workflow (click extends, double-click finishes+segment, right-click finishes without segment, Esc cancel-or-finish per confirmed-segment count), pin highlight states (normal/candidate/active-origin), 1/2/3-colour spiral rendering (same geometry, dash-offset strands), cascading pin-delete into threads as one undo step (interior removal correctly splits into surviving fragments, endpoint removal keeps one). 137 tests. Thread Eraser removes a whole Thread Path per click (segment-level splitting deferred — scope note); nearest-pin lookup is still an O(n) scan (spatial index deferred per 07-pin-geometry-engine.md §Performance, tracked as a follow-up not silently dropped). **Post-ship fix (2026-09-16, found by the user):** Thread insertion's nearest-pin detection (candidate highlight, click-extend, double-click-finish) could target pins on a hidden Pin Layer, and picked whichever pin was geometrically closest regardless of which Pin Layer was active. Fixed via new `nearestThreadInsertionPin` (`src/ui/canvas/hitTesting.ts`), wired into all three call sites in `useThreadDrawing.ts`: filters to visible Pin Layers only, then checks the active Pin Layer as a priority stage (wins outright if it has any pin in range, even a farther one) before falling back to the nearest pin among the other visible layers — mirrors the pin-over-grid snapping priority shape from 05-canvas-and-viewport.md. Scoped to Thread insertion only; `nearestPinOrMirrorOwner` (Edit-mode Select) and `nearestPinOwner` (Pin/Path Erasers) were left unchanged, not requested. Specs 12 and 13 updated (new "Pin Layer Scope for Thread Insertion" subsection + 3 Gherkin scenarios in spec 12). 5 new unit tests in `hitTesting.test.ts` (657 tests total); live-verified via Playwright + the debug hook: 3 pins at increasing distance from the cursor on 3 layers (active/other-visible/hidden) confirmed the active layer's farther pin wins the hover-candidate and a real click; moving the active layer's pin out of range confirmed fallback to the other visible layer, with the hidden layer's closer pin still excluded — no new console-error classes. |
| M6 | Layers | M3, M5 | Done | Pin/Thread Layers panels (tabs, create/rename/duplicate/delete/reorder/visibility/lock), generic layerOps shared by both kinds, active-layer targeting for new pins/threads, hidden-retains-contents + locked-blocks-edits-but-not-visibility guarantees, layer duplication regenerates pin IDs so existing threads keep pointing at the originals, deleting a layer cascades into thread segments in one undo step. 151 tests. "Select all objects on a layer" (multi-select) not implemented — scope note, would require extending Selection beyond single-object. |
| M7 | Undo/Redo Wiring | M2–M6 | Done | Ctrl/Cmd+Z / Ctrl/Cmd+Shift+Z global shortcut (skips text-entry targets so native input undo isn't hijacked), toolbar buttons reflect live history state. Audit of Undoable Operations list against M2–M6: all covered by Commands except "Add segment" during an in-progress thread draft, which is treated as part of the single "Add thread" commit-on-finish command rather than a per-click undo step (judgment call — undoing mid-draw individual clicks isn't a normal editing action; documented here rather than silently assumed). Full suite: 155 tests, this is the integration checkpoint for M1–M6. |
| M8 | Persistence | M7 | Done | Versioned project file schema (v1) with a migration seam (no-op today, ready for v2), New/Save/Open wired via browser download + file picker (File System Access API / autosave adapter deferred to Phase 2), loading resets undo history, full round-trip verified through real `JSON.stringify`/`parse` including pin IDs and thread pin references staying intact. 163 tests. |
| M9 | Printing & Export (MVP) | M8 | Done | Print preview (independently selectable elements, 1:1/fit/custom scale, paper size+orientation, print-vs-editor visibility independence verified), pin+thread statistics panels (project totals, per-path cards). Native export already satisfied by M8's Save. 179 tests. Print preview is on-screen only (no @media print stylesheet / physical-accuracy calibration — that's explicitly Phase 2). |
| M10 | Phase 2 | M9 | Done | All six Phase 2 items: (1) board appearance UI + custom-texture upload + 8 wood/4 paint presets (this also filled a real M2 gap — no appearance UI existed before); (2) thread twist pitch, configurable per-thread and as a drawing default; (3) autosave (debounced, localStorage, restore/discard banner); (4) print calibration (reference-line flow, correction factor applied to 1:1 scale); (5) print tiling (overlap, trim/alignment marks, page numbers/coordinates, multi-page preview); (6) SVG/PDF/PNG/JPEG export (SVG and PDF are true vector, PDF lazy-loaded via dynamic import since svg2pdf.js is ~470KB and browser-only). 223 tests total. Found and fixed a real pre-existing bug while building tiling: bounding-box code sampled only each path segment's start point, collapsing circles/arcs/ellipses to a single point — silently wrong since M2 (affected Fit-to-viewport too, just never exercised by a test until tiling's math caught it). Also discovered jsdom 25's Blob/File lack `.text()`/`.arrayBuffer()` and `URL.createObjectURL` doesn't exist at all — added a Node-Blob/File swap + a create/revokeObjectURL stub to test setup, which retroactively let M8's FileMenu Save/Open get real tests it never had. PDF export itself (`pdfExport.ts`) cannot be unit-tested here — svg2pdf.js's README states it does not work under jsdom (crashes at module-load); confirmed firsthand, not assumed. Raster export's actual canvas rasterization is similarly untestable in jsdom (no real 2D context backend) — only its pure DPI/mime-type math is covered. |
| M11 | Play Mode | M9 | Done | Fifth Editor mode (`"play"`) replaying Thread Path draw order one segment at a time; frame model reconstructed from current array order (no timestamp field exists — documented, accepted approximation, see 19-play-mode.md). Transport is local `EditorShell` state (`usePlaybackTransport`), not routed through `EditorStore`/undo — matches the existing `overlay` (Print/Stats) precedent, since Play mode never mutates the document. Reuses `BoardLayer`/`GridLayer`/`PinLayersView`/`ThreadLayersView` unchanged in a new dedicated `PlaybackCanvas` (deliberately not the interactive `Canvas`, which is built entirely around per-mode pointer dispatch Play mode has no use for). Video export (`captureStream`+`MediaRecorder`, `.webm` only — no cross-browser MP4 encoder exists) rasterizes each frame by serializing the LIVE `<svg>` DOM node, so the export matches the on-screen multi-colour/twist rendering exactly, rather than reusing the static exporter's simpler single-colour thread rendering. 349 tests total. Video-recording mechanics (`MediaRecorder`/`captureStream`/real canvas rasterization) cannot be unit-tested in jsdom — same class of limitation already accepted for PDF/raster export in M10; only the pure MIME-type-selection and bounding-box/pixel-sizing math are unit-tested, the real recording path verified live via Playwright instead. |
| M12 | In-App Help | M2–M11 | Done | Help button (between Stats and Print) opens a full-bleed overlay matching the Stats/Print convention exactly (no backdrop, no Escape-to-close), with 7 tabs (Edit, Pin, Thread, Pan, Play, Layers, Keyboard & Mouse) mirroring the Mode Switcher order plus 2 cross-cutting topics. Content is one data-driven `helpContent.ts` module rendered through a single generic section/item view, not 7 bespoke components. Default tab matches the current Editor mode on open, re-defaulting on every reopen since the panel only mounts while `overlay==="help"`. Keyboard/Mouse tab documents only what's actually implemented in code (confirmed via full pointer/keydown-handler audit) — omits spec-only, unimplemented gestures (wheel-zoom, shift-click multi-select, arrow-key nudging, Delete/Backspace) rather than describing them as available. 359 tests total (+9 over M11). Live-verified via Playwright: default-tab-per-mode, tab switching, Close, and re-default after a mode switch all confirmed. |
| M13 | Scale Tool & Pin Distance | M6, M7 | Done | Fifth Edit tool (Scale, grouped with Move/Rotation/Merge) — press-drag-release, horizontal-pixel-delta-driven factor, always pivots on the shape's own centroid (centre stays invariant, only size fields scale; Arc's curvature scales too since it's a physical sagitta length). Pin distance numeric field added to the Selection panel. Both feed into the existing per-shape `distributePins` pipeline unchanged, then reattach every Thread Path pin reference to its nearest new pin (`buildNearestPinRemap` over `allPinsWithMirrors`, generalizing Merge's remap-and-collapse via new `remapPinsInThreadPathByMap`) as one bundled `{pinLayers, threadLayers}` undo step (`commitPinPathWithReattach`, shared by `commitPinPathScale` and `setPinProperty`'s spacing branch). 384 tests total (+25 over M12). Live-verified via Playwright: radius scale up/down with centroid fixed, pin-count recompute, thread visibly reattached to the new nearest pin, single-step Undo, Esc-cancel leaves state untouched, locked-layer blocks the commit (re-verified on a clean reload after an initial debug-instrumented run was confounded by Vite/Chromium HTTP caching serving a stale module — not a real defect, noted here so a future session doesn't rediscover the same red herring). Scope note: this reattachment fix is intentionally limited to Scale and Pin distance, per explicit request — other numeric geometry-field edits (typed-in Position/Radius/etc.) still mint fresh pin ids with no thread reattachment, a pre-existing gap left as-is. |

| M14 | Thread Draft Pattern-Follow (Right Arrow) | M5 | Done | `ArrowRight` in Thread Draw mode: once the draft has 4+ vertices, extrapolates the next one from the two interleaved-by-position-parity arithmetic sequences the vertices form (`threadPattern.ts`'s `computeNextPatternPinId`), wrapping around whichever Pin Path the pattern's most recent member in that group belongs to. Wired as a non-undoable `EditorStore.advanceThreadDraftByPattern()` (same transient-draft treatment as `extendThreadDraft`/`retractThreadDraft`), keyed in `useThreadDrawing.ts` alongside `Escape`/`ArrowLeft`. 403 tests total (+19 over M13). Live-verified via Playwright: seeded pins 3/6/9/12 on a real 16-pin circle Pin Path, sent real `ArrowRight` keydowns through the mounted Canvas, confirmed the exact 15/2/5/8 wraparound sequence from the spec's worked example, confirmed `ArrowLeft` still composes correctly on top of pattern-added vertices, and confirmed the new Help panel copy renders. Scope note: only plain (non-mirrored) pins participate — a symmetry-mirrored pin id has no stable Pin-N position, so it makes `ArrowRight` a no-op rather than guessing, documented in 22-thread-follow-pattern.md's Scope limits. |

| M15 | Keyboard Move/Rotate/Scale | M7, M13 | Done | Arrow-key nudge (`useKeyboardTransform.ts`) driven by whichever of Move/Rotation/Scale is the active Edit tool — 1px/1°/1% per press, 10x with Shift, ignoring text-entry targets. Reuses `commitPinPathTransform`/`commitPinPathScale` unchanged (locked-layer blocking, undo, thread reattachment inherited for free); `geometryCenter` exported from `pinPath.ts` as the keyboard Rotate/Scale pivot (unlike the mouse Rotation tool's external press-point pivot); `isTextEntryTarget` extracted to `src/ui/keyboard.ts`, shared with the Undo/Redo shortcut. Big-step modifier was originally Alt/Cmd, switched to Shift after live use showed Alt/Cmd colliding with OS/browser shortcuts (e.g. macOS Option+Arrow word-navigation). Hold-to-repeat: native `event.repeat` auto-fire is ignored in favour of a homegrown `setTimeout`/`setInterval` loop at 500ms delay / 100ms interval — the WPF/UWP `RepeatButton` de facto standard (researched, not guessed), chosen because native OS key-repeat delay/rate is user-configurable and inconsistent across machines. 19 new tests (423 total, incl. fake-timer coverage of the delay/interval/keyup/mid-hold-lock/key-switch cases). Help content and README updated. Live-verified via Playwright + the debug hook: Move/Shift+Move nudge distances matched `screenDistanceToDocument` at the real fitted-viewport zoom (8.57, not the pre-fit default of 4) exactly; Rotate confirmed anticlockwise/clockwise signs and centroid invariance on a Rectangle; Scale confirmed 1%/10% factors, centroid invariance, pin-count recompute, and thread reattachment to the nearest new pin; single-step Undo confirmed for both Move and Scale; locked-layer blocking confirmed; confirmed a focused numeric input's own native arrow-stepping fires instead of the keyboard transform (width went 8→8.1 via the input's own step, not 8.08). Hold-to-repeat also live-verified with real timing (`page.keyboard.down`/`up` + real waits, not fake timers): holding ArrowRight 650ms produced exactly 2 commits (immediate + one repeat tick at ~600ms, `10 + 2×0.1167 = 10.233`), release stopped it immediately, and 2 separate Undo steps restored the original position. |
| M16 | Internationalization | M2–M15 | Done | `react-i18next`/`i18next` wired via a Provider-less module-singleton (bundled JSON resources, `initAsync: false`, no Suspense) imported once in `main.tsx` and once in `test/setup.ts` — verified all 441 pre-existing tests kept passing unmodified throughout every extraction batch, since `useTranslation()` falls back to the singleton when no `<I18nextProvider>` wraps the tree. English + full Portuguese (pt-BR) translations across 10 namespaces (`common`, `boardSetup`, `editorShell`, `menus`, `toolbars`, `panels`, `printPreview`, `canvas`, `help`, `errors`) covering every `src/ui/` component plus `src/App.tsx`'s autosave-restore banner (outside `src/ui/`, caught only by a live-verification pass, not the static string inventory) and the full ~240-line Help panel (`helpContent.ts` restructured to hold translation keys, not literal text). Custom-built `LanguageSwitcher` (Globe trigger + accessible listbox, own click-outside/Escape/arrow-key handling — a new dropdown pattern, `18-design-system.md` had none) mounted identically in Board Setup and the Editor Shell top bar, both reading the shared global `i18n.language`. First-visit auto-detect from `navigator.language` (`pt*` → pt-BR), explicit choice persisted to `localStorage` (`stringartit:language:v1`) and overriding detection thereafter. `projectFile.ts` error throws promoted to named classes (`InvalidProjectFileError`, `NoMigrationPathError`, `IncompatibleProjectVersionError` gaining a public `foundVersion` field) so `src/ui/toolbars/openFileErrors.ts` can translate the Open-file alert via `instanceof` discrimination instead of matching English `.message` text; domain-layer invariant-guard errors and raw export-failure `err.message` text stay untranslated by design (framework boundary / unscripted diagnostic detail, respectively). 453 tests total (+30 over M15): `detectLanguage.test.ts`, `languagePreference.test.ts`, `LanguageSwitcher.test.tsx`, `keyParity.test.ts` (walks every en/pt-BR namespace pair, asserts identical key sets — all 10 pass), `LanguageSwitcher.integration.test.tsx` (real `BoardSetup`/`EditorShell` mount, switches language, asserts real translated text renders in both). Live-verified via Playwright: language switch on both mount points updates text immediately; Help panel's 7 tabs fully render in Portuguese; localStorage persistence confirmed across reload in both directions; a live Open-file error alert round-tripped through Portuguese and back to English. Live verification caught one real bug the unit tests couldn't (they only assert `alertSpy` was called, never the message content): `openFileErrors.ts`'s `t()` calls omitted `{ns: "errors"}`, so under `FileMenu.tsx`'s `useTranslation(["menus","errors"])` (default ns "menus", the first array element) they silently resolved to the raw key string instead of translated text — fixed by making every call pass `{ns: "errors"}` explicitly rather than relying on the caller's hook default namespace. **Post-ship addition (2026-09-15):** three more languages added — Spanish (`es`), French (`fr`), German (`de`) — extending every mechanism built for pt-BR rather than introducing anything new: `SupportedLanguage`/`SUPPORTED_LANGUAGES`/`LANGUAGE_META` in `languages.ts` grew to 5 entries; 30 new namespace JSON files (`src/i18n/locales/{es,fr,de}/*.json`) mirroring the `en` key structure leaf-for-leaf, wired into `resources.ts` and `index.ts`'s `supportedLngs`; `detectLanguage.ts`'s prefix-matching chain extended (`es*`→es, `fr*`→fr, `de*`→de); `LanguageSwitcher.tsx` needed no code change, since it already iterated `SUPPORTED_LANGUAGES` generically. `keyParity.test.ts` generalized from a hardcoded en/pt-BR pair to loop over every non-en language in `SUPPORTED_LANGUAGES`; `detectLanguage.test.ts` and `languagePreference.test.ts` updated since both had used `fr` as their "unsupported value" example (swapped to `it`, which stays genuinely unsupported). 575 tests total (+44 over M19, the extra key-parity/detection cases). Live-verified via Playwright + the debug hook: Language Switcher lists all 5 options at both mount points; switching to Spanish, French, and German each updated Board Setup and Editor Shell top-bar text immediately; `localStorage` persistence confirmed across reload for French; Help panel rendered fully translated prose in German; a live Open-file error alert round-tripped through Spanish and back to English; no new console errors. |

| M17 | Radial Context Menu | M3, M4, M5, M6, M11, M13 | Done | Right-click on the canvas opens a cursor-anchored radial menu (`@spaceymonk/react-radial-menu`, zero-dep, React-19-safe — chosen over `react-circular-menu` (styled-components dep), `@jonbell/react-radial-menu` (excludes React 19), and a custom-built menu (user's explicit choice)), themed to design-system tokens via the library's internal CSS custom properties + a new `--shadow-float` token (its first floating-component use). Slices are mode/sub-state-aware (`radialMenuSlices.ts`, pure + unit-tested): Select/Move/Rotate/Scale/Merge (+conditional Commit Merge at 2+ accumulated pins) in Edit mode; the 9 basic Pin tools (Polygon/Star family deliberately excluded — toolbar-only, scope note); Draw/Eraser/Segment in Thread mode, entirely swapped for Cut/Back/Next while a draft is in progress; Fit/Zoom In/Zoom Out (anchored at the click point, not viewport centre) in Pan mode; the full playback transport in Play mode. Replaces the two previous instant right-click actions (Thread draft finish, Merge commit) with menu picks — a deliberate behaviour change, confirmed with the user first. Mounted once in `EditorShell.tsx` (not `Canvas.tsx`) since it must cover both the interactive Canvas and the separate `PlaybackCanvas` (Play mode). Escape/outside-click dismissal is a deliberate departure from the Print/Stats/Help `overlay` convention's "no Escape-to-close" rule, documented as such. 473 tests total (+20 over M16), including 2 pre-existing Canvas tests adapted since the behaviour they covered moved to EditorShell. Live-verified via Playwright + the debug hook: real right-click + real slice clicks confirmed the exact slice set and correct store mutation for all 5 modes, the Commit Merge conditional slice (absent below 2, appears at 2+, merges on click), the Thread draft swap (Next no-ops below 4 vertices, Back retracts, Cut commits as one undo step), Pan zoom anchored at the click point (asymmetric panOrigin shift confirmed, not the toolbar's centre-anchor), Play mode's Next Frame slice advancing playback, and Escape/outside-click both dismissing without side effects — no new console errors (one pre-existing, already-documented ModeSwitcher border/borderColor warning from M11 fired on ordinary mode switches, unrelated to this feature). Help panel's `rightClick` entry and README updated to describe the new radial-menu behaviour instead of the old instant right-click actions. |

| M18 | Edit-Mode Multi-Select & Granularity | M6, M13, M15, M17 | Done | Pin Path/Pins granularity switch (segmented pill, same visual family as `ModeSwitcher`) placed above the Edit Tools grid in `SelectToolbar.tsx`; flipping it clears the selection. `Selection` union replaced (`pinPath` → `pinPaths`/`pins`/`threadPath` variants, `EditorState.ts`) — single-selection consumers (`getSelectedPinPath`, `PinPropertiesPanel`, `SymmetryPanel`, keyboard transform) unaffected via a thin wrapper over the new `getSelectedPinPaths()`. New `useSelectTool.ts` implements click (replace) / Alt-Cmd-click (toggle) / rubber-band (replace) / Alt-Cmd-rubber-band (union) / Esc-cancels-drag, both granularities, via new `pinPathsTouchingRect`/`pinsTouchingRect` (`hitTesting.ts`). Move/Rotation/Scale generalized to N paths or N pins (`useMoveTool`/`useRotateTool`/`useScaleTool`, `useKeyboardTransform`, new `pinGroups.ts` for pins-mode per-path grouping) — path-mode Scale's pivot is the exact single-path `geometryCenter` formula unchanged for 1 path, the mean of every selected path's pins for 2+ (`multiSelect.ts`'s `selectionCentroid`); pins-mode pivot is always the mean of the selected pins (`pinsCentroid`); pins-mode Move/Rotation/Scale is a direct raw x/y pin transform with geometry/spacing left untouched (`commitPinsTransform`), same "custom/stale" precedent as the Pin Eraser. `scaleGeometry` generalized to `scaleGeometryAboutPivot` (an external-pivot bug in the naive generalization — circle/ellipse/polygon/star/polygram centers and rectangle/square position not moving under an external pivot — caught and fixed before it shipped, single-path behavior proven byte-identical via `pivot===geometryCenter`). Merge unified into an instant action (`commitSelectionMerge`/`canCommitSelectionMerge`, `EditorStore.ts`) fired against the current selection instead of its own accumulate-click gesture — `useMergeTool.ts`/`MergeSelectionOverlay.tsx` deleted, replaced by `SelectedPinsOverlay.tsx` (pins-mode highlight) and `SelectionMarqueeOverlay.tsx` (live rubber-band rect); toolbar Merge is now a full-width action button, radial menu's `commitMerge` slice removed and `merge` itself gated by `canCommitSelectionMerge`. Path-mode Merge (`combinePinPaths`, `multiSelect.ts`) combines every selected path's pins into the first-selected path's identity, collapses pins within a floating-point-safety tolerance (1e-6, not a visual snap distance), then remints **every** surviving pin a fresh id and reattaches every original pin (colliding or not) to its nearest survivor via the same nearest-pin mechanism Scale already uses — confirmed user decision, not the minimal "only colliding pins renumber" option. Pins-mode Merge keeps the existing per-pin averaging rule but resolves its destination path via `resolveMergeDestinationPath` (most selected pins wins, then closest own-pins centroid, then lowest numeric pin-path id — confirmed via a `pinpath-10` vs `pinpath-9` test proving it's a numeric, not lexicographic, comparison). All new plural commit methods (`commitPinPathsTransform`, `commitPinPathsScale`, `commitPinsTransform`, `commitSelectionMerge`) are all-or-nothing on any locked layer and commit as one bundled `SetValueCommand`. 525 tests total (+52 over M17), incl. new `multiSelect.test.ts`, `hitTesting.test.ts`, `EditorStore.multiSelect.test.ts`, `EditorStore.merge.test.ts`, `SelectToolbar.test.tsx`, and every pre-existing single-selection test (M3/M13/M15/M17) updated to the new `Selection` shape and passing unmodified in substance. Live-verified via Playwright + the debug hook: real rubber-band drag selected both of two circles; real Alt-click toggled a path in/out of the selection; a real Move drag on 2 selected paths committed and reverted as one Undo; a real Scale drag on 2 selected paths recomputed both paths' pins about the exact predicted combined centroid, reattached a connecting thread to fresh nearest pins, and reverted both paths + the thread in one Undo; locked-layer block confirmed (zero mutation); toolbar Merge combined 2 circles (31+31→62 pins) and reverted in one Undo; granularity switch clearing the selection confirmed; a real click + real Alt-click in Pins granularity built a 2-pin selection (`SelectedPinsOverlay` rendered both), and the Merge button correctly combined them into 1 pin at the destination path resolved by the most-selected-pins rule; a real right-click + real click on the radial menu's Merge slice combined a third path into the destination. No new console-error classes (the pre-existing `ModeSwitcher` `border`/`borderColor` shorthand-conflict warning from M11/M17 also fires on the new granularity switch tabs, since they copy the exact same conditional-style pattern — documented, not fixed, consistent with the existing precedent). Scope note: multi-selection has no Delete action (SelectionPanel shows a summary only for 2+ paths or any pins) — not requested, a known gap per `26-edit-mode-multi-select.md`. **Post-ship fix (found by the user):** path-mode Merge originally left the destination's `geometry` as whichever single shape type the first-selected path was, while `pins[]` held the pooled pins from every merged shape; since Scale/Pin-distance always regenerate `pins[]` strictly from `geometry`, merging e.g. a circle with a line and then scaling silently deleted every pin from whichever shape didn't match the retained geometry type — reproduced live against the user's saved project file (a circle+line merge collapsed 97 pins to 61, losing the entire second shape). Fixed by giving the merge's destination path a Freehand geometry threading through every combined pin instead of keeping one shape's original geometry (`commitPathModeMerge`, `EditorStore.ts`); spec 26 and its Merge test both updated; new regression test (`EditorStore.merge.test.ts`) reproduces the circle+line+Scale scenario; re-verified live against the exact reported repro (loaded the user's `.tmp/string-art-project.json`, merged the circle with the line, scaled — all 97 pins now survive and scale correctly, confirmed by Undo restoring the exact pre-scale and pre-merge states). **Post-ship fix (2026-09-16, found by the user, same request as M5's row):** Edit-mode Select (click and rubber-band, both Pin Path and Pins granularity) could target/select pins and paths on a hidden Pin Layer, and had no active-layer preference at all — whichever pin/path was geometrically nearest (click) or simply touched (rubber-band) won, regardless of which Pin Layer was active. Since Move/Rotation/Scale/Merge only ever act on `state.selection`, fixing Select's targeting was sufficient to scope all four transitively — no changes needed in `useMoveTool`/`useRotateTool`/`useScaleTool`/`commitSelectionMerge` themselves. Fixed via 4 new `hitTesting.ts` functions built on a shared `visibleLayersPrioritized` helper (also used to refactor M5's `nearestThreadInsertionPin` onto the same primitive): `nearestSelectPathHit`/`nearestSelectPinHit` (click, either granularity) and `selectablePinPathsTouchingRect`/`selectablePinsTouchingRect` (rubber-band, either granularity) — each filters to visible Pin Layers, then tries the active layer alone first (a match there wins outright — nearer pin or bigger touched set on another visible layer notwithstanding) before falling back to the other visible layers. Wired into all 4 call sites in `useSelectTool.ts`, replacing the unscoped `nearestPinOrMirrorOwner`/`nearestPinOwner`/`pinPathsTouchingRect`/`pinsTouchingRect` calls there (those 4 primitives are kept as-is and still exported/tested unscoped, since `nearestPinOwner` is also shared by the unrelated Pin/Path Erasers in Pin mode — explicitly out of scope for this request). Spec 26 gained a new "Pin Layer Scope for Selection" section + 5 Gherkin scenarios. 11 new unit tests in `hitTesting.test.ts` (668 tests total). Live-verified via Playwright + the debug hook: 3 pins on 3 layers (active/other-visible/hidden) at increasing distance from a click point confirmed the active layer's farther pin/path wins a real click in Pin Path granularity; a real rubber-band drag touching both the active and another visible layer's path selected only the active layer's; moving the active layer's pin out of the rect confirmed fallback to the other visible layer while the hidden layer's path (also inside the rect) stayed excluded throughout — no new console-error classes. **Post-ship addition (2026-09-16, requested by the user):** a read-only Stats box (Pins/Actual gap/Path perimeter) added to `SelectionPanel.tsx`, shown whenever one or more Pin Paths are selected in Edit mode (absent for a Pins-granularity or empty selection). Single-path selection shows all 3 figures; a 2+ path selection sums Pins and Path perimeter across the selection but omits Actual gap (ambiguous across differently-spaced paths — confirmed with the user rather than guessed, e.g. via an average). `PinPathStatistics` (`src/application/document/statistics.ts`) gained a new `perimeterCm` field — `geometryToContourPaths(path.geometry)` summed through `pathLength` per contour, so multi-contour Text Pin Paths sum correctly — reused by both the single- and multi-path box (no duplicate computation). The pre-existing `multiPathsSelected` summary sentence dropped its "(N pins total)" parenthetical since Pins now lives in the box instead. i18n keys added to all 5 locales (`selectionPanel.stats.*`), `keyParity.test.ts` green. 4 new tests (674 total): `statistics.test.ts` (perimeter on a closed circle built to an exact known perimeter, and on a hand-built 2-contour Text fixture), `SelectionPanel.test.tsx` (single-selection values, multi-selection sum + Actual-gap omission, absent when nothing selected). Live-verified via Playwright + the debug hook: a real 7cm-radius circle selection showed Pins 44 / Actual gap 1.00 cm / Path perimeter 43.98 cm (2π·7, exact); adding and selecting a second 3cm-radius circle showed the multi box (Pins 63, Path perimeter 62.83 cm = 2π·7 + 2π·3, exact) with no Actual gap row; deselecting hid the box entirely — no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor warning and an unrelated favicon 404). |
| M19 | Thread Select Tool | M5, M12 | Done | 4th Thread tool, `select` (`ThreadToolbar.tsx`, `EditorState.ts`'s `ThreadTool` union), reusing the existing `nearestThreadPath` hit-test already used by the Thread/Segment Erasers — no new hit-testing needed. New `{type:"threadPath",layerId,pathId}` `Selection` variant; `EditorStore.getSelectedThreadPath()`/`setThreadProperty(layerId,pathId,{colours?,width?})` (locked-Thread-Layer guarded, one `SetValueCommand<ThreadLayer[]>` per edit) — deliberately separate from `setThreadDefaults` (which only ever edits what the *next* drawn thread uses, confirmed untouched by a live check). New `ThreadPropertiesPanel.tsx` (mirrors `SelectionPanel`'s empty-state pattern, reuses `ThreadToolbar`'s exact colour-swatch/width markup) mounted in `EditorShell.tsx` under `ThreadToolbar` when a Thread Path is selected — Thread mode previously had no side panel at all. `ThreadPathVisual`/`ThreadLayersView` gained a `selected` prop rendering an additive `--accent` halo stroke underneath the existing multi-colour spiral rendering (same approach `PinPathVisual` already uses for its selected guide line), not a replacement of it. Selecting a Thread Path persists across switching to another Thread tool (Draw/Eraser/Segment), matching existing cross-mode selection-persistence precedent. 525 tests total (shared with M18's count — both milestones landed in the same session): `EditorStore.threadSelect.test.ts`, new assertions in `Canvas.thread-drawing.test.tsx` and `ThreadPropertiesPanel.test.tsx`. Live-verified via Playwright + the debug hook: a real click on a drawn thread's segment selected it and rendered the `ThreadPropertiesPanel` with its live colours/width; editing the panel's Width field updated the selected Thread Path's `width` (confirmed the drawing-defaults `threadDefaults.width` stayed untouched) and reverted on Undo; locking the Thread Layer correctly blocked the same edit. **Post-ship fix (found by the user):** the original design put colour/width/twist-pitch fields in BOTH `ThreadToolbar` (drawing defaults) and `ThreadPropertiesPanel` (selected thread) — shown together whenever a thread was selected, a confusing duplicate-fields UI, and the selected-thread copy never exposed the colour-count (1/2/3) buttons at all, so an existing thread's colour count could never be changed. Fixed by making `ThreadPropertiesPanel` dual-context exactly like `PinPropertiesPanel` (always rendered, title switches between "(selected)"/"(defaults)", one set of fields for both), stripping `ThreadToolbar` down to just its tool buttons + legend, and generalizing `EditorStore.setThreadProperty` from a 3-arg `(layerId,pathId,patch)` selected-thread-only method into a 1-arg `(patch)` dual-context method (branches on `state.selection`, falls through to the existing `setThreadDefaults` when nothing's selected) — same shape as `setPinProperty`. Spec 27 updated to describe the unified panel; 3 new tests (531 total) including a regression test for the colour-count bug; live-verified via Playwright (defaults context, selected context, 1→3 colour-count change on a selected thread, twist-pitch field appearing) — no duplicate fields, no new console errors. **Post-ship addition (2026-09-16, requested by the user):** a read-only Stats box (Segments/Thread length/Pins visited, per docs/specs/17-statistics.md) added to `ThreadPropertiesPanel.tsx` right after the twist-pitch field, shown only in selected-thread context (absent in defaults context) — reuses `threadPathStatistics(thread, pinLayers)` unchanged (same fn `StatisticsPanel.tsx`'s per-thread `StatCard` calls), no new store methods or props, row styling mirrors `StatCard`'s label/value rows. i18n keys added to all 5 locales (`threadPropertiesPanel.stats.*`), `keyParity.test.ts` green. 2 new tests in `ThreadPropertiesPanel.test.tsx` (values match a computed fixture; box absent with nothing selected). Live-verified via Playwright + the debug hook: real selection of a drawn thread showed the box with correct live values; deselecting hid it; erasing an endpoint pin (which forks the thread's `pathId`, a pre-existing, out-of-scope selection-staleness behavior) correctly fell back to the defaults context; reselecting the resulting shorter thread showed updated Segments/length/pins-visited without remounting — no new console errors (only the pre-existing documented `ModeSwitcher` border/borderColor warning). |

| M20 | Grid Settings Dialog | M2 | Done | New `GridSettingsPopover.tsx` (anchored popover, `docs/conventions/ui-patterns.md` §Anchored settings popover — 3rd consumer of that shape alongside `ExportMenu`/`LanguageSwitcher`) holds Gap X/Gap Y/Grid colour/Grid opacity, opened by a "Grid settings" button next to Snap in `CanvasToolbar.tsx`; the 4 fields were removed from the toolbar's main row (Grid/Snap toggle chips stayed put). Reuses `EditorStore.setGrid()` unchanged (still transient/non-undoable, no change to that). Adopted `--shadow-float` for the popover's elevation instead of repeating `ExportMenu`/`LanguageSwitcher`'s hardcoded box-shadow — those two are left as-is (out of scope) but `18-design-system.md` now documents both the token's second consumer and a new "Anchored settings popover" component-vocabulary entry, correcting a stale claim that the Radial Context Menu was the app's only floating component. Spec 05 gained the previously-undocumented Grid colour/opacity settings plus a new "Grid Settings UI"/"Grid settings popover" section and Gherkin scenarios. 581 tests total (net +6 new): `GridSettingsPopover.test.tsx` (open/close, pre-filled values, field edits, Escape/outside-click dismissal, re-click-to-close) plus `Canvas.test.tsx`'s two inline-field tests replaced by one direct-`setGrid` dot-rendering check (popover field editing no longer needs `Canvas` mounted at all). i18n `canvasToolbar.gridSettings` key added across all 5 locales, `keyParity.test.ts` green. Live-verified via Playwright + the debug hook: real click opened the popover pre-filled with live grid state; real field edits (Gap X, Grid colour) updated the store and the rendered grid dots immediately; Escape and an outside click on the canvas both closed it; toolbar's main row confirmed to no longer show the 4 inline fields; no new console-error classes (only the pre-existing documented `ModeSwitcher`/toggle-chip `border`/`borderColor` shorthand warning and an unrelated favicon 404). |

| M21 | Help: About Tab | M12 | Done | 8th/last Help tab (`src/ui/panels/help/AboutTabContent.tsx`), wired via a new optional `HelpTab.Content` field on the existing data-driven `helpContent.ts` (the other 7 tabs keep using `sections`, now optional; `HelpPanel.tsx` renders `tab.Content` when present, else falls back to the generic `HelpSectionView` list). Read-only app-info property rows (Name/Description/live `package.json` version/Author); contact form (Name, Subject restricted to Question/Support/Issue/Other, Message) whose Send is an `<a href="mailto:lfsvendrame@gmail.com?subject=StringArtIt - <Token>&body=...">` — a genuinely new "send" pattern for this backend-less app, documented in `docs/conventions/ui-patterns.md` §Mailto contact link (kept a real `href="#"` + `aria-disabled` when inert, rather than omitting `href`, so the element keeps its accessible `link` role either way). The mailto subject token stays canonical English regardless of UI locale — only the dropdown's visible option labels translate — a deliberate scope note in spec 28. New `Textarea` entry added to `18-design-system.md`'s Component Vocabulary (first use in the app). i18n: `about.*` + `tabs.about` added to all 5 `help.json` locales with real translations, `keyParity.test.ts` green. 586 tests total (+5 over M20: `AboutTabContent.test.tsx`'s 4 new tests plus 1 new `HelpPanel.test.tsx` case, with the existing seven-tabs test widened to eight): app-info fields, exactly-4-subject-options, Send inert with empty fields, and the built mailto href/subject/body all covered. Live-verified via Playwright + the debug hook: real click through Help→About rendered app info with the live `package.json` version; a real form fill produced the exact `mailto:lfsvendrame@gmail.com?subject=StringArtIt - Support&body=Name: Ana\n\nHelp please` href; Send stayed inert (`aria-disabled`, `href="#"`) with empty fields; Close still dismissed the overlay; no new console-error classes (only the pre-existing, already-documented favicon 404). |

| M22 | Text Pin Path | M3, M13 | Done | New Text Pin tool (`PinToolbar.tsx`, next to Freehand) — click-once-then-type instead of click-drag, wired in `Canvas.tsx` as a single-click branch (same shape as `eraser`/`path-eraser`) that awaits the default font then calls the existing generic `addPinPath`, which already hands off to Edit mode with the new path selected — no new sync code needed for that flow. First genuinely multi-contour `PinPathGeometry` variant (`contours: Point[][]`), added because a letter's hole ("o", "e") or disconnected piece ("i"'s dot) needs more than one closed loop inside one Pin Path — new domain primitive `closedPolylineShape` (mirrors `freehandShape` but closed) plus `commandsToContours` (pure Bezier-to-polyline flattening, framework-free, hand-built-command unit tests, no font file needed) and `geometryToContourPaths` (generalizes `geometryToPath` to N contours; every other shape is just `[geometryToPath(g)]`, byte-identical). `distributePins` gained a `"text"` branch running the closed-path algorithm independently per contour (documented as a new subsection in `07-pin-geometry-engine.md`) and concatenating pins, same `pins[]` shape as every other Pin Path. Move/Rotate/Scale need zero Text-specific tool code — `contours`+`origin` get the exact same treatment Freehand's `points[]` already gets in `translateGeometry`/`rotateGeometry`/`scaleGeometryAboutPivot`/`geometryCenter`; Merge needs no changes at all (operates on `pins[]`, agnostic of geometry type). Scope note (matches the pre-existing Pin-Eraser-vs-geometry-edit precedent): editing any of the six text fields after a manual Rotate/Scale regenerates contours from scratch at the field's literal values, discarding the transform — confirmed live, not just asserted. Font loading (`opentype.js`, `src/infrastructure/fonts/`) is the one place async touches this feature: `ensureFontLoaded` fetches+parses+memoizes by URL, so only a Font/Weight/Italic *switch* to a not-yet-loaded combination pays a network cost — plain typing never awaits a real network round-trip, keeping `EditorStore`/`pinPath.ts` fully synchronous. 20 bundled Google Fonts (SIL OFL 1.1, `public/fonts/` — new folder, sourced live via `raw.githubusercontent.com/google/fonts`, real static per-weight files hand-verified to exist rather than guessed, since most modern Google Fonts families are now variable-only) spanning sans-serif (Lato, Poppins, PT Sans, Barlow, Hind, Titillium Web), serif (PT Serif, Crimson Text, Arvo, Cardo, Tinos), cursive (Pacifico, Sacramento, Great Vibes, Kaushan Script, Alex Brush — Regular only, no bold/italic files exist for these), and monospace (Courier Prime, Space Mono, IBM Plex Mono, Anonymous Pro); no `@types/opentype.js` dependency (targets the stale 1.x API against this app's 2.0.0) — a small local ambient `.d.ts` declares just the 4 functions/fields actually used. 633 tests total (+47 over M21): `polyline.test.ts`, `glyphOutline.test.ts` (domain), `fontCatalog.test.ts`/`fontLoader.test.ts` (mocked fetch, module-reset-per-test cache isolation)/`textContours.test.ts` (integration against the real bundled pt-sans file — this is what caught and fixed a real sign bug: opentype.js's `Glyph.getPath` already negates the font's Y-up coordinates, so an initially-added extra flip in `textContours.ts` was double-flipping and rendering upside-down; removed, confirmed correct by both a unit test and the live screenshot below) (infrastructure), new `describe` blocks in `pinPath.test.ts` for multi-contour distribution and the four generic transforms against a hand-built "hole letter" fixture (application), `PinToolbar.test.tsx` (new file), `Canvas.text-drawing.test.tsx`, `PinPathVisual.test.tsx` (multi-contour `d` attribute), and 5 new cases in `SelectionPanel.test.tsx` (UI). i18n: `pinToolbar.basicTools.text`, `selectionPanel.fields.{font,weight,italic,size,letterSpacing,text}`, `selectionPanel.shapeTypes.text`, `selectionPanel.weightOptions.{regular,bold}` across all 5 locales, `keyParity.test.ts` green. Live-verified via Playwright + the debug hook: a real click placed an empty Text Pin Path and switched to Edit mode with it selected (zero mock); real typing of "on" into the Text field produced 3 contours (o's ring+hole, n's single loop) and 26 pins with no new console-error classes; a screenshot at a computed fit-to-selection viewport visually confirmed correct, right-side-up multi-contour rendering (the "o"'s hole and both pin rings clearly visible); a real Move drag shifted `origin` and every contour point together; a programmatic Scale (per this codebase's live-verification convention for drag-math-independent checks) roughly doubled the pin count and rescaled about the shape's own centroid; Undo/Redo confirmed per-keystroke (matches the existing, pre-M22 SelectionPanel/PinPropertiesPanel precedent — no debouncing exists anywhere in this codebase to copy, so none was invented here); a `toProjectFile`/`loadProject` round-trip preserved contours/pins exactly with no re-fetch. Scope notes: Weight+Italic never combine into a single file (Italic always resolves to the plain italic file regardless of the Weight dropdown, since no Bold+Italic files were sourced); script/cursive fonts offer Regular only (no Weight/Italic options); `size`/`letterSpacing` field edits are per-keystroke undo steps like every other SelectionPanel field, not batched. **Post-ship refinement:** moved Text to the first field in the group (was last) and added autofocus — a `useEffect` in `SelectionPanel.tsx` keyed on `state.selection` (not on every geometry edit, since typing goes through `updatePinPathGeometry` and never touches `selection`) focuses the Text input whenever a Text Pin Path becomes selected, covering both the tool's placement click and re-selecting an existing Text Pin Path later; 636 tests total (+3: field-order, focus-on-select, and no-refocus-on-keystroke), live-verified via Playwright (real click → Text field shown first and focused → typed directly with no intervening click, confirmed via the accessibility tree's `[active]` marker and `document.activeElement`). |
| M23 | SEO — Technical Tags & Landing Content | M2, M16 | Done | `index.html` head rewrite (description/canonical/OG/Twitter/theme-color/favicon/manifest/JSON-LD `SoftwareApplication`); `public/robots.txt`, `sitemap.xml`, `manifest.webmanifest`, `favicon.svg`, `apple-touch-icon.png`, `og-image.png`; `document.documentElement.lang` now synced to the active i18next locale. 4 new inline landing sections (Hero/Features/How-it-works/FAQ, `src/ui/panels/landing/`) wired around `BoardSetup` in `App.tsx`'s `!entered` branch — `BoardSetup`'s own heading demoted `<h1>`→`<h2>` so the page has exactly one `<h1>`. New `landing` i18n namespace, real copy authored for all 5 locales (`en`/`pt-BR`/`es`/`fr`/`de`) — the initial ship had shipped English placeholders for the 4 non-English locales, flagged as a known gap and fixed the same day once the user called it out. 686 tests total. Live-verified via Playwright: hero/BoardSetup/features/how-it-works/FAQ all render correctly; full BoardSetup flow (shape/dimension/appearance → Continue to Editor → EditorShell) unchanged; language switch (`en`→`es`→`en`) updated both `BoardSetup`'s translated strings and `document.documentElement.lang`; no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning and an unrelated favicon.ico 404); built `dist/index.html` inspected directly and confirmed to carry every new tag verbatim. |

| M24 | WebMCP Agent Tools | M8, M9, M13 | Done | 18 tools registered via `navigator.modelContext.registerTool` (`src/infrastructure/webmcp/tools.ts`), feature-detected, wired from `App.tsx` alongside (but kept separate-and-documented from) the unstable debug hook: `get_document_stats`; board `set_board_shape`/`set_board_dimensions`/`set_board_appearance` (solid/wood/painted only — gradients/custom-texture stay UI-only); pin-layer `add_pin_layer`/`set_active_pin_layer`/`toggle_pin_layer_visible`/`toggle_pin_layer_locked`; `add_pin_path` (10 of 11 `PinPathGeometry` variants — `text` excluded, needs UI-only font infra) / `delete_pin_path`; the same 4 for thread layers; `add_thread_path` (composes `cancelThreadDraft`/`extendThreadDraft`×N/`finishThreadDraft` — no new store method) / `delete_thread_path`; `undo`/`redo`. New `projectThreadTotals` in `statistics.ts` (project-level thread aggregate, mirrors `projectTotalPins`). 696 tests total (+10: `tools.test.ts` covers every Gherkin scenario against a mocked `navigator.modelContext`, `statistics.test.ts` +2). Live-verified via Playwright: real Chrome (no `navigator.modelContext`, true of every shipping browser as of this spec) confirmed the no-op path — `'modelContext' in navigator` is `false`, zero new console errors, board setup/debug-hook interaction unaffected. Accepted gap, documented in the spec itself: the real Chrome-146-Canary dispatch path (an actual agent invoking a tool through the browser's own implementation) isn't live-verifiable anywhere yet — same class of limitation this codebase already accepts for PDF/MediaRecorder mechanics (M10/M11) — so registration and every execute handler's correctness rests entirely on the unit tests instead. |

| M25 | Generator Mode | M6, M7 | Done | 6th Editor mode ("Generate", `ModeSwitcher`/`GeneratorToolbar`/`GeneratorPanel`/`GeneratorPreviewOverlay`). Researched a competitor app's 19 built-in patterns first (`~/projects/pocs/research_string_art`, not in this repo) — all 19 are pure parametric math (pins from circle/line/polygon primitives, 17/19 single-or-composite regular shapes, 2/19 sampled along a closed-form curve), so freehand geometry is used only for the curve-sampled case, never as a "couldn't figure out the shape" fallback. Shipped a curated 5-pattern set exercising every engine capability — Mandala (single circle, modular multiplication `i·base mod n`), Star (circle+star composite), Freestyle (up to 3 independent circles, round-robin), Star of David (7 nested-polygon tiles — see post-ship fix note below), Spirals (polar-arm curve sampled directly into a `"freehand"` geometry, bypassing `distributePins`' arc-length resampling since the formula-computed points ARE the desired pin positions) — the other 14 researched patterns are the same registry shape and are an explicit follow-up, not built here. New pure engine: `src/domain/generator/` (`roundRobin.ts`, `mandala.ts`, `spiralArms.ts`) + `src/application/document/generator/generatorPatterns.ts` (registry + `buildGeneratorPattern` dispatch), plus a new domain primitive `spacingForPinCount` (`src/domain/paths/distribution.ts`) bridging this app's spacing-driven pin distribution to the pattern engine's count-driven params, floating-point-safety-tested to land on the exact requested count (not just close). Generate/Re-generate build a transient, non-undoable `generatorDraft` (same treatment as an in-progress Thread Path draft); Confirm is the only undoable step — always creates 2 brand-new permanent layers (never merges into an existing one, so no locked-layer check applies) in one bundled `SetValueCommand<{pinLayers,threadLayers}>`, single Undo removes both. Leaving Generator mode (or loading a project) discards an uncommitted draft. 738 tests total (+15 over M24: `distribution.test.ts`, `roundRobin.test.ts`, `mandala.test.ts`, `spiralArms.test.ts`, `generatorPatterns.test.ts`, `EditorStore.generator.test.ts`, `GeneratorPanel.test.tsx`, plus updated `ModeSwitcher.test.tsx`/`radialMenuSlices.test.ts` for the new mode). **Caught by the real typecheck, not the live-verification pass:** `npx tsc --noEmit -p .` was silently checking zero files this whole session (root `tsconfig.json` uses project references with `files: []`, which needs `tsc -b`/build mode to actually traverse them) — switching to `tsc -b --force` surfaced two real exhaustiveness gaps the new `"generate"` EditorMode had opened: `radialMenuSlices.ts`'s mode switch (no `default`, would return `undefined` and crash `RadialContextMenu`'s `.map` on a Generator-mode right-click) and `helpContent.ts`'s `MODE_TO_HELP_TAB: Record<EditorMode, HelpTabId>` (missing key). Both fixed (empty slice set for Generate; Help defaults to the Edit tab, since Generator mode has no dedicated Help content yet) and regression-tested. i18n: `generatorToolbar`/`generatorPanel`/`modes.generate` keys added to all 5 locales, `keyParity.test.ts` green. `18-design-system.md`'s stale "four editor modes, not reused past ~4 options" ModeSwitcher note corrected to six (it had already fallen behind Play mode without being updated). Live-verified via Playwright + the debug hook: real clicks generated and visually confirmed all 5 patterns (screenshotted — a genuine cardioid/spirograph Mandala, two visibly overlapping triangles for Star of David, 3 real pinwheel arms for Spirals), Re-generate fully replaced the prior preview each time, Confirm created exactly 2 new real layers (visible in the Layers panel, named e.g. "Generated — Spirals") and switched them active, single Undo removed both, switching to Edit mode mid-draft discarded it with zero orphaned layers and re-entering Generate showed no stale draft — no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning, now firing once per mode switch as expected). **Post-ship fix (2026-09-17, found by the user): Star of David rebuilt.** The initial "two flat overlapping triangles, round-robin threaded" implementation was structurally wrong, not just visually plainer — flagged by the user after comparing it against a live reference render of the same researched competitor app. The real construction is 7 independently-threaded nested-polygon tiles (1 central hexagon + 6 triangles centred on its edges), each spiralling inward through `depth` shrinking/twisting copies of itself — that inward spiral is what actually produces the dense swirl look, not a flat outline. Rebuilt from first principles as new pure domain primitives (`src/domain/generator/nestedPolygon.ts`: `nestedPolygonLevels`/`nestedPolygonVertices`/`connectTwoSidesLocalIndices`, unit-tested against a worked traversal example and exact segment-count formula) and cross-checked numerically against the reference render's actual nail coordinates (radii/angles extracted from a user-downloaded SVG for calibration only — no source code or artwork copied into this codebase, file deleted immediately after the numeric check): the outer tip radius exactly equals the board's inscribed radius `R0`, the central hexagon's own vertex radius is exactly `R0/√3`, and each triangle tile sits exactly 30° off the nearest hexagon vertex — all three matched this rebuild's formulas before anything was adjusted to fit. `GeneratorParams`'s star-of-david variant changed from `{nailsPerSide, rotation}` to `{depth, layerAngle, rotation, mirrorTiling}`; `GeneratorPanel` fields and all 5 locales' i18n updated to match. 750 tests total (+12: `nestedPolygon.test.ts`'s 9 cases, 3 rewritten `generatorPatterns.test.ts` star-of-david cases). Live-verified via Playwright + the debug hook against the app's own dev server: real Generate produced 7 Pin Paths (60/30/30/30/30/30/30 pins, matching `6·depth`/`3·depth`) and 24 Thread Paths (`6+6·3`), screenshotted and visually confirmed a genuine 6-pointed star silhouette with nested inward-spiralling fans in each point and in the centre — no new console-error classes. **Post-ship addition (2026-09-17, requested by the user): multicolor palette + live auto-apply.** (1) `GeneratorPanel` gained an editable colour palette (`[swatch][swatch]... [+][-]`) capped per-pattern by `maxGeneratorColours(params)` — the number of independent Thread Path runs a pattern's *current* params will produce (Mandala: its own `layers`; Star of David: fixed `24`; Star/Freestyle/Spirals: `1`, since each threads as one continuous run and a 2nd colour would never attach to anything). Run `i` gets `colours[i % paletteLength]` as its own single colour (never the whole palette handed to one run, which would render as a multi-strand twist instead — a different, existing feature). Fixed a real bug found while wiring this up: `buildStarOfDavid` was already passing the *entire* palette array into every one of its 24 runs instead of cycling one colour per run — never exercised before because Star of David's palette was always length 1 until this feature added a UI path to grow it. Palette state lives in `GeneratorPanel` (not `state.threadDefaults`, so it never leaks into Thread mode's hand-drawn-thread colour) and is passed explicitly into `generatePattern(params, colours)`. (2) Removed the "Re-generate" button per explicit user request ("apply the changes as soon as any pattern property changed") — after the first (still explicit) `Generate` click, every later pattern/param/colour edit auto-regenerates, debounced ~300ms via a `useEffect` keyed on `[hasDraft, params, visibleColours, store]`; a `justGeneratedRef` guard suppresses the one redundant immediate re-run the effect would otherwise fire right after the manual Generate click. Caught and fixed one real bug before it shipped: the palette array passed to that effect must be `useMemo`'d (not sliced fresh every render) — an unmemoized array reference changes on every render, including the one caused by the debounced `generatePattern` call's own store update, which would have re-triggered the effect forever (an infinite auto-regenerate loop with no user input). 763 tests total (+13 over the previous row: 3 new `maxGeneratorColours` cases, 2 colour-cycling cases including the Star of David fix, 1 multicolor-UI describe block in `GeneratorPanel.test.tsx`, 3 new fake-timer live-auto-apply cases, plus 4 existing tests updated for the removed Re-generate button). Live-verified via Playwright + the debug hook: real `+`/`−` clicks grew/shrank the palette up to and disabled exactly at the cap ("Up to 4 for this pattern" shown for a 4-layer Mandala), 4 hand-picked swatch colours produced 4 distinctly-coloured Thread Paths matching exactly (screenshotted: 4 visibly distinct concentric colour bands), and the Generate button correctly disappeared after first use with edits applying live — no new console-error classes (only the pre-existing documented `ModeSwitcher` warning). **Post-ship fix (2026-09-17, found by the user): Star rebuilt as a nested star.** The original implementation was a single flat star (`"star"` `PinPathGeometry`, no nesting) round-robin-woven with an outer circle — every point of the star looked identical, since nothing in the construction varied per point. Flagged by the user comparing it against a live reference render, same as the Star of David report. Rebuilt using the same nested-polygon technique already proven correct for Star of David: new `src/domain/generator/nestedStar.ts` (`nestedStarLevels`/`nestedStarVertices`) generalizes that tile construction to a star's alternating outer/inner vertices — `depth` shrinking/twisting copies of the star, reusing `connectTwoSidesLocalIndices` unchanged (already generic over side count) for one adjacent-side fan per nested-star side (`2·starPoints` sides total). A tip-to-valley side and a valley-to-tip side are geometrically different by construction, so their fans naturally look different per point — no per-point special-casing needed, same principle as Star of David's hexagon-vs-triangle tiles looking different from each other. Threading is now 1 ring-weave Thread Path (circle ↔ star's outermost/level-0 vertices, reusing the existing round-robin primitive) plus one fan per side. `GeneratorParams`'s star variant gained `depth`/`layerAngle` (dropped nothing — all prior fields kept); `maxGeneratorColours("star")` changed from a flat `1` to `1 + 2·starPoints` to match the new run count. Found and fixed one latent bug while rewriting: the old single-thread `buildStar` passed the palette array straight into one `createThreadPath` call, which happened to be harmless only because Star's colour cap was always 1 before this fix — the new per-side-fan version threads correctly from the start (one colour per run, cycling by index, same rule as every other multi-run pattern). 773 tests total (+10 over the previous row: `nestedStar.test.ts`'s 6 cases, 4 rewritten `generatorPatterns.test.ts` star cases, `maxGeneratorColours` star case updated, 1 `GeneratorPanel.test.tsx` case retargeted from Star to Spirals since Star is no longer a 1-colour-cap pattern). Live-verified via Playwright + the debug hook: real Generate on the Star pattern produced a circle Pin Path (`circleNails` pins) + a nested-star Pin Path (`depth·starPoints·2` pins) and `1+2·starPoints` Thread Paths, screenshotted and visually confirmed each of the 5 points now shows its own distinct inward-spiralling fan instead of a uniform flat weave — no new console-error classes. **Post-ship fix #2 (2026-09-17, found by the user): Star rebuilt again — the nested-star fix above was itself wrong.** The user reported the nested-star result "looks more like Mandala than Star" — correct: concentric shrinking/twisting copies is a legitimate technique (it's what Star of David's tiles actually do), but it isn't what this specific pattern does. This time, rather than guess again from screenshots, read the actual pattern source already present in the earlier research clone (`~/projects/pocs/research_string_art/vendor/source/src/scripts/string_art_types/Star.pattern.ts` and `shapes/StarShape.ts` — TypeScript recovered from the reference site's own published source map, kept read-only in that separate research repo, never copied into this codebase) to understand the real construction instead of continuing to infer it from rendered pixels. The actual shape is a "spoke wheel": `starPoints` straight spokes from the board centre to the rim (not a pointed-polygon outline, and not nested/concentric at all), plus one outer circle. The threading is a classic curve-stitch/parabola technique (connect point k of one ray to point count-1-k of a second nearby ray — a generic, non-copyrightable geometric technique, independently re-derived and re-implemented from scratch as new original code, `src/domain/generator/curveStitch.ts`'s `curveStitchPairs`) applied between each spoke and its OWN LOCAL arc of circle pins (never the whole ring, never another point's arc) — this locality is what actually produces "different per point," not nesting. Deleted the now-wrong `nestedStar.ts`/`nestedStar.test.ts` from the previous fix. `GeneratorParams`'s star variant changed again: `circleNails`/`depth`/`layerAngle` dropped, `sideNails` added (the circle's pin count is now derived as `starPoints·(sideNails-1)`, matching the real proportions, rather than set independently); `maxGeneratorColours("star")` simplified from `1 + 2·starPoints` to a flat `starPoints`. 771 tests total (net −2 from the previous row: `curveStitch.test.ts`'s 3 cases added, `nestedStar.test.ts`'s 6 removed, 5 `generatorPatterns.test.ts` star cases rewritten, 1 i18n key added across 5 locales for `sideNails`). Live-verified via Playwright + the debug hook against the app's own dev server: real Generate on Star (default params) produced 1 circle Pin Path + 5 spoke Pin Paths + 5 Thread Paths as expected, and — critically, since the previous "fix" also looked plausible in a cropped screenshot before a wider view exposed the problem — a Fit-to-viewport screenshot was taken specifically to check the WHOLE board, confirming 5 visually distinct local fans converging into the 5 points with no concentric/spiral artifact — no new console-error classes. **Post-ship fix #3 (2026-09-17, requested by the user): Star given a denser, three-zigzag weave per point.** The single-fan version above was structurally right but visually thin. Rather than continue reverse-engineering a specific competitor's exact output — a line not worth crossing even in original code — this elaborates the SAME generic curve-stitch idea into an original three-zigzag-per-neighbouring-pair design: `src/domain/generator/starWeave.ts` (`starSpokeCircleZigzag`, `starAdjacentSpokeZigzag`) adds a second spoke↔circle sweep (mirroring the first, both pivoting on the shared boundary pin between adjacent points) plus a third zigzag directly between adjacent spokes, bypassing the circle. `buildStar` now emits `3·starPoints` Thread Paths (`maxGeneratorColours("star")` updated to match); `curveStitchPairs`/`curveStitch.ts` from the previous fix is superseded and removed, since the new zigzag functions produce a proper continuous thread per pass instead of disconnected pairs. 775 tests total (+4 over the previous row: `starWeave.test.ts`'s 6 cases added, `curveStitch.test.ts`'s 3 cases removed, `generatorPatterns.test.ts` star cases rewritten for the 3-pass structure, net +1 there). Live-verified via Playwright + the debug hook: real Generate on Star (default params) produced 1 circle Pin Path + 5 spoke Pin Paths + 15 Thread Paths (3×5) as expected — no new console-error classes. |

| M26 | Generator Mode — Remaining 14 Patterns | M25 | Done | Shipped the 14 patterns M25 deferred (Wave, Hexagon Spades, Dance of Planets, Sun, Vortex, Polygon, Flower, Assymetry, Spiral, Maurer Rose, Comet, Flower of Life, Lotus, Crosses) — all 19 researched patterns are now built. Same approach as M25 and its Star fixes: each pattern is an *original* implementation of the researched pattern's general documented technique, reusing this app's own tested primitives wherever the technique matched one already built, never a byte-identical reproduction of a specific reference's exact traversal; a few (Flower of Life, Crosses, Assymetry) ship deliberately simplified versions, stated plainly in `32-generator-mode.md`. Two new shared domain primitives extracted/generalized first, since several patterns depend on them: `tileRingLayout` (`src/domain/generator/tileRing.ts`, generalized from Star of David's own `buildStarOfDavidTiles`/`buildTilePinPath` tile-ring construction — Star of David itself refactored onto it with zero behavior change, verified via its existing tests plus a new byte-for-byte-equivalent-geometry test) and `twoRayZigzag` (`src/domain/generator/rayZigzag.ts`, generalized from Star's own `starAdjacentSpokeZigzag` reversed-pairing zigzag to two independent finite rays instead of a shared circle). A shared application-layer `buildTileFans` helper (`generatorPatterns.ts`) then unified the "N tiles, one adjacent-side-fan-per-side" traversal across Star of David, Hexagon Spades, and Flower of Life. 5 new pure domain functions for the patterns with no existing-primitive fit: `waveLayerSequences` (Wave — a partial/rotating-offset generalization of Mandala's own `mandalaLayerSequences`), `asymmetryZigzag` (Assymetry — one zigzag over a circle+spoke's combined wrapped index space), `spiralDecayingWalk` (Spiral — a single walk whose chord span decays each of `repetition` passes), `maurerRosePoints` (Maurer Rose — the classic public 1987 Maurer-rose curve formula, curve-sampled directly into pins like Spirals already does), `cometLayerSequences` (Comet — offset-alternation passes whose run length and offset both shrink per layer). Sun and Flower reuse `buildStar`/`buildPolygonCore` calls directly rather than duplicating their geometry. Dance of Planets is the first generator pattern to use the native `"regular-polygon"` `PinPathGeometry` (previously only reachable via the Pin tool), vertex-anchored spacing derived the same way Polygon/Flower now do (`spacingForPinCount(sideLength, nailsPerSide)` per side). 825 tests total (+50 over M25's final 775: `tileRing.test.ts`, `rayZigzag.test.ts`, `wave.test.ts`, `asymmetryZigzag.test.ts`, `spiralWalk.test.ts`, `maurerRose.test.ts`, `comet.test.ts` in `src/domain/generator/`, plus 14 new `buildGeneratorPattern —` describe blocks and an updated 19-pattern registry test in `generatorPatterns.test.ts`). i18n: `generatorToolbar.patterns.*` (14 new pattern names) and `generatorPanel.fields.*` (30 new field labels, including `outerType`/`innerType`'s `shapeCircle`/`shapePolygon` option labels) added across all 5 locales, `keyParity.test.ts` green. `docs/specs/32-generator-mode.md` gained a full "The 14 M26 patterns" table (layout/threading/params/scope-note per pattern) and 14 new Gherkin scenarios; Scope section updated to record all 19 patterns as shipped. Live-verified via Playwright + the debug hook against the app's own dev server (a second Vite instance on a free port, since the default 5173 was occupied by an unrelated project): all 14 patterns' default params built successfully via `generatePattern` with sane pin/thread counts read back from `generatorDraft` (cross-checked against the same math the unit tests assert), then all 14 cycled through the *real* UI (pattern dropdown → Generate/live-auto-apply) with zero new console-error classes. Fit-to-viewport screenshots visually confirmed the four highest-risk composites: Hexagon Spades (a genuine 6-triangle hexagram silhouette with nested colour-banded zigzags, no central hub), Sun (Star's spoke-wheel plus 3 visibly distinct shrinking decorative rings reading as a proper radiant sun), Maurer Rose (a textbook 7-petal rose curve, N=7/angle=71°), and Crosses (a clean 4-line "#" grid with curve-stitched crossing weaves at all 4 intersections) — the remaining 10 patterns reuse already visually-proven primitives (circle+modular, round-robin, polygon+zigzag) and were confirmed structurally rather than re-screenshotted individually. **Post-ship correction (2026-09-17, requested by the user, using their own downloaded reference SVGs in `.examples/` plus the researched pattern algorithms):** several patterns' traversals were genuinely wrong structurally, not just simplified — read the actual algorithm descriptions (`~/projects/pocs/research_string_art/docs/pattern-specifications.md` and the individual `*.pattern.ts` source files, both pre-existing read-only research artifacts) to understand each real technique, then reimplemented every fix as original code (own naming/structure/comments, this app's own very different data model and geometry primitives) — never copying source verbatim. **Vortex** was the most significant fix: the original "closed-outline-per-level" shape was a genuinely different (and far sparser) topology than the real per-side same-index-chord weave — rebuilt with one native `regular-polygon` Pin Path per nested level (`nailsPerSide` now a real param) and `layers·nailsPerSide` closed-loop Thread Paths (chaining a same-index chord across every side reproduces the exact chord set with zero extra segments). **Wave**'s traversal was upgraded from a bare 2-point chord-per-index to the real 4-point cell (`k, k·base mod n, +base, k+1` for advancing odd `k`), matching Mandala's own denser "brush-stroke" table texture. **Polygon**/**Flower** switched from a reversed-pairing zigzag (`twoRayZigzag`, still used by Crosses) to a new `sameIndexZigzag` (same-index chords across two sides), matching a regular polygon's own side-to-side envelope technique. **Spiral**'s walk was rebuilt from a monotonic one-directional shrink to the real oscillating forward/backward decay (crossing back over itself as it tightens, `innerLength` reinterpreted as the STARTING span/"thickness" rather than a floor). **Comet**'s two independent shrink parameters (run length and offset) were collapsed into the real model — only the offset (`firstLayerSize`, shrinking by `layerDistance` per layer) is a free parameter; each layer's own run length is now correctly DERIVED from it (`n-d+1`), not set independently (param renamed `distance`→`layerDistance`, i18n updated across all 5 locales). **Assymetry**'s zigzag switched from a "meet in the middle" two-pointer walk to the real "fixed offset, advancing index" technique, its spoke nail count now derived to match the circle's own physical spacing (`round(circleNails/2π)`), and its default `startFraction` changed from 0 to 0.25 to match the reference's own default layer. **Hexagon Spades**' tile proportions were aligned to Star of David's own exact ratios (`R0/3` triangle radius, `2·R0/3` helper radius) instead of an arbitrary 50/50 split. Sun, Lotus, and Flower of Life were reviewed against the same source but left as their already-documented simplifications (their real constructions — a wholly different star-plus-backdrop base shape, an intricate multi-section radial patch system, and a `6·levels²`-scaling hex-grid, respectively — would require much larger rewrites for comparatively low incremental fidelity gain per this session's established original-code-over-exact-reproduction principle). Maurer Rose needed no change (already the correct public 1987 formula). 834 tests total (+9 over the previous row: `sameIndexZigzag` cases in `rayZigzag.test.ts`, rewritten `wave.test.ts`/`spiralWalk.test.ts`/`comet.test.ts`/`asymmetryZigzag.test.ts` cases, 2 new `vortex` build-test cases replacing the old one). `docs/specs/32-generator-mode.md`'s M26 pattern table and affected Gherkin scenarios updated to match. Full regression green (`tsc -b --force`, `eslint .`, `npx vitest run`, 834/834). **Post-ship removal (Lotus, 2026-09-18):** after this correction pass, Lotus was rewritten several more times against a source-backed mathematical derivation document the user provided, but never reached a result the user judged correct — removed from the registry, panel, i18n, and tests entirely rather than ship it wrong; 18 of the 19 researched patterns remain built. **Post-ship correction (Crosses, 2026-09-18):** the earlier simplified 4-line "#" grid was replaced with the real fixed 10-line arrangement (a central spine split into 4 nail-bearing segments, plus 3 rows of 2 arm lines straddling the segment gaps) and its own `crossesWeave` traversal (`src/domain/generator/crossesWeave.ts`, source-derived from `connectLines`'s alternating-side zigzag with a source-copied 10-entry `(spineIndex, row)` connections list), including the previously-missing `orientation` and `sidesRotation` controls (the latter tilts only row 0's and row 2's arms, pivoting on each arm's own inner end, sign-flipped per corner exactly as the source's `getRowLineRotationConfig`); `gap` now doubles as the source's own default-locked length/width gap rather than an invented single offset. Canvas-autofit bounding-box growth for large `sidesRotation` and the source's fine-control/colour-banding options remain unported, stated plainly in `32-generator-mode.md`. See `32-generator-mode.md`. **Post-ship re-addition (Lotus, 2026-09-18):** re-implemented from a more precise source-backed mathematical derivation (`~/projects/pocs/research_string_art/docs/lotus-pattern-generation.md`, cross-referencing `Lotus.pattern.ts`'s `getPatchColor`/`drawPatch`/`drawStrings`/`generatePatches`/`getNails`/`getStepCount`) as small, independently unit-tested pure functions (`src/domain/generator/lotus.ts`: `lotusFit`, `lotusPetalCenter`/`lotusPetalPoint`/`lotusCenterCirclePoint`, `lotusGeneratePatches`/`lotusDrawPatch`, `lotusGetStepCount`) rather than one large routine — the previous attempts' failure mode was judging correctness by eye alone with no way to isolate which piece of the index arithmetic was wrong. `sides` overlapping partial-arc "petal" circles around a helper circle, removal re-fits the whole construction back up to size exactly like the source; each `(petal circle, section)` patch is two fans of nails converging on one shared target, threaded as one Thread Path per patch (revisiting the shared target between sources, since a >2-spoke fan has no single trail through it without retracing — visually identical to independent segments, just grouped for one recolourable run per patch). Params: `sides`, `density`, `rotation`, `removeSections`, `renderCenter`, `centerRadius`, `radialColor`. Two reference concepts dropped as stated simplifications: canvas margin (this engine has none, and the fit ratio is algebraically margin-independent at margin=0) and "Render center nails" (a source-documented cosmetic-only toggle that hides some interior nails without changing which strings are drawn). 892 tests total, all green (`npx vitest run`): `lotus.test.ts`'s 23 cases in `src/domain/generator/`, plus a `buildGeneratorPattern — lotus` describe block, a `maxGeneratorColours` lotus case, and the 19-pattern registry count restored in `generatorPatterns.test.ts`. i18n: `generatorToolbar.patterns.lotus` and `generatorPanel.fields.{removeSections,renderCenter,centerRadius,radialColor}` added across all 5 locales, `keyParity.test.ts` green. `docs/specs/32-generator-mode.md` gained a dedicated Lotus section (pin layout/threading/params/scope notes) replacing the removal note. Live-verified via Playwright + the debug hook against the app's own dev server: real Generate+Confirm produced a genuine flower/lotus silhouette — 9 overlapping pointed petals converging on a filled centre disc at default params (18 sides, density 15, ~3 sections removed), a full unremoved 12-petal pinwheel with `removeSections=0`, a 12-petal rosette with no centre anchor (`renderCenter=false`), and a 14-petal layered mandala-like flower with `radialColor=true` and a single centre pin (`centerRadius=0`) — screenshotted and visually confirmed each time; no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning). **Post-ship addition (2026-09-18, requested by the user): Generator panel usability pass — sliders, colour fieldset, thread-width control.** (1) Every pattern-parameter field across all 18 patterns switched from a plain number-box `NumberField` to a new shared `SliderField` (`src/ui/panels/fields/SliderField.tsx`, moved out of `GeneratorPanel.tsx` once `ThreadPropertiesPanel` also needed it) — an `<input type="range">` with an adjacent monospace value readout, same `min`/`max`/`step` bounds as before, an explicit `aria-label` (overriding the implicit wrapping-`<label>` accessible name, which would otherwise concatenate the label text with the value readout) so every existing `getByLabelText` test query kept working unchanged. New `.slider-field-range` track/thumb styling added to `src/ui/theme.css` (`--bg-panel-2` track, `--accent`/`--accent-strong` thumb, 120ms ease — no new tokens). (2) The colour-swatch section became a real `<fieldset>`/`<legend>` group (`--bg-app`/`--border`/`--radius-md`, matching the existing grouped-panel recipe) with the section title and `+`/`−` buttons sharing the legend's first row, buttons right-aligned; swatches moved to the row beneath. `+`/`−` stay rendered and simply go `disabled` at the count cap/floor (per explicit user request — the buttons should not disappear, only grey out) — `src/ui/theme.css` gained a standard `.btn:disabled` treatment (`--text-tertiary` text, 0.5 opacity, `not-allowed` cursor, hover suppressed) that now applies app-wide to every disabled `.btn`, not just these two, since no disabled-button styling existed anywhere before this; the `visibleColours` `useMemo` and its debounce-effect dependency (the load-bearing fix for a prior infinite-auto-regenerate bug) were left untouched throughout. (3) A new thread-width `SliderField` (0.5–5, step 0.5) was added to `GeneratorPanel`, writing straight through the existing `store.setThreadProperty({ width })` — deliberately the same shared `threadDefaults.width` `ThreadPropertiesPanel` already edits in Thread mode (confirmed with the user rather than adding a generator-local width parameter), so `generatePattern`/`confirmGeneratedPattern` needed no changes. `ThreadPropertiesPanel`'s own width field was migrated to the same `SliderField` for consistency now that it's a shared component. 895 tests total (+3 over the previous row): `GeneratorPanel.test.tsx`'s existing `multicolor palette` describe block's `toBeDisabled()`/`not.toBeDisabled()` assertions stayed as-is (still correct for the disabled-not-hidden final behavior), plus a new both-buttons-enabled middle-state case, a slider-bounds smoke test, and a thread-width-writes-through test; `EditorStore.generator.test.ts` and `ThreadPropertiesPanel.test.tsx` needed no changes (the latter already asserted by label text/store state, not input type). i18n: `generatorPanel.fields.threadWidth` added across all 5 locales, `keyParity.test.ts` green. `docs/specs/18-design-system.md` gained two Component Vocabulary entries (Slider (range input), Fieldset/legend grouping) plus a disabled-button-styling note on the Primary/secondary buttons entry; `docs/specs/32-generator-mode.md` gained a "Panel controls" section. **Found and fixed by the user immediately after, same change:** the live auto-apply debounce effect's dependency array (`GeneratorPanel.tsx`) listed `[hasDraft, params, visibleColours, store]` — `state.threadDefaults.width` was missing, so dragging the new thread-width slider after the first Generate silently updated `threadDefaults.width` but never re-ran `generatePattern`, leaving the preview's stroke width stale until some other field happened to change too. Added `state.threadDefaults.width` to the dependency array; `generatePattern` already read the live value on every call (only the effect wasn't re-firing), so no change was needed there. New regression test (`GeneratorPanel.test.tsx`): Generate, change thread width, advance the debounce timer, assert `generatorDraft.threadPaths[0].width` picked up the new value. 896 tests total. Full regression green (`tsc -b --force`, `eslint .` — 0 errors, pre-existing unrelated warnings only, `npx vitest run`, 896/896). Live-verified via Playwright + the debug hook against the app's own dev server: sliders render with working value readouts and drag-to-update on multiple patterns (including Freestyle's nested per-circle fields); at Mandala's default `layers=1` both colour buttons are confirmed still present but visibly greyed/disabled (not unmounted), raising `layers` and clicking Add/Remove enables/disables them exactly at the cap/floor with both enabled in the middle state; dragging the new thread-width slider in Generator mode and checking Thread mode's own width field confirms the shared write in both directions, and — after the debounce-dependency fix above — the Generator preview's own draft now re-generates with the new width past the debounce; Generate → Confirm → Undo unaffected; no new console-error classes. |

| M27 | Pin Path Tool | M3, M7, M17 | Done | New "Path" Pin tool (`PinTool` id `"polygon"`, toolbar icon `Waypoints`) — click-per-vertex free-form polygon draft, snap-resolved same as every other Pin tool. New `PinPathGeometry` `"polygon"` variant (`pinPath.ts`: `closedPolylineShape`-backed, added to `VERTEX_ANCHORED_TYPES` alongside translate/rotate/scale/center cases mirroring "freehand"'s treatment). Transient `EditorState.polygonDraft` (`{points: Point[]} \| null`), mirroring `ThreadDraft`'s non-undoable-until-committed shape — new `EditorStore` methods `extendPolygonDraft`/`retractPolygonDraft`/`finishPolygonDraft`/`escapePolygonDraft`/`cancelPolygonDraft`, cleaned up on `setMode`/`setPinTool` exit like `generatorDraft`. New `usePolygonDrawing.ts` hook (click dispatch + close-on-first-vertex hit-test reusing the existing pin-snap radius, global `Escape`/`ArrowLeft` keydown scoped to Pin mode + the Path tool) wired into `Canvas.tsx`; live preview reuses the existing accent-dashed preview pipeline by rendering the in-progress vertices as an open `"freehand"` geometry (gets symmetry-mirror preview for free, no new rendering component needed) plus a small close-target highlight ring. Radial context menu's Pin-mode slice set made conditional on `polygonDraft` (`radialMenuSlices.ts`/`RadialContextMenu.tsx`), same swap shape as Thread's Cut/Back/Next — new Cut/Back/Cancel slices (`Scissors`/`ArrowLeft`/new `X` icon); i18n `radialMenu.cancel` + `pinToolbar.basicTools.path` added across all 5 locales, `keyParity.test.ts` green. Spec `33-pin-path-tool.md` added (index row in `00-overview-and-scope.md`); touched specs `07`/`08`/`20`/`25` updated to reflect the new tool (07's Vertex-Anchored list, 08's tool list, 20's Pin/Keyboard&Mouse tab content — also fixed a stale pre-M17 "right-click never a generic context menu" line found while updating it, 25's Pin-mode conditional slice set + new Gherkin scenarios). Help content (`helpContent.ts` + all 5 locales' `help.json`) and `README.md` updated with the new tool and its Esc/Back/Cancel/close-on-first-vertex shortcuts. 917 tests total (+21 over the previous row's 896: `EditorStore.polygon.test.ts`'s 14 cases, `pinPath.test.ts`'s 2 new cases (vertex-anchored + scaleGeometryAboutPivot cross-check), `radialMenuSlices.test.ts`'s 1 new case (plus 1 existing case updated for the new `pinPath` slice), `Canvas.polygon-drawing.test.tsx`'s 6 real-click/keydown cases). `tsc -b --force` and `eslint .` both clean (0 errors, only the 4 pre-existing unrelated `react-refresh/only-export-components` warnings in `boardFill.tsx`). Live-verified via Playwright + the debug hook against the app's own dev server: real clicks placed 4 vertices with a visible live dashed preview (screenshotted), right-click showed exactly Cut/Back/Cancel while drafting (screenshotted) and the normal 10 pin-tool slices otherwise, Back/ArrowLeft both retracted the last vertex and cancelled the draft when it removed the only one, clicking back near the first vertex (within the snap radius) closed a 4-vertex draft into a real `"polygon"` Pin Path with the correct vertex-anchored pin count (40 pins for a 4×10cm square) and handed off to Select mode with it selected, Cut committed a 3-vertex draft the same way, Cancel discarded a 3-vertex draft with NO commit and left mode unchanged (unlike Cut) — confirming the two are genuinely different code paths, Escape silently discarded a <3-vertex draft, a locked Pin Layer blocked the commit, and Undo removed the just-committed path in one step; no new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning). One real test-methodology bug caught and self-corrected mid-session, not a product bug: several early live-verification steps were unknowingly run in Select mode because `addPinPath` auto-switches to `"select"` on every commit (existing, correct behaviour) — subsequent Cut/Cancel clicks were silently landing on the *Edit-mode* radial menu instead of a Path draft's, giving false-positive "nothing happened" results; caught by checking `state.mode` after an unexpected outcome, fixed by explicitly resetting `setMode("pin")`/`setPinTool("polygon")` before each new scenario, then every scenario re-verified for real. |
| M28 | Keyboard Shortcuts | M2, M3, M5, M11, M12, M16, M18, M22, M27 | Done | Central `useKeyboardShortcuts.ts` dispatcher (mounted once in `EditorShell.tsx`) covers ~40 bindings: New Board `Enter`/`L` (own local listener in `BoardSetup.tsx`), General (`Ctrl/Cmd+S` Save, `Ctrl/Cmd+O` Open, `Ctrl+Alt+N` New, `?` Help, bare `+`/`-` Zoom, bare `0` Fit, `Ctrl+Shift+G`/`Ctrl+Shift+A` Grid/Snap), bare-digit tab switching (1-6), and every Edit/Pin/Thread/Play tool letter plus their Shift-modified secondary actions (granularity toggle, symmetry cycle, colour count, thread width, export-to-video). `SymmetryPanel.tsx`/`ThreadPropertiesPanel.tsx` refactored to share `symmetryConfigForType`/`coloursForCount` with the dispatcher so mouse and keyboard can't drift apart; `FileMenu.tsx`/`LanguageSwitcher.tsx` gained `forwardRef`+`useImperativeHandle` handles (`openFilePicker`, `openOrCycle`) for the same reason. Toolbar buttons show their shortcut as a `[X]` suffix (visible on Edit/Pin/Thread/tab-switcher labels, in the tooltip for Play's icon-only buttons) composed at render time, not a new i18n key. `+`/`-` ambiguity (Thread's `Shift++`/`Shift+-` vs global bare zoom) resolved by mode-first dispatch precedence, documented in the spec rather than requiring an unreproducible modifier combo. Spec `34-keyboard-shortcuts.md` added (index row in `00-overview-and-scope.md`) with ~40 Gherkin scenarios. Help content (`helpContent.ts`'s `keyboard-mouse` tab) and all 5 locales' `help.json` updated with the full shortcut reference. 950 tests total (+29 new: `EditorShell.keyboard-shortcuts.test.tsx`'s 24 cases, 3 new `BoardSetup.test.tsx` cases, 1 new `LanguageSwitcher.test.tsx` case, plus 1 pre-existing unrelated `tsc -b --force` type error in `App.newProject.test.tsx` fixed in passing — `window.stringArtItDebug` is typed optional but guaranteed-set post-render). `tsc --noEmit`, `npm run build`, and `eslint .` all clean (0 errors; only pre-existing `react-refresh/only-export-components` warnings, now +5 for the newly-exported shared constants/helpers, same accepted class as `boardFill.tsx`'s pre-existing 4). Live-verified via Playwright + the debug hook against the app's own dev server: New Board Enter (blocked while a dimension field has focus, fires after blur) and L (opens the language dropdown, a second press cycles and closes) both confirmed with real keystrokes; in the Editor, all 6 digit tab switches, all 11 Pin-tool letters, the 5-step symmetry cycle (wrapping back to none), all 4 Edit-tool letters plus Shift+P granularity toggle and J merging 2 real Pin Paths into 1, all 4 Thread-tool letters plus Shift+1/2/3 colour count and Shift++/Shift+- width (0.5 steps, both directions), bare `=` zooming instead of touching thread width while in Thread mode, Ctrl+Shift+G/A toggling grid/snap, bare +/-/0 zoom/zoom/fit, `?` opening and its Close button closing Help, Ctrl+Alt+N returning to New Board, Ctrl+S triggering a real file download (`string-art-project.json`) and Ctrl+O opening a real native OS file-chooser dialog (confirmed via Playwright's modal-state detection, then cancelled), and all 4 Play-tab step keys plus Space toggling Play/Pause with a committed thread frame — every one verified via real keystrokes and store-state/DOM assertions, not simulated. Tooltip bracket-suffixes (e.g. `data-tooltip-content="Next frame [N]"`) confirmed present in the live DOM. No new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning, seen once per mode switch during the digit-tab-switching pass). **Post-review fixes (2026-09-20, found by the user):** (1) Shift+1/2/3 colour-count didn't work on a real keyboard — Shift+digit produces the shifted symbol as `e.key` (Shift+2 → `"@"`, never `"2"`), so detection was switched to `e.code` (`Digit1`/`Digit2`/`Digit3`, layout/shift-independent); caught because a Playwright-synthesized `press('Shift+2')` doesn't reproduce real shift-remapping and gave a false pass in the original live verification, and the original unit test manually set `key:"2"` rather than the real shifted character — both fixed (test now sets `key:"@", code:"Digit2"`, and live-verified with a manually dispatched real-shape `KeyboardEvent`). (2) Tab-switcher `[1]`-`[6]` visible bracket suffixes reverted per the user's follow-up request — `ModeSwitcher.tsx` labels are back to plain text; the 1-6 shortcuts still work, just undocumented on the pills themselves (only in Help). (3) Added an arrow-key pan fallback the user requested: arrows now pan the viewport (`Shift+Arrow` for a bigger step) whenever not claimed by `useKeyboardTransform` (Move/Rotation/Scale tool active in Edit mode, gated on the tool not a live selection), an in-progress Thread draft (`ArrowLeft`/`ArrowRight` only), or an in-progress Path/polygon draft (`ArrowLeft` only) — implemented by re-deriving those three independent listeners' own guard conditions rather than a shared flag, keeping the addition purely additive. Direction was initially inverted (fixed to match `usePanInteraction.ts`'s existing drag-to-pan sign convention exactly: pressing an arrow moves the board the same way dragging in that direction would). All three fixes live-verified via Playwright + the debug hook; 6 new pan-fallback tests + 1 corrected colour-count test added (956 tests total). |
| M29 | Zig-Zag and Parabolic Thread Tools | M5, M17, M22, M28 | Done | Core math module `src/application/document/twoPinSequence.ts` (`computeSamePathCandidates`/`computeCrossPathCandidates`, unified `interleave`+`reverseSecond` algorithm — one shared function covering both tools' same-path/cross-path cases, differing only in which run gets reversed) implemented and unit-tested (13 cases in `twoPinSequence.test.ts`, reproducing all 4 user-supplied worked examples exactly plus adjacent-pins/closed-path-two-arcs/cross-path-direction-choice edge cases). Full UI wired: `ThreadTool` gained `"zigzag"`/`"parabolic"`, transient `EditorState.twoPinDraft` + `EditorStore` methods (`startTwoPinDraft`/`chooseSecondPin`/`setTwoPinChosenCandidate`/`resolveTwoPinDraft`/`cancelTwoPinDraft`/`retractTwoPinDraft`, discarded on `setMode`/`setThreadTool` exit like every other draft), `useTwoPinSequenceDrawing.ts` hook + `TwoPinDraftLayer.tsx` preview wired into `Canvas.tsx`, `ThreadToolbar.tsx` buttons (`Waypoints`/`Croissant` icons), Thread-mode radial-menu slices (`radialMenuSlices.ts`/`RadialContextMenu.tsx` — normal `threadZigzag`/`threadParabolic` slices plus a draft-mode Cut/Back/Cancel swap once 2+ candidates exist), `Z`/`P` keyboard shortcuts (`useKeyboardShortcuts.ts`, plus an arrow-key-pan-claim row for the in-progress draft), and `helpContent.ts`/all 5 locales' `help.json`/`toolbars.json` (`keyParity.test.ts` green — one locale-script mistake caught and fixed: an anchor string matched an earlier "select" line in the Edit tab's shortcut block instead of Thread's, landing the new keys in the wrong section for 4 of the 5 locales, found by the parity test itself). 982 tests total (+26 over M28's 956: `twoPinSequence.test.ts`'s 13 cases, 1 `radialMenuSlices.test.ts` case updated for the new Thread-mode slice set, plus the rest from other post-M28 work already merged before this milestone started). `tsc --noEmit`/`eslint` clean. Live-verified via Playwright + the debug hook against the app's own dev server: real clicks on a closed 8-pin circle (non-adjacent pins) correctly populated 2 arc candidates (lengths 4 and 6) matching the hand-derived math exactly, live cursor-hover correctly swapped the previewed candidate between the two arcs' far endpoints, and a free-position 3rd click committed whichever was previewed, as one atomic Undo/Redo step; an open 10-pin Line with Zig-zag auto-committed on the 2nd click (no 3rd click) with pin order `[1,10,2,9,3,8,4,7,5,6]`, and an 11-pin Line with Parabolic gave `[1,7,2,8,3,9,4,10,5,11,6]` — both exactly matching the user's original worked examples; `Z`/`P` bare-key shortcuts and the Draw/Zig-zag/Parabolic/Select/Eraser/Segment toolbar row all confirmed via real keystrokes/clicks (screenshotted); the radial menu showed all 5 tool slices normally and swapped to Cut/Back/Cancel while a draft was ambiguous, with a real click on Cut committing the previewed candidate; Escape and ArrowLeft both worked mid-draft (Escape cancels outright, ArrowLeft drops populated candidates back to awaiting the second pin, keeping the same anchor). No new console-error classes (only the pre-existing documented `ModeSwitcher` border/borderColor shorthand warning). **Post-ship addition (2026-09-21, requested by the user): step-by / full-fill / cycles configuration.** `twoPinSequence.ts`'s `computeSamePathCandidates`/`computeCrossPathCandidates` gained a `tool`+`settings` parameter (`TwoPinFillSettings: {stepA, stepB, fullFill, cycles?}`, replacing the old plain `reverseSecond` boolean, now derived internally) and a `strideList`/`interleaveFull` pair implementing per-side striding (`stride = step+1`, verified against the user's own worked example: `step=2` both sides on 10/20-pin paths → `[1,11,4,14,7,17,10,20]`) plus bounded full-fill (append the longer strided side's remaining tail once the shorter is exhausted). Parabolic full-fill on a same-CLOSED-path pair is the one case that isn't bounded: `computeSamePathCandidates` detects this (`tool==="parabolic" && closed && settings.fullFill`) and builds the SAME firstHalf/secondHalf pairing from the whole ring (every pin, chosen direction) instead of the clicked arc, then repeats that one full-ring pass `cycles` times (`cycles=1` default reproduces today's single pass) — every other combination (cross-path, same-open-path, or Zig-zag) ignores `cycles` entirely and falls back to the same bounded tail-append rule. New `EditorState` fields `zigzagSettings`/`parabolicSettings` (`ZigzagSettings`/`ParabolicSettings`), same "next-draw defaults, not undoable" treatment as `threadDefaults` — new `EditorStore.setZigzagSettings`/`setParabolicSettings`, read by `chooseSecondPin` at click-2 time. UI: extracted `CheckboxField` out of `GeneratorPanel.tsx` into `src/ui/panels/fields/CheckboxField.tsx` (same "move out once a 2nd panel needs it" precedent `SliderField` went through at M26), added a `disabled` prop to `SliderField`, and added two new conditional sections to `ThreadPropertiesPanel.tsx` (`ZigzagSettingsBox`/`ParabolicSettingsBox`, shown only while the matching tool is active — Step A/Step B sliders + Full-fill checkbox for both, plus a Cycles slider for Parabolic, disabled until Full-fill is checked). i18n: 6 new `threadPropertiesPanel.*` keys added across all 5 locales, `keyParity.test.ts` green. Spec `35-zigzag-parabolic-tools.md` gained a "Configuration" section + 6 new Gherkin scenarios. 989 tests total (+7 over the previous row's 982: 7 new cases in `twoPinSequence.test.ts` covering the worked example, a leftover-pins case, Zig-zag full-fill in both Case 1 and Case 2, Parabolic full-fill's ring-walk at cycles=1 and cycles=3, and Parabolic's cross-path fallback). Full regression clean: `npm run build` (the real deploy command), `npx vitest run`, `npx eslint .`. Live-verified via Playwright + the debug hook against the app's own dev server: the Zig-zag/Parabolic settings boxes render correctly (screenshotted) with the right fields per tool; the Cycles slider is confirmed disabled by default and becomes enabled via a real checkbox click; a real Step A slider drag (keyboard arrow-right ×3) wrote `stepA: 3` to the store; an 8-pin closed circle with Parabolic + full-fill + cycles=3, clicked for real (2 clicks + a free-position 3rd click), produced the exact same 24-pin sequence the unit test derived by hand, committed as one atomic Undo/Redo step; no new console-error classes. This is a follow-up increment on the already-Done M29 milestone, not a new milestone number, per this repo's "Post-ship addition" convention. **Post-ship correction (2026-09-21, found by the user): full-fill's algorithm was wrong — rebuilt around "apply the same existing extraction, to both options" instead of inventing a new one.** The first pass above (`interleaveFull` appending "the longer side's remaining tail") was a genuine misunderstanding, caught by the user after 3 rounds of clarification with concrete worked examples. The correct model: full-fill never invents new math — everywhere the tools already require a 3rd click to disambiguate between two extraction options (Case 2: a mid-path anchor's two directions; Case 1 closed-path: the two arcs), full-fill just uses **both** options instead of picking one, via the SAME `extractIds`/`remainingInDirection` primitives already used for the non-full-fill candidates. Case 2: new `anchorRuns()` in `twoPinSequence.ts` — for a mid-path anchor on an OPEN path with full-fill on, combines both directions into one run (every pin walking dir `+1`, then every pin walking dir `-1` with the duplicated anchor dropped), collapsing that anchor's own direction ambiguity to a single candidate (no 3rd click); a CLOSED-path anchor or an anchor already at a true endpoint is unaffected (nothing to combine — either both directions already give the full ring, or only one direction had any extent to begin with). The outer pairing between the two anchors' runs is always a plain `interleave` truncated to whichever is shorter — NO tail-append ever happens; `interleaveFull` was deleted entirely. Case 1: the closed-path "walk the whole ring" ring-fill mechanism from the first pass was actually correct in shape, just wrongly gated to Parabolic only (`tool === "parabolic" && closed && fullFill`) — fixed to apply to both tools (`closed && fullFill`), since "use both arcs = use the whole ring" is tool-agnostic; `cycles` stays Parabolic-only (Zig-zag has no Cycles field). One real regression bug found while rewriting: `buildCrossPathSequence` was reversing Parabolic's second run BEFORE truncating it to match the first run's length, which drops pins off the wrong end (the one nearest the clicked pin, not the far end) — fixed by capping both raw runs to equal length before reversal, verified against the pre-existing `EditorStore`-adjacent regression tests that caught it. Rewrote 5 of the 7 full-fill unit tests in `twoPinSequence.test.ts` around the user's own concrete confirmed examples (a mid-path anchor's combined-run order, unequal-length truncation with no tail, Parabolic's cross-path combine, Zig-zag's whole-ring closed-path fill) — 993 tests total (+4 over the previous row's 989). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and Gherkin scenarios rewritten to match. `npm run build`/`npx vitest run`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook against the app's own dev server: two real 10-pin Line Pin Paths, real clicks on path A's mid-path 5th pin then path B's endpoint pin (Zig-zag, full-fill on) committed immediately (no 3rd click) with pinIds `[5,11,6,12,7,13,8,14,9,15,10,16,4,17,3,18,2,19,1,20]` — exactly the corrected algorithm's hand-derived result — as one atomic Undo/Redo step; no new console-error classes. **Post-ship correction #2 (2026-09-21, found by the user via real use): Case 1's closed-path full-fill ignored the second clicked pin entirely.** The user clicked pin 68 then pin 1 on a 70-pin closed circle and got a thread starting `68,69,67,...` instead of the expected `68,1,...` — the previous pass's `ringFill` built the whole-ring range by walking `pinCount` steps from ONLY the first-clicked pin, splitting it at an arbitrary ring-relative midpoint that had nothing to do with where the second pin actually was. Root cause was the same class of mistake as the Case 2 correction above (inventing new math instead of reusing what already existed) — fixed the same way: `computeSamePathCandidates`'s closed-path branch now runs the SAME unchanged bounded-arc `buildSameRangeSequence` call the non-full-fill case already uses, once per arc (short arc, then long arc), and concatenates the two — each arc's own pairing still starts with `(A,B)` as its first pair, exactly matching what the user asked for ("create a segment between 68 and 1, and then fill both sides"). `cycles` still repeats this concatenated both-arcs pass. Rewrote the 3 Case-1-closed-path full-fill tests in `twoPinSequence.test.ts` (hand-verified against an 8-pin fixture) plus added a regression test confirming the non-full-fill 3rd-click path is untouched — 994 tests total (+1 over the previous row's 993). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and affected Gherkin scenarios rewritten to match. `npm run build`/`npx vitest run`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook reproducing the EXACT reported scenario: a real 70-pin closed Circle, real clicks on pin 68 then pin 1 (Zig-zag, full-fill on), committed immediately (no 3rd click) with the first 10 pins `[68,1,69,70,68,1,67,2,66,3]` — first pair `(68,1)` as expected, short arc finishing `(69,70)`, then the long arc restarting at `(68,1)` and zigzagging inward — as one atomic 72-pin Undo/Redo step; no new console-error classes. **Post-ship correction #3 (2026-09-21, found by the user via real use): concatenating the two arcs drew a spurious segment between them.** Correction #2's `[...arcSequence(1,forwardN), ...arcSequence(-1,backwardN)]` concatenation was itself wrong: a `ThreadPath.pinIds` array is always rendered as ONE continuous connect-the-dots line, so gluing two independently-built arc sequences together always draws an extra segment from wherever the first arc's zigzag happens to end to wherever the second arc's `(A,B)` pair restarts — reported live as "a wrong segment between 5 and 110" on a 141-pin circle, traced to the exact boundary (pin 2 → pin 107, not literally 5/110 but visually close given tight pin spacing). Fixed by NOT concatenating: `TwoPinCandidate` gained an optional `extraSequences?: string[][]` field (only ever populated by Case 1 closed-path full-fill), and `EditorStore.commitTwoPinSequence` was generalized from committing one `ThreadPath` to committing an array of them in one bundled `SetValueCommand<ThreadLayer[]>` — each arc/circle becomes its own separate strand (still both created together as one Undo step), matching how real string art commonly ties off and starts a fresh piece of thread rather than forcing one unbroken line through unrelated points. New `EditorStore.twoPinSequence.test.ts` (3 cases) verifies the store-level commit produces 2 separate `ThreadPath`s with the exact expected `pinIds` per arc, bundled as one atomic undo/redo; 3 of the Case-1-closed-path tests in `twoPinSequence.test.ts` updated to assert `sequence`/`extraSequences` separately instead of one concatenated array — 997 tests total (+3 over the previous row's 994). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and affected Gherkin scenarios rewritten to describe two separate Thread Paths. `npm run build`/`npx vitest run`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook reproducing the exact reported 141-pin scenario (pins 107→37, Zig-zag, full-fill on): exactly 2 separate Thread Paths created (72 and 71 pins), the first ending cleanly at `[...,141,3,1,2]` with no trailing jump, the second starting fresh at `(107,37)` with no connecting segment between them, both as one atomic Undo/Redo step; screenshotted a clean symmetric parabolic-weave pattern (previously showed one long stray diagonal chord cutting through the middle); no new console-error classes. **Post-ship correction #4 (2026-09-21, found by the user via real use): Parabolic's closed-path full-fill used the WRONG shape entirely — it needed a single continuous constant-offset walk, not the two-separate-arcs shape correction #3 had just fixed Zig-zag to use.** The user supplied a real reference (clicking pins 125 & 22 on a 141-pin ring) showing the expected result keeps the SAME (A,B) offset while both pins advance together — `[125,22, 126,23, 127,24, ..., 123,20, 124,21]`, one continuous 141-pair strand, no closing repeat of the first pair — completely different from the two-bounded-arcs shape. New `ringWalkSequence()` in `twoPinSequence.ts`: extracts one full lap from each anchor (`extractIds(..., dir=1, count=pinCount, closed=true, ...)`), strides each independently by its own Step A/Step B, and interleaves them — reusing the exact same `extractIds`/`strideList`/`interleave` primitives every other case already uses, no new pairing math, just a different extraction shape (a full lap per anchor instead of a bounded arc). `computeSamePathCandidates`'s closed-path-fullFill branch now forks by tool: Parabolic uses `ringWalkSequence` (repeated `cycles` times, each its own separate strand via `extraSequences`, same "don't concatenate" reasoning as correction #3); Zig-zag is completely unaffected, keeping the two-separate-arcs shape correction #3 shipped. Rewrote the Parabolic-specific Case-1-closed-path tests in `twoPinSequence.test.ts` and added an explicit Zig-zag-is-unaffected test plus a direct reproduction of the exact reported 141-pin/pins-125-and-22 example (asserting the precise first-10 and last-2 elements, 282-element total length, and that the `(125,22)` pair itself never repeats) — 1000 tests total (+3 over the previous row's 997). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and Gherkin scenarios split into separate Zig-zag/Parabolic closed-path subsections to reflect the now-genuinely-different shapes. `npm run build`/`npx vitest run`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook reproducing the exact reported scenario (141-pin Circle, real clicks on pin 125 then pin 22, Parabolic full-fill): a single Thread Path, 282 pins, `first10 = [125,22,126,23,127,24,128,25,129,26]` and `last4 = [123,20,124,21]` — exact match to the user's worked example — committed as one atomic Undo/Redo step; screenshotted a clean cardioid/envelope-style ring pattern (matching the shape of the user's own "right" reference); no new console-error classes. **Post-ship correction #5 (2026-09-21, found by the user via real use): a stride coprime with the pin count silently truncated the ring walk to roughly half.** With `Step B` configured to skip 1 pin (stride 2) on a 119-pin ring (odd, so `gcd(119,2)=1`), the walk should visit all 119 positions before returning to its own start — but `ringWalkSequence` extracted a fixed `pinCount`-length window of *consecutive* raw positions and THEN strided that window, which silently truncates to ~half (60 pairs) instead of continuing to wrap until the walk's true cycle closes. Fixed with a proper modular-walk primitive: new `gcd()` + `modularWalk(path, startIndex, stride, groupIndex)` computes the correct cycle length as `pinCount / gcd(pinCount, stride)` and walks exactly that many hops directly (no intermediate "extract then re-stride" step); `ringWalkSequence` now calls `modularWalk` once per anchor (each can have a different cycle length when Step A ≠ Step B) and lets the existing `interleave` truncate to whichever is shorter, same shape as every other paired sequence in this module. Separately confirmed with the user (AskUserQuestion) that `cycles` repeating the exact same walk verbatim as fully-overlapping duplicate strands — not sweeping a new offset each time — is the intended, final behavior; no further change needed there. New tests in `twoPinSequence.test.ts` reproduce the exact reported 119-pin/pins-99-and-19/stride-2 scenario (confirming 119 pairs, not 60) and confirm `cycles=3` correctly repeats that corrected 119-pair walk 3 times — 1002 tests total (+2 over the previous row's 1000). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and Gherkin scenarios updated to describe the corrected cycle-length rule. `npm run build`/`npx vitest run`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook reproducing the exact reported scenario (119-pin Circle, real clicks on pin 99 then pin 19, Parabolic full-fill, Step B=1, Cycles=3): 3 separate 238-element (119-pair) Thread Paths, `first10 = [99,19,100,21,101,23,102,25,103,27]` matching the corrected math exactly, committed as one atomic Undo/Redo step; screenshotted a dense, fully-covered spiral/cardioid weave (previously a visibly sparse half-ring pattern); no new console-error classes. **Post-ship correction #6 (2026-09-21, found by the user via real use): differing Step A/Step B strides on a closed-path full-fill pair still used the wrong shape — correction #5's fix only handled the EQUAL-stride case correctly.** The user reported pins 124 and 15 on a 151-pin ring, Step A = 0 (stride 1), Step B = 2 (stride 3) — `ringWalkSequence` gave both anchors a full independent 151-pair modular cycle (151 being prime, both strides are coprime with it), but the real expected result stops much earlier. Root cause: "each anchor walks its own full cycle" (correction #5) is only well-defined when the two strides actually MATCH — lap over lap, a constant stride keeps the (A,B) offset constant, but differing strides make the offset drift, so there's no natural "return to start" to walk toward for the pair as a whole. Confirmed rule from the user: always look at the vertex/side with the LOWER step number — that side's own forward arc to the OTHER anchor becomes the walk's reference length; the higher-step side then takes exactly one fewer term at its own stride (no early stop of its own), and the lower side's now-partnerless final term is appended as a dangling tail instead of being dropped. `ringWalkSequence` forks on `strideA === strideB`: equal strides keep correction #5's `modularWalk`-based full-independent-cycle behavior unchanged (still verified against the 141-pin/pins-125-and-22 and 119-pin/pins-99-and-19-both-strides-equal examples); different strides use new `modularRun()` (a fixed-count strided walk, factored out of `modularWalk`) plus new `interleaveKeepingLongerTail()` (pairs to the shorter length, then appends whatever's left of the longer side instead of discarding it). This also corrects the existing 119-pin/pins-99-and-19/Step-B=1 unit test, which had asserted a full 119-pair walk for THAT differing-stride case too (1 vs 2) — re-verified against the real reference file (`.examples/string-art-parabolic-cicles3-right.json`) and found to actually match the new lower-step-bounded shape exactly (79 elements: 39 pairs plus a dangling 40th), not 119 pairs; the old expectation had never been checked against the file's full exact byte content, only its first 10 elements, which happen to be identical under both theories. Updated that test plus added a direct reproduction of the newly reported 124-and-15 example (asserting first-8/last-6 elements and the 85-element total length) — 1003 tests total (+1 over the previous row's 1002). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section split the closed-path Parabolic full-fill explanation into equal-stride/different-stride subsections, and one Gherkin scenario was corrected (`Step A and Step B both set to skip 1`, previously just `Step B`) plus a new one added for the differing-stride case. All `circles`/`cicles` references renamed to `cycles` throughout the codebase (types, state, store, UI labels, i18n keys AND translated values across all 5 locales, spec, this milestone's own notes) — scoped carefully to avoid touching unrelated pre-existing "circles" references elsewhere (M10's bounding-box note, M18/M25/M26's Generator Mode Freestyle-circles references). `npm run build`/`npx vitest run` (1003 tests)/`npx eslint .`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook reproducing the exact reported scenario (151-pin Circle, real clicks on pin 124 then pin 15, Parabolic full-fill, Step A=0/Step B=2): a single 85-element Thread Path, pin sequence byte-for-byte identical to the user's reference file (`.examples/string-art-parabolic-cycles1-right.json`) end to end, committed as one atomic Undo/Redo step; no new console-error classes. **Post-ship correction #7 (2026-09-21, found by the user via real use): correction #6's "lower-step-bounded, dangling tail" rule was itself wrong — full-fill must NEVER stop early, period.** The user reported the 124/15 differing-stride fix from correction #6 "is not even completing one turn" and clarified directly: "look at the path with the lower step number, it will have more pins — doesn't matter if the other path already reaches its initial pin, only continue." This overrides correction #6's interpretation of the reference file, which turned out to represent a bounded/partial case rather than the actual intended full-fill semantics. New, simpler unified rule replacing correction #6's branching logic entirely: the walk runs for as many terms as the LONGER of the two anchors' own natural cycle lengths (`pinCount / gcd(pinCount, stride)`, computed independently per anchor via new `cycleLength()`); the shorter-cycle anchor just keeps wrapping around and repeating its own cycle (via `modularRun`'s plain modulo indexing) for the remaining terms instead of ever being a stopping bound. This one formula now covers every case uniformly with no special-casing: equal strides (both cycles already the same length) reduce to correction #5's original full-independent-cycle shape unchanged; differing strides that are BOTH coprime with the pin count (as in 124/15 on a 151-pin ring, and 99/19 on a 119-pin ring) also both have the full pin count as their cycle length, so the walk still runs the complete ring with no early stop; only a stride whose `gcd` with the pin count exceeds 1 produces a genuinely shorter cycle for that side, which then visibly repeats within the longer walk. `ringWalkSequence` simplified back down (deleted correction #6's `interleaveKeepingLongerTail` and the `strideA === strideB` branch entirely) to one `Math.max(cycleLength(...), cycleLength(...))`-bounded `interleave` call. This also REVERTS correction #6's changes to the 99/19 unit test back to the original 119-pair expectation (the 79-element/dangling-tail result was specific to the now-superseded bounded-arc theory) and to the 124/15 test (now 151 pairs, matching the full-ring shape, instead of the reference file's 85-element bounded result) — plus a new synthetic test (a 12-pin ring, Step A stride 1 vs Step B stride 4, `gcd(12,4)=4` so B's own cycle is only 3 pins) directly verifying the wrap-and-repeat behavior the user described, hand-computed and cross-checked against a standalone script before being written into the test. 1004 tests total (+1 over the previous row's 1003). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section rewritten again to describe the never-stops-early rule (replacing correction #6's equal/differing-stride subsection split), and its differing-stride Gherkin scenario corrected to describe wrap-and-repeat instead of bounded-arc-with-dangling-tail. `npm run build`/`npx vitest run` (1004 tests)/`npx eslint .`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook reproducing the exact reported scenario (151-pin Circle, real clicks on pin 124 then pin 15, Parabolic full-fill, Step A=0/Step B=2): a single 302-element (151-pair) Thread Path, `first8 = [124,15,125,18,126,21,127,24]`, committed as one atomic Undo/Redo step through the real store, not just the pure function; no new console-error classes. **Post-ship correction #8 (2026-09-21, found by the user via real use): correction #7's `max(cycleA, cycleB)` stopping rule was still wrong in general — the pair only truly closes at `lcm(cycleA, cycleB)`.** User reported full-fill "not even completing one turn" again after correction #7, then diagnosed the actual root cause directly: computing each side's strided sequence independently and zipping them together afterwards is the wrong shape of algorithm — it should be one combined walk with a control variable per side. `max(cycleA, cycleB)` only coincides with the true closing point when one cycle length happens to divide the other, which was true of every case tested so far by coincidence; when neither divides the other (e.g. a 12-pin ring, cycle lengths 4 and 6 — `max=6`, but the pair doesn't return to its own start until `lcm(4,6)=12`), `max` cuts the walk off halfway through the true pattern. `ringWalkSequence` rewritten around the user's suggested shape: split into `pairCycleLength()` (a single loop advancing two cursors together, one per side, stopping when both are back at their own start at the same step — this computes `lcm(cycleA, cycleB)` by construction, no `gcd`/`lcm` helper needed) and `ringSide()` (one side's own sequence out to that shared hop count); `ringWalkSequence` itself is now just those two calls plus `interleave`. Also addressed separate, unprompted feedback on code style: the previous version carried a long multi-paragraph comment explaining the stopping-condition derivation and its history of being wrong twice — user asked for comments to be strictly necessary and objective, and for complex logic to be split into small named functions instead of one function with a big comment; this rewrite follows that going forward. Added a new unit test specifically constructed to distinguish `lcm` from `max` (cycle lengths 4 and 6 on a 12-pin ring, hand-verified against a standalone script before being written into the test) — the existing synthetic test from correction #7 didn't actually distinguish the two theories (its cycle lengths happened to divide evenly). 1005 tests total (+1 over the previous row's 1004). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and its differing-stride Gherkin scenario rewritten to describe the `lcm`-based stopping rule. `npm run build`/`npx vitest run` (1005 tests)/`npx eslint .`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook on a real 12-pin Circle (Step A=2/stride 3, Step B=1/stride 2 — cycle lengths 4 and 6, `lcm=12`): a single 24-element Thread Path exactly matching the unit test's hand-derived sequence, committed through the real store; no new console-error classes. **Post-ship correction #9 (2026-09-21, found by the user via real use): correction #8's `lcm(cycleA, cycleB)` rule was based on a mistaken description the user gave earlier and later retracted — the walk should stop at `min`, not `lcm`.** The user explicitly corrected their own prior guidance ("sorry, I made a mistake"): the walk should stop the moment ANY vertex reaches/passes its own origin pin, counting that as one circle — not continue until the PAIR jointly realigns. Confirmed the equal-stride case was never in question ("for steps with the same value it is working perfect"); the mistake was specific to the earlier "doesn't matter if the other path already reaches its initial pin, only continue" description from correction #7, which is what led to both correction #7's `max` and correction #8's `lcm` — both wrong in the same direction (continuing past the first anchor's own return instead of stopping there). Simplified `ringWalkSequence` back to independent per-anchor cycle lengths (`gcd`/`cycleLength` restored) with `Math.min(cycleLength(n, strideA), cycleLength(n, strideB))` as the hop count — no joint-cursor simulation needed, since stopping at the first EITHER anchor returns is just the smaller of two independently-computable values; `pairCycleLength` (correction #8's joint-loop helper, no longer needed) deleted. Every previously-confirmed example is unaffected: 125/22 (both stride 1), 99/19 and 124/15 (differing strides that both happen to be coprime with a prime/odd pin count) all have EQUAL cycle lengths on both sides by coincidence, so `min` gives the same result `max`/`lcm` did; only the two synthetic 12-pin tests (the ones specifically constructed to have genuinely different cycle lengths) actually change — rewritten from expecting 12/12 pairs to the correct 3/4 pairs (whichever anchor's own cycle is shorter: Step B=3/stride 4 → cycle length 3 in one; Step A=2/stride 3 → cycle length 4 in the other), hand-verified against a standalone script before being written into the tests. 1005 tests total (unchanged count — two tests rewritten in place, none added or removed). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and differing-stride Gherkin scenario rewritten again to describe the `min`-based stopping rule. `npm run build`/`npx vitest run` (1005 tests)/`npx eslint .`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook on a real 12-pin Circle (Step A=0/stride 1, Step B=3/stride 4): a single 6-element Thread Path `[1,2,2,6,3,10]` (3 pairs, stopping once B returns to its own start), matching the unit test exactly, committed through the real store; no new console-error classes. Separately, split the previous correction's long explanatory comment into small named helper functions per the user's explicit standing preference (see this session's memory) — no logic change from that, verified by the unchanged test count before this correction. **Post-ship correction #10 (2026-09-21, found by the user via real use): correction #9's `min(cycleA, cycleB)` used exact-return cycle lengths (`n/gcd(n,stride)`), but a side doesn't need to land exactly back on its own origin to count as stopped — overshooting past it counts too.** User precision: "it don't need to reach the exact origin, it only should pass by origin — if the origin is 12 for a path with step equals 2 and it passes over and reaches pin 13, it is meaning that it passed over the origin." Root cause: `n/gcd(n,stride)` is the hop count for an EXACT return to the starting index, which is only reachable when `stride` divides `n`; whenever it doesn't (the overwhelmingly common case — coprimality with `n`, the previous correction's implicit assumption via `gcd`, is unrelated to whether `stride` divides `n`), the walk never lands on its own origin at all and `min(cycleA, cycleB)` let it keep going for its full (misleadingly large) coprime cycle length instead of stopping the first time it swept past its own start. New `hopsToPassOwnOrigin(n, stride)`: the first hop whose cumulative offset (`hop × stride`) is at least `n` — computed as `Math.ceil(n / stride)`, PLUS one more hop if `stride` doesn't divide `n` exactly (that extra hop is the one that overshoots onto a genuinely new, not-yet-visited pin, which the user confirmed should be included; an exact multiple lands ON the origin itself, which is excluded so the pair isn't redundantly repeated — this preserves the long-confirmed stride-1 behavior of stopping one hop before closing the loop). `ringWalkSequence` now takes `Math.min(hopsToPassOwnOrigin(n, strideA), hopsToPassOwnOrigin(n, strideB))`; `cycleLength`/`gcd` (correction #9's helpers) removed as no longer used. Regression impact: every equal-stride example is unaffected (stride always divides `n` when compared against itself). Of the differing-stride examples, both 12-pin synthetic tests happen to use strides that are exact divisors of 12, so they're also unaffected; the 99/19 (119-pin, Step B=1) and 124/15 (151-pin, Step B=2) examples DO change, since their strides (2 and 3) don't divide their odd/prime pin counts — 99/19 now stops at 61 pairs (was 119), 124/15 now stops at 52 pairs (was 151); both rewritten with hand-verified exact sequences (cross-checked against a standalone script). Added a new test on a clean 10-pin ring (Step A=0/stride 1, Step B=2/stride 3) specifically demonstrating the overshoot-inclusion behavior with small, easy-to-verify numbers. 1006 tests total (+1 net: two tests rewritten in place, one new test added). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and Gherkin scenario rewritten again to describe the reach-or-pass rule (exact landing excluded, overshoot included). `npm run build`/`npx vitest run` (1006 tests)/`npx eslint .`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook on a real 10-pin Circle (Step A=0/stride 1, Step B=2/stride 3): a single 10-element Thread Path `[1,2,2,5,3,8,4,1,5,4]`, B's walk correctly stopping on pin 4 (the pin it overshoots onto) rather than continuing toward its own exact origin (pin 2, which it never actually revisits within its own stride-3 cycle on a 10-pin ring), matching the unit test exactly, committed through the real store; no new console-error classes. **Post-ship correction #11 (2026-09-21, found by the user via real use): Parabolic's `cycles>1` wasn't extending the walk at all — it just repeated the SAME short cycles=1 pattern as separate overlapping strands, which stopped meaning anything once a single cycle became a short partial arc (correction #10) instead of always being a full lap.** User's diagnosis, with pseudocode: track how many times a side has reached/passed its own origin with a counter, and only actually stop once that counter reaches `cycles` — i.e. cycles should CONTINUE the same walk, not restart it. `hopsToPassOwnOrigin` generalized to take `cycles` directly: the stopping hop is now the first one whose cumulative offset reaches `cycles × pinCount` (still exact-landing-excluded / overshoot-included per side, same rule as correction #10, just evaluated against a `cycles`-scaled target instead of a single pinCount) — `ringWalkSequence` takes `settings.cycles` directly and returns ONE (now correctly longer) sequence. `computeSamePathCandidates`'s Parabolic branch no longer wraps `ringWalkSequence` in `Array.from({length: cycles}, ...)` — calls it once and returns a single candidate with no `extraSequences`; Zig-zag's branch is unchanged (still separate strands per cycle, since its two-arc shape has no origin-passing ambiguity to begin with and was never part of this bug report). Net effect: `cycles=1` is unaffected (identical to before); `cycles=N`'s sequence is now always an exact prefix-extension of `cycles=(N-1)`'s, verified directly (`cycles=3`'s first 122 elements on the 99/19 example equal `cycles=1`'s entire sequence). This is a genuine behavior change from the EARLIER user-confirmed "3 identical overlapping copies" answer for the equal-stride case (back when a single cycle was always a full lap, so repeating it identically as separate strands and extending it as one continuous walk were visually indistinguishable) — now that a single cycle can be a short partial arc (correction #10), continuing is what actually achieves "full-fill," so this correction applies the continuation model uniformly rather than only to the previously-broken differing-stride case. Rewrote both `cycles=3` tests (8-pin equal-stride, and 119-pin/pins-99-19 differing-stride) to assert the prefix-extension property instead of separate-strand equality; both hand-verified against a standalone script. 1006 tests total (unchanged — two tests rewritten in place). `docs/specs/35-zigzag-parabolic-tools.md`'s Configuration section and both affected Gherkin scenarios (closed-path full-fill, and Cycles) rewritten to describe the continuation model. `npm run build`/`npx vitest run` (1006 tests)/`npx eslint .`/`tsc -b --force` all clean. Live-verified via Playwright + the debug hook on a real 10-pin Circle (Step A=0/stride 1, Step B=2/stride 3, Cycles=2): a single 16-element Thread Path `[1,2,2,5,3,8,4,1,5,4,6,7,7,10,8,3]`, whose first 10 elements exactly match the Cycles=1 result before continuing further, matching the unit test exactly, committed through the real store as one atomic Undo/Redo step; no new console-error classes. **This work is complete but not yet committed or pushed — the user asked to validate first.** |
| M30 | Component Styling Migration (inline style to CSS files) | — | In Progress | Batches 1-3/7 done. Batch 3: new shared `OverlayPanel.tsx`/`.css` (+ split-out `OverlayPanelHeader`) and `.tab-underline`/`.tab-underline--active` in theme.css (declared both bare and `.btn`-compounded, since LayersPanel's tabs and HelpPanel's tabs had different pre-migration hover behavior — preserved exactly, not unified); StatisticsPanel/HelpPanel/AboutTabContent/LayersPanel migrated. Live-verified via Playwright, guards green. Remaining: PrintPreviewPanel (batch 4, will consume `OverlayPanel` but keep its own header markup — different sizing, not drift), toolbars+canvas (batch 5), remaining panels (batch 6), EditorShell (batch 7, also closes the 3 remaining EditorShell border/borderColor warning instances). |

Status values: `Not Started`, `In Progress`, `Blocked` (note the blocker), `Done`.

## Definition of Done (applies to every milestone)

- [ ] All Gherkin scenarios in the milestone's listed specs exist as automated tests and pass.
- [ ] No domain/application code imports React or `src/ui/` (M0's boundary check stays green).
- [ ] All new mutations route through a `Command` (undoable), not direct state assignment.
- [ ] UI matches [18-design-system.md](../specs/18-design-system.md) tokens/components (no ad-hoc colours, fonts, radii, or one-off component patterns).
- [ ] Status Table row updated to `Done` with a one-line note (what shipped, any deferred edge case).
- [ ] Any spec correction made during implementation is committed alongside the code change, not left as a mental note.
