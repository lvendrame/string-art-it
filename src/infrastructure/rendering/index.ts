// Canvas/SVG draw adapters shared by the editor renderer. Print/export renderers reuse
// the same Document Engine + Geometry Engine outputs (see docs/specs/01-architecture.md
// §Rendering Architecture) rather than duplicating geometry logic.
// Filled in at M2 — Board & Viewport. See docs/specs/05-canvas-and-viewport.md.
export {};
